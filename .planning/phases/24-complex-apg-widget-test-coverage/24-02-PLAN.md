---
phase: 24-complex-apg-widget-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/DatePicker/DatePicker.test.tsx
autonomous: true
requirements:
  - TC-10
task_count: 1
must_haves:
  truths:
    - "DatePicker.test.tsx exists and is picked up by vitest (test:ci green)"
    - "Test file carries WCAG-SC JSDoc header (test:wcag-headers count rises to 19)"
    - "Tier 1 describe asserts what the source actually implements (type=date input, label htmlFor, ref forwarding, className passthrough, description/error wiring, aria-invalid, aria-describedby chain)"
    - "Tier 2 describe asserts axe-clean smokes AND no-throw stub for the full APG dialog-grid keystroke matrix (Arrow/Home/End/PageUp/PageDown/Shift+PageUp/Shift+PageDown/Enter/Escape) per D-01 RadioGroup pattern"
    - "Implementation-note JSDoc block records that APG dialog-grid is NOT implemented (native input type=date) and references TC-10-IMPL + TC-10-LIVE backlog items"
    - "0 querySelector / 0 configureAxe / 0 toMatchSnapshot in the file (D-02a)"
  artifacts:
    - path: "packages/components/src/DatePicker/DatePicker.test.tsx"
      provides: "Tier 1+2 coverage for TC-10 with D-01 stub strategy"
      contains: "describe('Tier 1: Table Stakes'"
  key_links:
    - from: "DatePicker.test.tsx"
      to: "packages/components/src/_test/helpers"
      via: "import { expectNoAxeViolations, expectUniqueIds }"
      pattern: "from '../_test/helpers'"
    - from: "DatePicker.test.tsx"
      to: "no-throw stub pattern"
      via: "D-01 RadioGroup pattern reference"
      pattern: "not\\.toThrow\\(\\)"
---

<objective>
Add Tier 1 + Tier 2 test coverage for `DatePicker` (TC-10) using the D-01 RadioGroup no-throw pattern, since the source is a native `<input type="date">` and implements NONE of the APG dialog-grid keyboard contract.

Purpose: Pin the (small) real contract — label wiring, aria-invalid toggling, aria-describedby chain, ref forwarding — and stub out the APG keystrokes so that when v0.7 lands the dialog-grid implementation (TC-10-IMPL), the no-throw stubs flip naturally into real assertions without test churn.

Output: `packages/components/src/DatePicker/DatePicker.test.tsx` (~10-12 `it()` blocks, Tier 2 keyboard collapses to a single parametrised no-throw test; axe-clean smokes; JSDoc note referencing TC-10-IMPL and TC-10-LIVE backlog).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-complex-apg-widget-test-coverage/24-CONTEXT.md
@.planning/phases/24-complex-apg-widget-test-coverage/24-RESEARCH.md

@packages/components/src/DatePicker/DatePicker.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/RadioGroup/RadioGroup.test.tsx
@packages/components/src/_test/helpers.ts
@packages/components/TESTING-CONVENTIONS.md

<interfaces>
<!-- DatePicker public API + verified source state (2026-05-11). -->
<!-- Source is a native <input type="date"> — no calendar grid, no dialog, no onKeyDown handler. -->

From packages/components/src/DatePicker/DatePicker.tsx:
```typescript
export interface DatePickerProps extends React.InputHTMLAttributes<HTMLInputElement> {
  label: string;
  description?: string;
  error?: string;
  className?: string;
}
export const DatePicker = forwardRef<HTMLInputElement, DatePickerProps>(...)
```

Source-side facts that constrain test assertions:
- `<input type="date" aria-invalid={!!error} aria-describedby={...chained ids or undefined}>`
- `<label htmlFor={id}>` — `id` accepts `providedId` prop OR falls back to `useId()`
- `<div id={descriptionId}>` rendered only when `description` is provided
- `<div id={errorId} role="alert">` rendered only when `error` is provided
- NO `onKeyDown` handler on the input (line 97-110 only spreads `...props`)
- NO `<LiveRegion>` / aria-live anywhere except the error role=alert
- forwardRef returns the inner `<input>` (line 98 `ref={ref}`)
- All extra HTML attributes spread to the input via `...props` (line 109)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write DatePicker.test.tsx with Tier-1 real + Tier-2 no-throw stub per D-01</name>
  <files>packages/components/src/DatePicker/DatePicker.test.tsx</files>
  <behavior>
    Tier 1 (~7 tests):
    - mounts and the input has type="date"
    - label is wired via htmlFor (getByLabelText returns the input)
    - forwards ref to the underlying <input> (probe pattern from Button.test.tsx)
    - passes className through additively on the outer container
    - description renders and its id appears in the input's aria-describedby
    - error renders with role="alert", its id appears in aria-describedby, and aria-invalid="true"
    - arbitrary HTML attributes pass through (e.g., min, max, name, data-* — assert via toHaveAttribute)

    Tier 2 (~4-5 tests):
    - APG dialog-grid keystrokes do NOT throw on the focused input (parametrised it.each over: ArrowLeft, ArrowRight, ArrowUp, ArrowDown, Home, End, PageUp, PageDown, {Shift>}{PageUp}{/Shift}, {Shift>}{PageDown}{/Shift}, Enter, Escape). Use `userEvent.keyboard`; assert the input is still in the document AND no exception is raised AND aria-invalid did not change spuriously.
    - axe-clean for default render
    - axe-clean for error-state render (description + error + aria-invalid all engaged)
    - axe-clean for description-only render (positive flow)
    - two `<DatePicker>` instances render with no duplicate ids (`expectUniqueIds`)

    Anti-patterns (D-02a) — ZERO of each:
    - querySelector / querySelectorAll
    - configureAxe (use only `expectNoAxeViolations`)
    - toMatchSnapshot
  </behavior>
  <action>
    Create `packages/components/src/DatePicker/DatePicker.test.tsx` mirroring the `RadioGroup.test.tsx` no-throw pattern for the Tier-2 keystroke block, and the `Button.test.tsx` structure for Tier-1.

    Per D-04, WCAG-SC JSDoc header covering:
    - 1.3.1 Info and Relationships (label htmlFor; description/error wired via aria-describedby)
    - 2.1.1 Keyboard (no-throw matrix — D-01 stub strategy)
    - 4.1.2 Name, Role, Value (input type=date is the accessible-name vehicle; aria-invalid; role=alert on error)
    - Do NOT list 4.1.3 (no live region) or 2.4.3 (no focus management beyond the native input).

    Add an explicit `Implementation note:` block in the JSDoc recording:
    - APG dialog-grid contract (role=grid calendar, gridcell day cells, Arrow/Home/End/PageUp/PageDown/Shift+PageUp/Shift+PageDown/Enter/Escape navigation) is NOT implemented — source uses native `<input type="date">` per a deliberate accessibility choice (line 26-29 component JSDoc).
    - Tier-2 keystroke tests are no-throw stubs per D-01 (Phase 22 RadioGroup pattern). When TC-10-IMPL lands the dialog-grid handler in v0.7, these stubs upgrade to real assertions without test churn.
    - Live-region (selected-date announcement) is NOT rendered. Deferred to TC-10-LIVE backlog.
    - jsdom does not process `<input type="date">` keyboard like a browser (Pitfall 3); a passing no-throw stub is testing structural sanity, NOT date-change behavior.

    Imports:
    ```ts
    import { useRef } from 'react';
    import { render, screen } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import { describe, it, expect } from 'vitest';
    import { DatePicker } from './DatePicker';
    import { expectNoAxeViolations, expectUniqueIds } from '../_test/helpers';
    ```

    Reference RESEARCH §8 (24-02 skeleton, lines 651-673) for `it()` titles and structure. Reference RESEARCH §"Code Examples" "D-01 no-throw stub for DataTable cell-nav" for the no-throw assertion shape (adapted to `userEvent.keyboard` since the input IS focusable — fireEvent escape hatch not needed here).

    For the parametrised no-throw test, use `it.each(['{ArrowDown}', '{ArrowUp}', ...])` and inside the test: focus the input, call `await user.keyboard(key)`, assert the input is still in the document and `aria-invalid` did NOT flip from the no-error baseline.

    For the ref-forwarding test, use the `useRef` + Probe pattern from Button.test.tsx (lines 32-51). Assert the captured node is the input element (not the wrapper).

    NEVER touch `package.json`, `tsconfig.json`, or any source file outside `packages/components/src/DatePicker/DatePicker.test.tsx`.
  </action>
  <verify>
    <automated>cd packages/components && npx vitest run src/DatePicker/DatePicker.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - `packages/components/src/DatePicker/DatePicker.test.tsx` exists
    - `npx vitest run src/DatePicker/DatePicker.test.tsx` reports 10-12 passing tests, 0 failures
    - `npm run test:wcag-headers` count rises by 1
    - Full repo `npm test -w @holmdigital/components` stays green
    - `grep -c "querySelector\|configureAxe\|toMatchSnapshot" packages/components/src/DatePicker/DatePicker.test.tsx` returns 0
    - At least one `expectNoAxeViolations(` call present
    - JSDoc header contains both "TC-10-IMPL" and "TC-10-LIVE" backlog references
    - At least one `not.toThrow(` or equivalent no-throw assertion in Tier 2
  </done>
</task>

</tasks>

<verification>
- Vitest run of the new file passes all assertions
- Full components-package test run remains green (no regressions)
- WCAG-SC marker scan counts the new file
- D-02a anti-pattern grep returns zero hits in the new file
- File ships at least one axe-clean smoke (D-03)
- JSDoc header documents TC-10-IMPL + TC-10-LIVE backlog references and the native-input rationale
</verification>

<success_criteria>
- TC-10 acceptance criterion 2: tests assert what source implements (Tier 1) + no-throw for the APG-keyboard gap (Tier 2) per D-01
- Live-region success-criterion bullet explicitly deferred via TC-10-LIVE backlog reference in JSDoc
- APG dialog-grid gap explicitly deferred via TC-10-IMPL backlog reference in JSDoc
- axe-clean for default AND error render
</success_criteria>

<output>
After completion, create `.planning/phases/24-complex-apg-widget-test-coverage/24-02-SUMMARY.md` recording final test count, the keystrokes in the no-throw matrix, and confirmation of axe-clean + zero anti-pattern hits.
</output>
