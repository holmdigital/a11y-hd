---
phase: 24-complex-apg-widget-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/MultiSelect/MultiSelect.test.tsx
autonomous: true
requirements:
  - TC-11
task_count: 1
must_haves:
  truths:
    - "MultiSelect.test.tsx exists and is picked up by vitest (test:ci green)"
    - "Test file carries WCAG-SC JSDoc header"
    - "Tier 1 describe asserts role=combobox input + listbox-on-open + chip rendering + onChange contract"
    - "Tier 2 describe asserts the keystrokes the source DOES implement (ArrowDown/Up open + activedescendant, Enter selects, Escape closes, Backspace removes last chip) AND no-throw stubs for missing APG behaviors (Space-toggle, Shift+ArrowDown/Up extend)"
    - "Chip remove <button> has aria-label='Remove <label>'; Enter and Space both activate (paired keyboard per D-02a)"
    - "Implementation-note JSDoc block records aria-multiselectable + Space-toggle + Shift+Arrow + live-region gaps; references TC-11-IMPL + TC-11-LIVE backlog"
    - "0 querySelector / 0 configureAxe / 0 toMatchSnapshot in the file (D-02a)"
  artifacts:
    - path: "packages/components/src/MultiSelect/MultiSelect.test.tsx"
      provides: "Tier 1+2 coverage for TC-11 with partial-stub strategy per D-01"
      contains: "describe('Tier 1: Table Stakes'"
  key_links:
    - from: "MultiSelect.test.tsx"
      to: "packages/components/src/_test/helpers"
      via: "import { expectNoAxeViolations, expectUniqueIds }"
      pattern: "from '../_test/helpers'"
    - from: "MultiSelect.test.tsx"
      to: "chip remove button keyboard activation"
      via: "Enter + Space paired per D-02a"
      pattern: "Remove "
---

<objective>
Add Tier 1 + Tier 2 test coverage for `MultiSelect` (TC-11). Apply the partial-stub strategy per D-01: test the substantial set of APG behaviors the source DOES implement (combobox-input + listbox popup + Enter-select + Escape-close + Backspace-removes-chip), and no-throw stub the gaps (aria-multiselectable absent, aria-selected hardcoded false, Space types literal space, no Shift+Arrow extend, no live region).

Purpose: Pin the real listbox-multi surface NOW so v0.7 TC-11-IMPL (aria-multiselectable + Space-toggle + Shift+Arrow + live region) can land without silent regressions to chip removal and Backspace UX.

Output: `packages/components/src/MultiSelect/MultiSelect.test.tsx` (~13-16 `it()` blocks; axe-clean smokes for default + with-selection; JSDoc note referencing TC-11-IMPL and TC-11-LIVE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-complex-apg-widget-test-coverage/24-CONTEXT.md
@.planning/phases/24-complex-apg-widget-test-coverage/24-RESEARCH.md

@packages/components/src/MultiSelect/MultiSelect.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/RadioGroup/RadioGroup.test.tsx
@packages/components/src/_test/helpers.ts
@packages/components/TESTING-CONVENTIONS.md

<interfaces>
<!-- MultiSelect public API + verified source state (2026-05-11). -->

From packages/components/src/MultiSelect/MultiSelect.tsx:
```typescript
export interface MultiSelectOption { value: string; label: string; }
export interface MultiSelectProps {
  label: string;
  description?: string;
  error?: string;
  options: MultiSelectOption[];
  selected: string[];                            // CONTROLLED — required prop
  onChange: (selected: string[]) => void;
  placeholder?: string;                          // default 'Select items...'; rendered only when selected.length === 0
  className?: string;
}
```

Source-side facts (verified against MultiSelect.tsx 2026-05-11):
- `<input role="combobox" aria-autocomplete="list" aria-expanded={isOpen} aria-haspopup="listbox" aria-controls={listboxId} aria-activedescendant={...}>`
- `<ul role="listbox" aria-label={label}>` rendered ONLY when `isOpen && availableOptions.length > 0` (gated by BOTH conditions — Pitfall 1)
- `<li role="option" aria-selected={false}>` — aria-selected HARDCODED to false (line 278); never reflects state (gap)
- NO `aria-multiselectable` attribute anywhere (gap; ROADMAP calls for it on the listbox)
- `availableOptions` excludes already-selected (line 55-58), so options in the popup are always not-yet-selected
- Chip render: `<span>{label}<button aria-label="Remove {label}">×</button></span>` (lines 220-235); chips themselves do not take focus
- onFocus on input sets isOpen=true (line 254) — so the listbox opens on focus alone if availableOptions is non-empty
- Keyboard handler (line 73-114): ArrowDown/Up sets isOpen=true + updates focusedIndex; Enter selects available option; Escape closes; Backspace on empty input removes last chip
- Space has NO handler — Space inserts literal space into the input (gap)
- Shift+ArrowDown/Up has NO modifier branch (gap)
- NO `<LiveRegion>` / aria-live (gap)
- Removing a chip calls `inputRef.current?.focus()` (line 70) — focus returns to input
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write MultiSelect.test.tsx with partial-stub coverage per D-01</name>
  <files>packages/components/src/MultiSelect/MultiSelect.test.tsx</files>
  <behavior>
    Tier 1 (~6 tests):
    - mounts with role="combobox" on the input; label wired via htmlFor
    - passes className through additively on the outer container
    - selected items render as visible chips (assert each chip's text content via getByText)
    - each chip's remove button has aria-label="Remove {label}" (assert via getByRole('button', { name: /remove apple/i }))
    - controlled `selected={[value]}` renders the matching chip; empty selected renders the placeholder
    - clicking an option in the listbox fires onChange with [...selected, value]

    Tier 2 (~9-11 tests):
    - ArrowDown on the focused input opens the listbox (aria-expanded flips to "true") AND moves aria-activedescendant to option-0
    - ArrowUp on the focused input opens the listbox AND moves aria-activedescendant to last option (wrap-from-(-1)-to-last branch line 88)
    - Enter on a focused available option calls onChange with [...selected, value] AND closes the listbox (aria-expanded flips back)
    - Escape closes the listbox without calling onChange
    - Backspace on EMPTY input fires onChange with `selected.slice(0,-1)` (removes last chip)
    - Backspace on NON-empty input does NOT fire onChange (the inputValue clears first)
    - Clicking a chip's remove button fires onChange with that value removed AND focus returns to the input (assert document.activeElement === input)
    - Enter on a focused chip remove button fires onChange (paired keyboard per D-02a)
    - Space on a focused chip remove button fires onChange (paired keyboard per D-02a; native <button> Space=click)
    - APG-gap keystrokes do NOT throw on the focused input (parametrised it.each: 'a' [Space-toggle expected but inserts char], '{Shift>}{ArrowDown}{/Shift}', '{Shift>}{ArrowUp}{/Shift}') — assert input still in DOM, no exception. Reference TC-11-IMPL inline.
    - axe-clean for default render (selected=[])
    - axe-clean for with-selection render (selected=['a','b'])
    - two `<MultiSelect>` instances render with no duplicate ids (`expectUniqueIds`)

    Anti-patterns (D-02a) — ZERO of each:
    - querySelector / querySelectorAll
    - configureAxe
    - toMatchSnapshot
  </behavior>
  <action>
    Create `packages/components/src/MultiSelect/MultiSelect.test.tsx` mirroring `Button.test.tsx` structure and using the `RadioGroup.test.tsx` no-throw pattern for the APG-gap row.

    Per D-04, WCAG-SC JSDoc header covering:
    - 1.3.1 Info and Relationships (label htmlFor; listbox referenced by aria-controls; chip aria-label)
    - 2.1.1 Keyboard (real for ArrowDown/Up/Enter/Escape/Backspace; no-throw for Space-toggle/Shift+Arrow)
    - 2.4.3 Focus Order (focus stays on input; chip remove returns focus to input)
    - 4.1.2 Name, Role, Value (role=combobox, role=listbox, role=option, aria-expanded, aria-controls, aria-activedescendant, aria-label on chip buttons)
    - Do NOT list 4.1.3 (no live region).

    Add an explicit `Implementation note:` block in the JSDoc recording:
    - APG listbox-multi gaps NOT implemented in source: `aria-multiselectable="true"` missing; `aria-selected` hardcoded to `false` on every option; Space-on-input types a literal space (no toggle handler); Shift+ArrowDown/Up does NOT extend selection.
    - These gaps are deferred to v0.7 backlog item TC-11-IMPL.
    - Live-region (selection count announcement) is NOT rendered — deferred to TC-11-LIVE backlog.
    - Tier-2 covers what the source DOES implement; the listed APG-gap keystrokes are no-throw stubs per D-01.
    - Chip navigation by ArrowLeft (line 104-112) is a comment-only placeholder in source — NO test for it (silent omission, no backlog; matches D-07-style handling of source-acknowledged TODO comments).

    Imports:
    ```ts
    import { render, screen } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import { describe, it, expect, vi } from 'vitest';
    import { MultiSelect } from './MultiSelect';
    import { expectNoAxeViolations, expectUniqueIds } from '../_test/helpers';
    ```

    Test data:
    ```ts
    const OPTIONS = [
      { value: 'a', label: 'Apple' },
      { value: 'b', label: 'Banana' },
      { value: 'c', label: 'Cherry' },
    ];
    ```

    Because the source REQUIRES the `selected` prop (no default), all tests must pass `selected={[]}` or `selected={['a']}` etc. Use `vi.fn()` for `onChange` and assert exact call shapes.

    Reference RESEARCH §8 (24-03 skeleton, lines 675-701) for `it()` titles.

    For the wrap-from-(-1)-to-last ArrowUp test, note that the source line 88 `prev > 0 ? prev - 1 : availableOptions.length - 1` wraps from -1 (initial) to last because -1 is not > 0. Reflect this exactly.

    For the chip-remove-button keyboard tests, query via `screen.getByRole('button', { name: /remove apple/i })` (per CLAUDE.md: chip remove button has aria-label="Remove {label}").

    NEVER touch `package.json`, `tsconfig.json`, or any source file outside `packages/components/src/MultiSelect/MultiSelect.test.tsx`.
  </action>
  <verify>
    <automated>cd packages/components && npx vitest run src/MultiSelect/MultiSelect.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - `packages/components/src/MultiSelect/MultiSelect.test.tsx` exists
    - `npx vitest run src/MultiSelect/MultiSelect.test.tsx` reports 13-16 passing tests, 0 failures
    - `npm run test:wcag-headers` count rises by 1
    - Full repo `npm test -w @holmdigital/components` stays green
    - `grep -c "querySelector\|configureAxe\|toMatchSnapshot" packages/components/src/MultiSelect/MultiSelect.test.tsx` returns 0
    - At least one `expectNoAxeViolations(` call present
    - JSDoc header contains "TC-11-IMPL" and "TC-11-LIVE" backlog references
  </done>
</task>

</tasks>

<verification>
- Vitest run of the new file passes all assertions
- Full components-package test run remains green
- WCAG-SC marker scan counts the new file
- D-02a anti-pattern grep returns zero hits
- File ships at least one axe-clean smoke
- JSDoc header documents the listbox-multi gaps + TC-11-IMPL + TC-11-LIVE backlog references
</verification>

<success_criteria>
- TC-11 acceptance criterion 3: real coverage for the implemented portion of the APG listbox-multi contract (combobox input, listbox-on-open, Enter selects, Escape closes, chip remove, Backspace-removes-last)
- Gap coverage: aria-multiselectable absence, hardcoded aria-selected, Space-toggle absence, Shift+Arrow absence all documented in JSDoc with TC-11-IMPL backlog reference
- Live-region gap documented with TC-11-LIVE backlog reference
- D-02a paired-keyboard requirement satisfied for chip remove (Enter + Space both tested)
</success_criteria>

<output>
After completion, create `.planning/phases/24-complex-apg-widget-test-coverage/24-03-SUMMARY.md` recording final test count, which APG keystrokes are no-throw stubs vs real assertions, and confirmation of axe-clean + zero anti-pattern hits.
</output>
