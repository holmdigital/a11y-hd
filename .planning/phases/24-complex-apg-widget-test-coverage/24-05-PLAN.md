---
phase: 24-complex-apg-widget-test-coverage
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/TreeView/TreeView.test.tsx
autonomous: true
requirements:
  - TC-13
task_count: 1
must_haves:
  truths:
    - "TreeView.test.tsx exists and is picked up by vitest (test:ci green)"
    - "Test file carries WCAG-SC JSDoc header"
    - "Tier 1 describe asserts role=tree + aria-label, role=treeitem per visible node, clicking selects + toggles expand, onSelect callback"
    - "Tier 2 describe asserts the full APG tree contract the source implements: aria-expanded only on parents, aria-level, roving tabindex single-zero invariant, ArrowDown/Up navigation, ArrowRight expand/move-to-child, ArrowLeft collapse/move-to-parent, Home/End focus first/last visible, Enter and Space (paired per D-02a) select + toggle"
    - "Typeahead and asterisk-expand OMITTED silently per D-07 (no test, no no-throw, no backlog)"
    - "axe-clean Tier-1 smoke for default render"
    - "0 querySelector / 0 configureAxe / 0 toMatchSnapshot in the file (D-02a)"
  artifacts:
    - path: "packages/components/src/TreeView/TreeView.test.tsx"
      provides: "Tier 1+2 APG tree coverage for TC-13 (full APG-required contract; APG-OPTIONAL silently omitted per D-07)"
      contains: "describe('Tier 1: Table Stakes'"
  key_links:
    - from: "TreeView.test.tsx"
      to: "roving tabindex invariant"
      via: "exactly one treeitem has tabIndex=0, rest -1"
      pattern: "tabIndex"
    - from: "TreeView.test.tsx"
      to: "waitFor for focus shift"
      via: "useEffect focus competes with userEvent — Pitfall 6"
      pattern: "waitFor"
---

<objective>
Add Tier 1 + Tier 2 test coverage for `TreeView` (TC-13) asserting the full APG tree contract that the source implements: roving tabindex, aria-expanded/aria-selected/aria-level wiring, and the complete ArrowDown/ArrowUp/ArrowRight/ArrowLeft/Home/End/Enter/Space keyboard matrix.

Purpose: TreeView is the most APG-substantive of the six widgets. Pin its near-full contract NOW so refactors cannot regress. Typeahead and asterisk-expand are APG-OPTIONAL and are omitted silently per D-07 (no test, no no-throw stub, no backlog item — the user decided these are out of scope for Phase 24).

Output: `packages/components/src/TreeView/TreeView.test.tsx` (~15-18 `it()` blocks; axe-clean Tier-1 smoke; JSDoc note documenting silent omission of typeahead + asterisk-expand).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-complex-apg-widget-test-coverage/24-CONTEXT.md
@.planning/phases/24-complex-apg-widget-test-coverage/24-RESEARCH.md

@packages/components/src/TreeView/TreeView.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/Tabs/Tabs.test.tsx
@packages/components/src/_test/helpers.ts
@packages/components/TESTING-CONVENTIONS.md

<interfaces>
<!-- TreeView public API + verified source state (2026-05-11). -->

From packages/components/src/TreeView/TreeView.tsx:
```typescript
export interface TreeNode {
  id: string;
  label: string;
  children?: TreeNode[];
}
export interface TreeViewProps {
  data: TreeNode[];
  ariaLabel?: string;                     // default 'Tree View'
  className?: string;
  onSelect?: (node: TreeNode) => void;
}
```

Source-side facts (verified against TreeView.tsx 2026-05-11):
- `<ul role="tree" aria-label={ariaLabel}>` (line 223)
- Recursive children rendered inside `<ul role="group">` (line 158)
- Each item is `<div role="treeitem" aria-expanded={hasChildren ? isExpanded : undefined} aria-selected={selectedId===id} aria-level={level} tabIndex={focusedId===id ? 0 : -1}>` (lines 165-174 for recursive path; lines 240-250 duplicate inline path for root)
- Initial focusedId is data[0].id (line 35); initial selectedId is null
- Roving tabindex: exactly the focused item has tabIndex=0; all others -1
- Keyboard handler (line 62-134):
  - ArrowDown -> focusedId = next visible
  - ArrowUp -> focusedId = prev visible
  - ArrowRight on collapsed-with-children -> expand; on expanded-with-children -> move to first child; on leaf -> no-op (falls through)
  - ArrowLeft on expanded -> collapse; otherwise move to parent (findParent recursion)
  - Home -> first visible; End -> last visible
  - Enter / Space (case ' ') -> select + onSelect + toggle expand IF has children
- useEffect (line 149-154) `focus()`es the node ref when focusedId changes — Pitfall 6 (use waitFor after keystrokes)
- NO typeahead handler; NO asterisk handler — OMIT silently per D-07
- NOTE on duplication: the root render is inlined at lines 238-281, mirroring the recursive renderTree at lines 156-209. Tests will pass against the inline path for root items and the recursive path for children. Don't be confused by the duplication.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write TreeView.test.tsx with full APG tree coverage (APG-OPTIONAL silently omitted)</name>
  <files>packages/components/src/TreeView/TreeView.test.tsx</files>
  <behavior>
    Tier 1 (~5 tests):
    - mounts with role="tree" AND aria-label (default 'Tree View'); custom ariaLabel prop overrides default
    - renders one role="treeitem" per visible node initially (root nodes only — children not visible until parent expanded)
    - passes className through additively on the container
    - clicking a leaf node selects it (aria-selected becomes "true") and calls onSelect with that node
    - clicking a parent node toggles expand AND selects it AND calls onSelect

    Tier 2 (~12-13 tests):
    - aria-expanded is OMITTED on leaf treeitems (assert `not.toHaveAttribute('aria-expanded')`)
    - aria-expanded reflects state on parent treeitems: "false" initially; "true" after click or Enter on the parent
    - aria-level is 1 on root nodes (assert via getAllByRole('treeitem') filtered to roots)
    - roving tabindex invariant: initially data[0] has tabIndex=0, all other visible items have tabIndex=-1
    - ArrowDown moves focus to next visible treeitem (use waitFor per Pitfall 6); tabIndex shifts to the new focused item
    - ArrowUp moves focus to previous visible treeitem (waitFor)
    - ArrowRight on a collapsed parent EXPANDS it (aria-expanded -> "true"; children become visible/queryable as new treeitems)
    - ArrowRight on an expanded parent moves focus to its first child (waitFor)
    - ArrowRight on a leaf is a no-op (does not throw, focus stays)
    - ArrowLeft on an expanded parent COLLAPSES it (aria-expanded -> "false"; children no longer rendered)
    - ArrowLeft on a collapsed/leaf node moves focus to its parent (waitFor)
    - Home focuses the first visible treeitem; End focuses the last visible treeitem
    - Enter on a focused leaf calls onSelect (paired with Space per D-02a)
    - Space on a focused leaf calls onSelect (paired keyboard per D-02a)
    - Enter on a focused parent calls onSelect AND toggles expand
    - axe-clean Tier-1 smoke for default render
    - axe-clean for expanded-state render (after expanding a node)
    - two `<TreeView>` instances render with no duplicate ids (`expectUniqueIds` — note: TreeView does not auto-generate ids; uses consumer-supplied node.id, so test data must use unique ids)

    Anti-patterns (D-02a) — ZERO of each:
    - querySelector / querySelectorAll
    - configureAxe
    - toMatchSnapshot
  </behavior>
  <action>
    Create `packages/components/src/TreeView/TreeView.test.tsx` mirroring `Tabs.test.tsx` (which is the canonical roving-tabindex template per RESEARCH §1 and CONTEXT.md). Tabs.test.tsx is the closest analog because it's the only Phase 22 file that asserts a true APG roving tabindex contract.

    Per D-04, WCAG-SC JSDoc header covering:
    - 1.3.1 Info and Relationships (role=tree + role=treeitem + role=group + aria-level)
    - 2.1.1 Keyboard (full APG tree matrix)
    - 2.4.3 Focus Order (single-tab-stop via roving tabindex; arrow keys move within)
    - 4.1.2 Name, Role, Value (role=tree, role=treeitem, aria-expanded, aria-selected, aria-level)
    - Do NOT list 4.1.3 (no live region).

    Add an explicit `Implementation note:` block in the JSDoc recording:
    - APG-OPTIONAL behaviors typeahead (character-key jumps to first matching item) and asterisk-expand (`*` expands all sibling nodes) are OMITTED from tests silently per D-07 (CONTEXT.md user decision). No no-throw stubs. No backlog item.
    - useEffect focus shift competes with userEvent (Pitfall 6) — all post-keystroke focus assertions wrap in `waitFor`.
    - TreeView source has a duplicated inline render at the root (lines 238-281 mirror the recursive renderTree at 156-209). Tests pass against both paths transparently.

    Imports:
    ```ts
    import { render, screen, waitFor } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import { describe, it, expect, vi } from 'vitest';
    import { TreeView, type TreeNode } from './TreeView';
    import { expectNoAxeViolations, expectUniqueIds } from '../_test/helpers';
    ```

    Test data — a 2-level tree with parent + leaf mix so all ArrowRight/ArrowLeft branches are exercisable:
    ```ts
    const DATA: TreeNode[] = [
      {
        id: 'fruits', label: 'Fruits',
        children: [
          { id: 'apple', label: 'Apple' },
          { id: 'banana', label: 'Banana' },
        ],
      },
      { id: 'veggies', label: 'Vegetables', children: [{ id: 'carrot', label: 'Carrot' }] },
      { id: 'grains', label: 'Grains' },  // leaf — for ArrowRight no-op test
    ];
    ```

    Reference RESEARCH §8 (24-05 skeleton, lines 728-752) for `it()` titles. Reference RESEARCH §"Code Examples" "TreeView roving tabindex + ArrowDown sequence" (lines 822-836) for the exact `waitFor` shape after keystrokes.

    For focus assertions, follow Pitfall 6: pass `expectFocusOn` as a callback that re-queries via `screen.getByRole('treeitem', { name: /label/i })` so the assertion is evaluated AFTER the await — not against a stale element reference captured before the keystroke. Or wrap each assertion in `await waitFor(() => expect(document.activeElement).toBe(...))`.

    For "no aria-expanded on leaves": find a leaf treeitem (e.g., 'Grains' or any expanded-tree child) and assert `expect(item).not.toHaveAttribute('aria-expanded')`.

    For `expectUniqueIds`: TreeView does NOT inject auto-generated ids in source — only consumer-supplied node.id values appear in nodeRefs (Map, not DOM ids). The assertion will pass trivially. Keep the test only if it provides documentation value; otherwise it's optional filler.

    NEVER touch `package.json`, `tsconfig.json`, or any source file outside `packages/components/src/TreeView/TreeView.test.tsx`.
  </action>
  <verify>
    <automated>cd packages/components && npx vitest run src/TreeView/TreeView.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - `packages/components/src/TreeView/TreeView.test.tsx` exists
    - `npx vitest run src/TreeView/TreeView.test.tsx` reports 15-18 passing tests, 0 failures
    - `npm run test:wcag-headers` count rises by 1
    - Full repo `npm test -w @holmdigital/components` stays green
    - `grep -c "querySelector\|configureAxe\|toMatchSnapshot" packages/components/src/TreeView/TreeView.test.tsx` returns 0
    - At least one `expectNoAxeViolations(` call present
    - At least one `waitFor(` after a keystroke (Pitfall 6 mitigation)
    - JSDoc Implementation-note block documents D-07 silent omission of typeahead + asterisk-expand
    - NO TC-13-IMPL backlog reference (D-07 says no backlog item)
  </done>
</task>

</tasks>

<verification>
- Vitest run of the new file passes all assertions
- Full components-package test run remains green
- WCAG-SC marker scan counts the new file
- D-02a anti-pattern grep returns zero hits
- File ships axe-clean smokes for default + expanded states (D-03)
- JSDoc Implementation-note documents D-07 silent omission (no backlog item created)
- All post-keystroke focus assertions wrap in waitFor (Pitfall 6)
</verification>

<success_criteria>
- TC-13 acceptance criterion 5: full APG tree contract (Arrow expand/collapse/navigate, Home/End, Enter/Space, single tabindex=0 roving) asserted against source
- Typeahead + asterisk-expand silently omitted per D-07 (no test, no no-throw, no backlog)
- D-02a paired-keyboard requirement satisfied for select-toggle (Enter + Space)
- axe-clean for default and expanded renders
</success_criteria>

<output>
After completion, create `.planning/phases/24-complex-apg-widget-test-coverage/24-05-SUMMARY.md` recording final test count, confirmation that typeahead + asterisk-expand are silently omitted (no backlog item), and confirmation of axe-clean + zero anti-pattern hits.
</output>
