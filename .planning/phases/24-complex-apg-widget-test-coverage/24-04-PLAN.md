---
phase: 24-complex-apg-widget-test-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/DataTable/DataTable.test.tsx
autonomous: true
requirements:
  - TC-12
task_count: 1
must_haves:
  truths:
    - "DataTable.test.tsx exists and is picked up by vitest (test:ci green)"
    - "Test file carries WCAG-SC JSDoc header"
    - "Tier 1 describe asserts the substantial real surface: caption, <th scope='col'>, sortable header rendered as <button>, custom render() override, row/cell mapping"
    - "Tier 2 describe asserts the strongest part of DataTable's APG conformance: sortable header contract — clicking toggles aria-sort none -> ascending -> descending; Enter and Space both activate (paired keyboard per D-02a)"
    - "Tier 2 no-throw stub for cell-arrow navigation (ArrowLeft/Right/Up/Down, Home/End, Ctrl+Home/End, PageUp/Down) using fireEvent.keyDown escape hatch (cells are non-focusable <td>)"
    - "Implementation-note JSDoc block records cell-arrow gap; references TC-12-IMPL backlog"
    - "0 querySelector / 0 configureAxe / 0 toMatchSnapshot in the file (D-02a)"
  artifacts:
    - path: "packages/components/src/DataTable/DataTable.test.tsx"
      provides: "Tier 1+2 coverage for TC-12 with partial-stub strategy per D-01"
      contains: "describe('Tier 1: Table Stakes'"
  key_links:
    - from: "DataTable.test.tsx"
      to: "aria-sort on <th>"
      via: "sort state assertion"
      pattern: "aria-sort"
    - from: "DataTable.test.tsx"
      to: "fireEvent.keyDown"
      via: "§5 escape hatch for non-focusable <td>"
      pattern: "fireEvent\\.keyDown"
---

<objective>
Add Tier 1 + Tier 2 test coverage for `DataTable` (TC-12) asserting the strong-but-partial APG surface the source ships: `scope="col"` headers, sortable column buttons with `aria-sort` toggle (none -> ascending -> descending), caption, custom `render()` override. Tier 2 also includes a no-throw stub for the missing APG cell-arrow keyboard navigation per D-01.

Purpose: The sortable-header contract is the strongest part of DataTable's APG conformance — pin it NOW so refactors cannot silently regress. The cell-arrow gap becomes a documented no-throw stub that flips to real assertions when TC-12-IMPL lands in v0.7.

Output: `packages/components/src/DataTable/DataTable.test.tsx` (~11-14 `it()` blocks; axe-clean smokes for default + sorted states; JSDoc note referencing TC-12-IMPL).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-complex-apg-widget-test-coverage/24-CONTEXT.md
@.planning/phases/24-complex-apg-widget-test-coverage/24-RESEARCH.md

@packages/components/src/DataTable/DataTable.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/RadioGroup/RadioGroup.test.tsx
@packages/components/src/_test/helpers.ts
@packages/components/TESTING-CONVENTIONS.md

<interfaces>
<!-- DataTable public API + verified source state (2026-05-11). -->

From packages/components/src/DataTable/DataTable.tsx:
```typescript
export interface Column<T> {
  header: string;
  accessor: keyof T;
  sortable?: boolean;                     // default false
  render?: (item: T) => React.ReactNode;  // custom cell renderer
}
export interface DataTableProps<T> {
  data: T[];
  columns: Column<T>[];
  caption: string;                        // REQUIRED for accessibility
  className?: string;
}
export function DataTable<T>(...)
```

Source-side facts (verified against DataTable.tsx 2026-05-11):
- `<table>` (implicit role=table); `<caption>{caption}</caption>` (line 130)
- `<th scope="col" aria-sort={...}>` on every column (line 134-142). aria-sort is `undefined` when not the active sort column; `"ascending"` or `"descending"` when active. NOT `"none"`.
- Sortable columns wrap the header in a `<button type="button" onClick={handleSort}>` (line 145-158). Non-sortable columns render plain text.
- handleSort toggles ascending -> descending on consecutive clicks of the same column (line 61-67). A different column resets to ascending.
- `<td>` cells render `column.render?.(row) ?? String(row[accessor])` (line 170-174). Plain `<td>` — no role override, no tabindex.
- NO `onKeyDown` anywhere; no cell-arrow navigation; no `role="grid"`.
- Sort indicator: `<span aria-hidden="true">` containing ▲ / ▼ / ↕ (line 152-157) — already correctly hidden from AT.
- Outer wrapper is `<div className={className} style={{overflowX:'auto'}}>` — className applies here, NOT on the table.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write DataTable.test.tsx with sortable-header real coverage + cell-arrow no-throw stub per D-01</name>
  <files>packages/components/src/DataTable/DataTable.test.tsx</files>
  <behavior>
    Tier 1 (~5 tests):
    - mounts a <table> with the supplied caption (getByRole('table') has accessible name from caption)
    - renders one <th scope="col"> per column (assert via getAllByRole('columnheader') length AND every header has scope="col")
    - renders one <tr> per data row (length === data.length + 1 header row OR use getAllByRole('row') minus the header)
    - cells render via String(row[accessor]) by default; cells with column.render override render the custom node (assert via getByText on the render output)
    - passes className through additively on the outer wrapper <div> (not the table)

    Tier 2 (~8-9 tests):
    - sortable column header renders as a <button> (assert via getByRole('button', { name: /name/i }))
    - non-sortable column header renders as plain text (no button)
    - clicking a sortable header sets aria-sort="ascending" on that <th> (initial click)
    - clicking the same sortable header again sets aria-sort="descending"
    - clicking a different sortable header sets aria-sort="ascending" on the new column AND removes aria-sort from the previous one
    - data rows reorder when sort is applied (assert first row's data shifts; use getAllByRole('row') and inspect cell text)
    - Enter on a focused sort button fires the same sort transition (paired keyboard per D-02a — native button)
    - Space on a focused sort button fires the same sort transition (paired keyboard per D-02a)
    - APG-grid cell-arrow keystrokes do NOT throw on a cell (parametrised: ArrowDown, ArrowRight, ArrowUp, ArrowLeft, Home, End, PageUp, PageDown, {Control>}{Home}{/Control}, {Control>}{End}{/Control}). USE fireEvent.keyDown (RESEARCH §5 escape hatch — <td> is not focusable, userEvent.keyboard cannot target it). Inline-comment the rationale. Assert table still in document and no exception raised.
    - axe-clean for default render (no sort applied)
    - axe-clean for sorted render (after clicking a sortable header)
    - sort indicator <span> is aria-hidden (assert via querying the span — but D-02a forbids querySelector; instead assert through getByRole('button') and check its first child is aria-hidden — OR scope this to the no-anti-pattern path: assert the accessible name of the button is the header text WITHOUT the indicator glyph, which proves the glyph is hidden from AT)

    Anti-patterns (D-02a) — ZERO of each:
    - querySelector / querySelectorAll
    - configureAxe
    - toMatchSnapshot
  </behavior>
  <action>
    Create `packages/components/src/DataTable/DataTable.test.tsx` mirroring `Button.test.tsx` Tier-1 structure and using `fireEvent.keyDown` (NOT `userEvent.keyboard`) for the cell-arrow no-throw row, per RESEARCH §5 — `<td>` is not focusable so userEvent has nothing to dispatch to.

    Per D-04, WCAG-SC JSDoc header covering:
    - 1.3.1 Info and Relationships (table + caption + scope="col" + column-header role)
    - 2.1.1 Keyboard (sortable-button Enter/Space activation real; cell-arrow no-throw)
    - 4.1.2 Name, Role, Value (role=table from <table>, role=columnheader from <th>, role=button from sortable header, aria-sort state, aria-hidden on indicator)
    - Do NOT list 4.1.3 (no live region) or 2.4.3 (no cell focus management — gap).

    Add an explicit `Implementation note:` block in the JSDoc recording:
    - APG grid cell-arrow keyboard navigation (ArrowLeft/Right/Up/Down between cells, Home/End row bounds, Ctrl+Home/End table bounds, PageUp/Down) is NOT implemented in source — cells are non-focusable `<td>`.
    - Tier-2 cell-arrow tests use the `fireEvent.keyDown` escape hatch (RESEARCH §5) because userEvent cannot dispatch to non-focusable elements.
    - APG grid roles (`role="grid"`, `role="gridcell"`, `role="row"`) are NOT applied — source uses native table semantics (acceptable for static table, gap for full APG grid keyboard).
    - `scope="row"` is NOT applied to first-column cells — source has no row-header support (gap, NOT covered by TC-12-IMPL scope).
    - Cell-arrow nav is deferred to v0.7 backlog item TC-12-IMPL. Sortable-header contract (aria-sort + Enter/Space) IS the strongest implemented APG surface and is tested for real.

    Imports:
    ```ts
    import { render, screen, fireEvent } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import { describe, it, expect } from 'vitest';
    import { DataTable, type Column } from './DataTable';
    import { expectNoAxeViolations } from '../_test/helpers';
    ```

    Test data — fixed shape so sort ordering is deterministic:
    ```ts
    interface Row { name: string; age: number; email: string; }
    const DATA: Row[] = [
      { name: 'Charlie', age: 30, email: 'c@x.com' },
      { name: 'Alice',   age: 25, email: 'a@x.com' },
      { name: 'Bob',     age: 28, email: 'b@x.com' },
    ];
    const COLUMNS: Column<Row>[] = [
      { header: 'Name',  accessor: 'name',  sortable: true  },
      { header: 'Age',   accessor: 'age',   sortable: true  },
      { header: 'Email', accessor: 'email', sortable: false },
    ];
    ```

    Reference RESEARCH §8 (24-04 skeleton, lines 703-726) for `it()` titles. Reference RESEARCH §"Code Examples" "D-01 no-throw stub for DataTable cell-nav" (lines 841-857) for the verbatim no-throw matrix shape — KEEP `fireEvent.keyDown`, inline-comment the §5 rationale.

    For "two tables render with no duplicate ids" test: DataTable does not auto-generate ids (no useId calls in source), so `expectUniqueIds` will pass trivially. Skip this test (no value-add) UNLESS the test count needs filler.

    NEVER touch `package.json`, `tsconfig.json`, or any source file outside `packages/components/src/DataTable/DataTable.test.tsx`.
  </action>
  <verify>
    <automated>cd packages/components && npx vitest run src/DataTable/DataTable.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - `packages/components/src/DataTable/DataTable.test.tsx` exists
    - `npx vitest run src/DataTable/DataTable.test.tsx` reports 11-14 passing tests, 0 failures
    - `npm run test:wcag-headers` count rises by 1
    - Full repo `npm test -w @holmdigital/components` stays green
    - `grep -c "querySelector\|configureAxe\|toMatchSnapshot" packages/components/src/DataTable/DataTable.test.tsx` returns 0
    - At least one `expectNoAxeViolations(` call present
    - JSDoc header contains "TC-12-IMPL" backlog reference
    - At least one `fireEvent.keyDown(` for the cell-arrow no-throw matrix (with inline §5 rationale comment)
  </done>
</task>

</tasks>

<verification>
- Vitest run of the new file passes all assertions
- Full components-package test run remains green
- WCAG-SC marker scan counts the new file
- D-02a anti-pattern grep returns zero hits
- File ships axe-clean smokes for default + sorted render (D-03)
- JSDoc header documents the cell-arrow gap + TC-12-IMPL backlog reference
- fireEvent.keyDown is used (and only used) for the cell-arrow no-throw stub with inline rationale
</verification>

<success_criteria>
- TC-12 acceptance criterion 4: real coverage for static grid semantics (caption, scope="col", aria-sort toggling, sortable-button Enter/Space activation); no-throw asserts + documented gap for cell-wise navigation
- Sort-state transitions (ascending -> descending; switching columns) verified
- D-02a paired-keyboard requirement satisfied for sort button (Enter + Space)
- TC-12-IMPL backlog reference present in JSDoc
</success_criteria>

<output>
After completion, create `.planning/phases/24-complex-apg-widget-test-coverage/24-04-SUMMARY.md` recording final test count, the cell-arrow keystrokes covered by the no-throw stub, and confirmation of axe-clean + zero anti-pattern hits.
</output>
