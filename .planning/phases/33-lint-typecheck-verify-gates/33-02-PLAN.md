---
phase: 33-lint-typecheck-verify-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/engine/package.json
  - packages/engine/src/core/regulatory-scanner.ts
autonomous: true
requirements: [PUB-09]
must_haves:
  truths:
    - "packages/engine/package.json gains a `typecheck` script running `tsc --noEmit`"
    - "packages/engine/package.json `verify` script chains in literal order: build, lint, typecheck, check:exports, check:types, test:ci"
    - "engine's two pre-existing lint ERRORS are resolved: (a) `__ENGINE_VERSION__ is not defined` (no-undef in regulatory-scanner.ts:30 area), (b) `@ts-ignore` should be `@ts-expect-error` (regulatory-scanner.ts:185)"
    - "`npm run lint -w @holmdigital/engine` exits 0 (errors = 0; warnings tolerated — eslint exits 0 on warnings)"
    - "`npm run typecheck -w @holmdigital/engine` exits 0 (preflight 2026-05-12: engine typecheck CLEAN; if TS2724/puppeteer surfaces during execution, fall back to `tsc --noEmit --skipLibCheck` per ROADMAP SC#4 contingency and document in summary + CHANGELOG)"
    - "`npm run verify -w @holmdigital/engine` exits 0 end-to-end"
    - "`prepublishOnly` script remains literally `npm run verify` — unchanged"
    - "`__ENGINE_VERSION__` continues to type-check via `packages/engine/src/globals.d.ts` (no change to runtime injection in tsup config)"
  artifacts:
    - path: "packages/engine/package.json"
      provides: "verify chain with lint + typecheck gates + ESLint globals fix for __ENGINE_VERSION__"
      contains: "\"typecheck\": \"tsc --noEmit\""
    - path: "packages/engine/src/core/regulatory-scanner.ts"
      provides: "Lint-clean source (no `@ts-ignore`, no undeclared globals)"
      contains: "@ts-expect-error"
  key_links:
    - from: "packages/engine/package.json `verify`"
      to: "ESLint pass + tsc --noEmit pass"
      via: "npm run chain"
      pattern: "build && npm run lint && npm run typecheck && npm run check:exports && npm run check:types && npm run test:ci"
    - from: "packages/engine/src/core/regulatory-scanner.ts:30"
      to: "ESLint config-level globals declaration"
      via: "`eslintConfig.globals.__ENGINE_VERSION__: 'readonly'` in package.json (or `/* global __ENGINE_VERSION__ */` block comment at top of regulatory-scanner.ts — executor picks least-invasive)"
      pattern: "__ENGINE_VERSION__"
---

<objective>
Add the `typecheck` script to `@holmdigital/engine`, rewire its `verify` chain, and fix the two pre-existing lint errors that currently block the new chain. Preflight (2026-05-12) shows engine typecheck is CLEAN but lint has 2 errors + 6 warnings; only errors block, but both errors are 30-second fixes so they go here rather than into a separate plan.

Purpose: Closes the engine half of PUB-09 SC#1, #2, #3, and (defensively) SC#4 by documenting the `--skipLibCheck` fallback inline.

Output:
- `packages/engine/package.json` with `typecheck` script, extended `verify` chain, and an ESLint `globals` entry for `__ENGINE_VERSION__`.
- `packages/engine/src/core/regulatory-scanner.ts` with `@ts-ignore` → `@ts-expect-error` (or removed entirely if no diagnostic surfaces under tsc).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@packages/engine/package.json
@packages/engine/tsconfig.json
@packages/engine/src/globals.d.ts
@packages/engine/src/core/regulatory-scanner.ts

<interfaces>
Current `packages/engine/package.json` scripts (verified 2026-05-12):

```json
"scripts": {
    "build": "tsup && node scripts/copy-assets.mjs",
    "test:ci": "vitest run",
    "lint": "eslint src",
    "prepublishOnly": "npm run verify",
    "check:exports": "publint --strict",
    "check:types": "attw --pack . --ignore-rules no-resolution",
    "verify": "npm run build && npm run check:exports && npm run check:types && npm run test:ci"
}
```

Preflight lint findings (2026-05-12):
- `packages/engine/src/core/regulatory-scanner.ts:30` — `__ENGINE_VERSION__ is not defined` (rule: `no-undef`) — ERROR
- `packages/engine/src/core/regulatory-scanner.ts:185` — Use `// @ts-expect-error` instead of `// @ts-ignore` (rule: `@typescript-eslint/ban-ts-comment`) — ERROR
- 6 warnings (`@typescript-eslint/no-explicit-any`, caught-error `no-unused-vars`) — non-blocking, DO NOT TOUCH this phase

Existing `__ENGINE_VERSION__` setup (verified 2026-05-12):
- `packages/engine/src/globals.d.ts` line 1: `declare const __ENGINE_VERSION__: string;` — TypeScript sees it (typecheck is clean). ESLint's `no-undef` does NOT read `.d.ts` files, hence the false positive.
- `packages/engine/src/core/regulatory-scanner.ts:30`: `return __ENGINE_VERSION__;`

The fix for `no-undef`: declare it as an ESLint `globals` entry. Least-invasive route is to add an `eslintConfig` block in `packages/engine/package.json`:

```json
"eslintConfig": {
    "globals": {
        "__ENGINE_VERSION__": "readonly"
    }
}
```

If the project has a root or shared ESLint config already (no `eslint.config.*` or `.eslintrc*` was found at any level — verified 2026-05-12), this `package.json#eslintConfig` block is the canonical place. The executor MUST run `npx eslint --print-config packages/engine/src/core/regulatory-scanner.ts | grep -A2 globals` first to confirm no existing globals layer exists; if one does exist, merge into it instead of creating a duplicate.

Fallback (if the executor judges `eslintConfig` in package.json the wrong abstraction layer): add `/* global __ENGINE_VERSION__ */` at the top of `regulatory-scanner.ts`. Document the choice in the summary.

The `@ts-ignore` → `@ts-expect-error` swap at line 185: the surrounding context is `// @ts-ignore` directly above `return await window.axe.run(document, { ... })` inside a `page.evaluate` callback (browser-side code). Under tsc, that line MAY no longer have a diagnostic to suppress (because `globals.d.ts` may or may not cover `window.axe`). The rule:

1. First do the simple swap `@ts-ignore` → `@ts-expect-error`.
2. Re-run `npm run lint -w @holmdigital/engine && npm run typecheck -w @holmdigital/engine`.
3. If lint then reports "Unused @ts-expect-error directive", REMOVE the directive comment entirely.
4. If lint passes, keep it.

The engine has had a TS2724 puppeteer-types issue historically (ROADMAP SC#4 contingency). Preflight 2026-05-12 shows it is NOT currently reproducing — `tsc --noEmit` exits 0. If it surfaces during execution of THIS plan:
- Append ` --skipLibCheck` to the `typecheck` script: `"typecheck": "tsc --noEmit --skipLibCheck"`.
- Document in the summary AND in the engine CHANGELOG entry (which is written by Plan 33-04, not here — leave a note in this plan's summary for 33-04 to pick up).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the two lint errors in regulatory-scanner.ts (ESLint globals + @ts-ignore swap)</name>
  <files>packages/engine/src/core/regulatory-scanner.ts, packages/engine/package.json</files>
  <read_first>
    - packages/engine/src/core/regulatory-scanner.ts (full file — to see both error sites in context)
    - packages/engine/src/globals.d.ts (confirm the type declaration exists)
    - packages/engine/package.json (confirm there is no existing `eslintConfig` block; if there is, merge)
  </read_first>
  <action>
    Step 1 — Run preflight to confirm the planner's findings still hold:
    ```
    cd packages/engine && npx eslint src 2>&1 | grep -E "error|Error" | head -10
    ```
    Expected output: 2 errors, exact lines and rules per `<interfaces>`. If the count or location differs, STOP and report.

    Step 2 — Add ESLint globals declaration. Edit `packages/engine/package.json`:
    - If no `eslintConfig` key exists, add at the top level (anywhere is fine; conventional placement is between `devDependencies` and the trailing `}`):
      ```json
      "eslintConfig": {
          "globals": {
              "__ENGINE_VERSION__": "readonly"
          }
      }
      ```
    - If an `eslintConfig` block already exists, merge `globals.__ENGINE_VERSION__: "readonly"` into it.
    - If a root-level ESLint config is discovered during `read_first` (none found at planning time), prefer that layer and document the choice.

    Step 3 — Swap `@ts-ignore` → `@ts-expect-error` at `packages/engine/src/core/regulatory-scanner.ts:185`:
    ```
    // @ts-ignore
    ```
    becomes
    ```
    // @ts-expect-error window.axe is injected by axe-core script tag at runtime; not part of lib.dom
    ```
    (Including a brief reason is required by `@typescript-eslint/ban-ts-comment` rule defaults.)

    Step 4 — Run lint + typecheck back-to-back:
    ```
    cd packages/engine && npx eslint src 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -10
    ```
    - If lint reports "Unused '@ts-expect-error' directive" → that means tsc no longer has a diagnostic there; REMOVE the directive line entirely. Re-run.
    - If tsc reports any new error → STOP and report. Most likely candidate is TS2724 from puppeteer; if so, apply the `--skipLibCheck` contingency below and document.

    Step 5 — TS2724 contingency (only if step 4 surfaces it): edit the `typecheck` script (added in Task 2 below) from `"tsc --noEmit"` to `"tsc --noEmit --skipLibCheck"` and leave a note for Plan 33-04 to mention in the CHANGELOG.

    Atomic commit (scoped pathspec):
    `git commit -m "fix(engine): resolve lint errors — declare __ENGINE_VERSION__ ESLint global + swap @ts-ignore → @ts-expect-error (PUB-09)" -- packages/engine/src/core/regulatory-scanner.ts packages/engine/package.json`

    NOTE: This commit touches the same `package.json` as Task 2. Keep both tasks in ONE plan so a single commit (or two sequential commits in the same plan execution) lands them together. Do NOT split across plans — would create a Wave-1 file-overlap conflict with the package.json edit in Task 2.
  </action>
  <verify>
    <automated>cd packages/engine && npx eslint src 2>&1 | grep -c " error " | tr -d '\n' && echo " errors (expect 0)"</automated>
  </verify>
  <done>
    - `npx eslint src` from `packages/engine/` reports 0 errors (warnings tolerated)
    - `packages/engine/src/core/regulatory-scanner.ts` no longer contains `@ts-ignore` (grep returns 0)
    - `package.json` has an `eslintConfig.globals.__ENGINE_VERSION__: "readonly"` entry (or chosen alternative documented in summary)
    - `npx tsc --noEmit` from `packages/engine/` exits 0 (or, if TS2724 surfaced, the `--skipLibCheck` fallback is in place and documented)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add typecheck script + rewire verify chain in engine/package.json</name>
  <files>packages/engine/package.json</files>
  <read_first>
    - packages/engine/package.json (post-Task-1 state — Task 1 already added `eslintConfig`)
  </read_first>
  <action>
    Edit `packages/engine/package.json`:

    1. Add a new script entry next to `lint`:
       ```
       "typecheck": "tsc --noEmit",
       ```
       (If Task 1 hit TS2724, this becomes `"tsc --noEmit --skipLibCheck"` with rationale captured in summary.)

    2. Replace the existing `verify` line:
       ```
       "verify": "npm run build && npm run check:exports && npm run check:types && npm run test:ci"
       ```
       with:
       ```
       "verify": "npm run build && npm run lint && npm run typecheck && npm run check:exports && npm run check:types && npm run test:ci"
       ```

    Do NOT modify `prepublishOnly` — it must remain literally `npm run verify`.

    Atomic commit (scoped pathspec):
    `git commit -m "chore(engine): add typecheck script + wire lint/typecheck into verify (PUB-09)" -- packages/engine/package.json`
  </action>
  <verify>
    <automated>cd packages/engine && npm run verify 2>&1 | tail -25 && node -e "const p=require('./package.json');if(!p.scripts.typecheck){console.error('missing typecheck');process.exit(1)};const v=p.scripts.verify;if(!v.includes('npm run lint && npm run typecheck && npm run check:exports')){console.error('verify order wrong:',v);process.exit(1)};if(p.scripts.prepublishOnly!=='npm run verify'){console.error('prepublishOnly drifted');process.exit(1)};console.log('ok')"</automated>
  </verify>
  <done>
    - `packages/engine/package.json` has `"typecheck": "tsc --noEmit"` (or the `--skipLibCheck` variant if Task 1 contingency was triggered)
    - `verify` script contains the literal substring `build && npm run lint && npm run typecheck && npm run check:exports && npm run check:types && npm run test:ci`
    - `prepublishOnly` is unchanged (still `npm run verify`)
    - `npm run verify -w @holmdigital/engine` exits 0
  </done>
</task>

</tasks>

<verification>
- `node -e "const p=require('./packages/engine/package.json'); console.log(p.scripts.typecheck, '|', p.scripts.verify)"` prints the typecheck script and the new verify chain
- `npm run lint -w @holmdigital/engine` exits 0 (0 errors, warnings allowed)
- `npm run typecheck -w @holmdigital/engine` exits 0
- `npm run verify -w @holmdigital/engine` exits 0
- `grep -c "@ts-ignore" packages/engine/src/core/regulatory-scanner.ts` returns 0
</verification>

<success_criteria>
Engine package now gates on lint + typecheck in `verify`. Two pre-existing lint errors resolved. The 6 lint warnings remain (non-blocking; out of scope per PUB-09). Ready for the final-wave global verify + version bump.
</success_criteria>

<output>
Create `.planning/phases/33-lint-typecheck-verify-gates/33-02-SUMMARY.md` recording:
- Final `verify` chain string
- Which ESLint globals-declaration route was taken (`package.json#eslintConfig` vs `/* global */` comment vs other) and rationale
- Whether the `@ts-expect-error` directive ended up kept or removed (and why)
- Whether TS2724 surfaced and `--skipLibCheck` was applied — if so, this MUST be flagged for Plan 33-04 to mention in the engine CHANGELOG entry
- Lint warning count (should remain 6 — confirm they were left untouched)
- Confirmation `prepublishOnly` was not modified
</output>
