---
phase: 33-lint-typecheck-verify-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/standards/package.json
autonomous: true
requirements: [PUB-09]
must_haves:
  truths:
    - "packages/standards/package.json gains a `typecheck` script running `tsc --noEmit`"
    - "packages/standards/package.json `verify` script chains in the literal order: build, lint, typecheck, check:exports, check:types, test:ci"
    - "`npm run typecheck -w @holmdigital/standards` exits 0 against the current source (preflight 2026-05-12: standards typecheck CLEAN)"
    - "`npm run lint -w @holmdigital/standards` exits 0 (standards has had a `lint` script since pre-phase; no ESLint errors expected)"
    - "`npm run verify -w @holmdigital/standards` exits 0 end-to-end"
    - "`prepublishOnly` script remains literally `npm run verify` — unchanged, so SC#5 holds"
  artifacts:
    - path: "packages/standards/package.json"
      provides: "verify chain with lint + typecheck gates"
      contains: "\"typecheck\": \"tsc --noEmit\""
  key_links:
    - from: "packages/standards/package.json `verify`"
      to: "packages/standards/package.json `typecheck` + `lint`"
      via: "npm run chain in literal order build && lint && typecheck && check:exports && check:types && test:ci"
      pattern: "build && npm run lint && npm run typecheck && npm run check:exports && npm run check:types && npm run test:ci"
---

<objective>
Add the `typecheck` script to `@holmdigital/standards` and rewire its `verify` chain to gate on `lint` + `typecheck` before `check:exports`. This is the smallest of the three Wave-1 plans because preflight (2026-05-12) shows the package is already clean on both new gates — no source fixes required, only `package.json` wire-up.

Purpose: Closes the standards-package half of PUB-09 SC#1, #2, #3.
Output: `packages/standards/package.json` with a `typecheck` script and an extended `verify` chain. No source changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@packages/standards/package.json
@packages/standards/tsconfig.json

<interfaces>
Current `packages/standards/package.json` scripts (relevant excerpt, verified 2026-05-12):

```json
"scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean",
    "test": "vitest",
    "test:ci": "vitest run",
    "lint": "eslint src",
    "check:exports": "publint --strict",
    "check:types": "attw --pack . --ignore-rules no-resolution",
    "verify": "npm run build && npm run check:exports && npm run check:types && npm run test:ci",
    "prepublishOnly": "npm run verify"
}
```

tsconfig.json extends `../../tsconfig.base.json` (strict mode + `noUnusedLocals` + `noImplicitReturns`). `tsc --noEmit` resolves via that config.

Preflight result (2026-05-12):
- `npx tsc --noEmit` from `packages/standards/` → exit 0 (CLEAN)
- `npm run lint -w @holmdigital/standards` → expected exit 0 (already in pipeline elsewhere, no known errors)

No source modifications required in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typecheck script and rewire verify chain in standards/package.json</name>
  <files>packages/standards/package.json</files>
  <read_first>
    - packages/standards/package.json (full file — to confirm the exact `verify` line before editing)
    - packages/standards/tsconfig.json (to confirm `tsc --noEmit` will pick up the right config)
  </read_first>
  <action>
    Edit `packages/standards/package.json`:

    1. Add a new script entry between `lint` and `validate-schema`:
       ```
       "typecheck": "tsc --noEmit",
       ```

    2. Replace the existing `verify` line:
       ```
       "verify": "npm run build && npm run check:exports && npm run check:types && npm run test:ci",
       ```
       with:
       ```
       "verify": "npm run build && npm run lint && npm run typecheck && npm run check:exports && npm run check:types && npm run test:ci",
       ```

    Do NOT modify `prepublishOnly` — it must remain literally `npm run verify` so SC#5 (publish fails if lint/typecheck fails) is satisfied transitively.

    Do NOT touch source files. Preflight confirmed standards has zero typecheck errors and (expected) zero lint errors.

    Atomic commit (scoped pathspec to avoid Wave-1 parallel-commit race per Phase-32 lesson):
    `git add packages/standards/package.json && git commit -m "chore(standards): add typecheck script + wire lint/typecheck into verify (PUB-09)" -- packages/standards/package.json`
  </action>
  <verify>
    <automated>cd packages/standards && npm run verify 2>&1 | tail -20 && node -e "const p=require('./package.json');if(!p.scripts.typecheck||p.scripts.typecheck!=='tsc --noEmit'){console.error('missing typecheck');process.exit(1)};const v=p.scripts.verify;if(!v.includes('npm run lint && npm run typecheck && npm run check:exports')){console.error('verify order wrong:',v);process.exit(1)};if(p.scripts.prepublishOnly!=='npm run verify'){console.error('prepublishOnly drifted');process.exit(1)};console.log('ok')"</automated>
  </verify>
  <done>
    - `packages/standards/package.json` has `"typecheck": "tsc --noEmit"`
    - `verify` script contains the literal substring `build && npm run lint && npm run typecheck && npm run check:exports && npm run check:types && npm run test:ci`
    - `prepublishOnly` is unchanged (still `npm run verify`)
    - `npm run verify -w @holmdigital/standards` exits 0
    - Commit scoped via `--` pathspec to avoid racing parallel Wave-1 plans
  </done>
</task>

</tasks>

<verification>
- `node -e "const p=require('./packages/standards/package.json'); console.log(p.scripts.typecheck, '|', p.scripts.verify)"` prints `tsc --noEmit | npm run build && npm run lint && npm run typecheck && npm run check:exports && npm run check:types && npm run test:ci`
- `npm run typecheck -w @holmdigital/standards` exits 0
- `npm run lint -w @holmdigital/standards` exits 0
- `npm run verify -w @holmdigital/standards` exits 0
</verification>

<success_criteria>
Standards package now gates on lint + typecheck in `verify`. No source changes. Ready for the final-wave global verify + version bump.
</success_criteria>

<output>
Create `.planning/phases/33-lint-typecheck-verify-gates/33-01-SUMMARY.md` recording:
- The exact final `verify` chain string
- Confirmation that `prepublishOnly` was not modified
- `tsc --noEmit` clean output (or any TS errors that surfaced unexpectedly)
- `eslint src` clean output (or any errors that surfaced)
</output>
