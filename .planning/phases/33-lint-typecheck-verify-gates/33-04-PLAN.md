---
phase: 33-lint-typecheck-verify-gates
plan: 04
type: execute
wave: 2
depends_on: ["33-01", "33-02", "33-03"]
autonomous: false
requirements: [PUB-09]
files_modified:
  - packages/standards/package.json
  - packages/components/package.json
  - packages/engine/package.json
  - packages/standards/CHANGELOG.md
  - packages/components/CHANGELOG.md
  - packages/engine/CHANGELOG.md
must_haves:
  truths:
    - "All 3 packages' `npm run verify` exits 0 when run in sequence (no env caching, no skipped steps)"
    - "standards version bumped 2.5.1 → 2.5.2 (PATCH — verify-chain wire-up only, no source behavior change)"
    - "components version bumped 2.6.0 → 2.6.1 (PATCH — typecheck fixes + @types/node devDep are dev-side only, public API byte-equivalent to 2.6.0)"
    - "engine version bumped 2.5.2 → 2.5.3 (PATCH — lint fix is dev-side only, public API byte-equivalent to 2.5.2; 2.5.2 already shipped Section 504 routing per CLAUDE.md, so 33-04 stacks a fresh patch on top)"
    - "`grep '\"version\"' packages/standards/package.json` returns `\"version\": \"2.5.2\"`; engine returns `\"version\": \"2.5.3\"`; components returns `\"version\": \"2.6.1\"`"
    - "Each of the 3 CHANGELOG.md files gains a new `## X.X.X — 2026-05-12` entry with literal `PUB-09` text and reference to Phase 33"
    - "All 3 package.json `prepublishOnly` scripts remain literally `npm run verify` — SC#5 holds (publish fails if lint/typecheck fails)"
    - "ROADMAP.md Phase 33 entry has its checkbox ticked and `Plans: [ ] 33-01 ... 33-04` are all marked complete"
    - "STATE.md is updated to reflect Phase 33 COMPLETE and v0.7 publish-gating effort closed"
  artifacts:
    - path: "packages/standards/package.json"
      provides: "Standards 2.5.2"
      contains: "\"version\": \"2.5.2\""
    - path: "packages/components/package.json"
      provides: "Components 2.6.1"
      contains: "\"version\": \"2.6.1\""
    - path: "packages/engine/package.json"
      provides: "Engine 2.5.3"
      contains: "\"version\": \"2.5.3\""
    - path: "packages/standards/CHANGELOG.md"
      provides: "Standards 2.5.2 release note for PUB-09"
      contains: "PUB-09"
    - path: "packages/components/CHANGELOG.md"
      provides: "Components 2.6.1 release note for PUB-09"
      contains: "PUB-09"
    - path: "packages/engine/CHANGELOG.md"
      provides: "Engine 2.5.3 release note for PUB-09"
      contains: "PUB-09"
  key_links:
    - from: "ROADMAP.md SC#1–#5"
      to: "verify chain across 3 packages"
      via: "all 3 prepublishOnly → verify → build && lint && typecheck && check:exports && check:types && test:ci"
      pattern: "lint && npm run typecheck"
---

<objective>
Final wave for Phase 33. Run `npm run verify` against all 3 packages in sequence to prove the new lint+typecheck gates work end-to-end, then bump PATCH versions and write CHANGELOG entries describing the PUB-09 closure. Update ROADMAP + STATE.

Purpose: Closes PUB-09 (and the v0.7 publish-gating effort). Gates SC#3 (all 3 verify exits 0) and SC#5 (prepublishOnly chain unchanged).

Version-bump rationale (PATCH across the board):
- This phase adds no public-API surface to any package. The `verify` script is consumed only by `prepublishOnly`; no downstream consumer reads `package.json#scripts.verify` of these packages. PATCH bumps signal "publishing pipeline hardened" without claiming a feature add.
- This deviates from the Phase 32 MINOR-for-test-coverage precedent because Phase 32 added test files (artifacts shipped in the source tree, even if not in `files: [...]`); Phase 33 only changes scripts + dev-side fixes. Dev-tooling cleanup is PATCH-honest.
- Engine baseline: `packages/engine/package.json` is currently `"version": "2.5.2"` (Section 504 routing already shipped — see CLAUDE.md inline notes). Plan 33-04 stacks a fresh `2.5.2 → 2.5.3` PATCH bump for the lint cleanup.

Output: Three PATCH bumps (standards 2.5.1→2.5.2, components 2.6.0→2.6.1, engine 2.5.2→2.5.3), three CHANGELOG entries, three sequential `npm run verify` exit-0 proofs, ROADMAP/STATE updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-lint-typecheck-verify-gates/33-01-SUMMARY.md
@.planning/phases/33-lint-typecheck-verify-gates/33-02-SUMMARY.md
@.planning/phases/33-lint-typecheck-verify-gates/33-03-SUMMARY.md
@packages/standards/package.json
@packages/components/package.json
@packages/engine/package.json
@packages/standards/CHANGELOG.md
@packages/components/CHANGELOG.md
@packages/engine/CHANGELOG.md

<interfaces>
Pre-Wave-2 state (after Plans 33-01..03 land):
- standards: version 2.5.1, verify chain includes lint+typecheck, both gates clean
- components: version 2.6.0, verify chain includes lint+typecheck, 27 prior tsc errors resolved, `@types/node@^22.10.2` added as devDependency (Route A1)
- engine: version 2.5.2 (already shipped Section 504 in this version — verified via current `packages/engine/package.json` read), verify chain includes lint+typecheck, 2 prior lint errors resolved

CHANGELOG formats vary across packages:
- standards: `## 2.5.1` style with `### Patch Changes` subsection, dash-prefixed entries, Changesets-style "Updated dependencies" callouts.
- engine: same `## 2.5.1` + `### Patch Changes` pattern.
- components: `## 2.6.0 — 2026-05-12` style with `### Added — …` subsection (Phase 32's introduced format). Less formal than the Changesets style.

This plan KEEPS each package's existing CHANGELOG voice. Do NOT homogenise.

Pickup from earlier plan summaries (read 33-01..03 summaries FIRST):
- If 33-02 summary flagged TS2724 + `--skipLibCheck` was applied, the engine CHANGELOG entry MUST call this out as a known compromise to revisit when puppeteer types fix lands upstream.
- If 33-03 summary flagged any deviation from the 5 planned categories (e.g. an unexpected 6th category surfaced), the components CHANGELOG entry MUST mention it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run sequential verify across all 3 packages — proves SC#3 + SC#5</name>
  <files>(none — read-only verification step)</files>
  <read_first>
    - .planning/phases/33-lint-typecheck-verify-gates/33-01-SUMMARY.md
    - .planning/phases/33-lint-typecheck-verify-gates/33-02-SUMMARY.md
    - .planning/phases/33-lint-typecheck-verify-gates/33-03-SUMMARY.md
    - packages/standards/package.json (confirm Wave-1 wire-up landed)
    - packages/components/package.json (confirm Wave-1 wire-up landed + @types/node devDep present)
    - packages/engine/package.json (confirm Wave-1 wire-up landed)
  </read_first>
  <action>
    Confirm Wave-1 deliverables:
    ```
    node -e "['standards','components','engine'].forEach(p=>{const j=require('./packages/'+p+'/package.json');const v=j.scripts.verify;const ok=v.includes('npm run lint && npm run typecheck && npm run check:exports');console.log(p,j.version,ok?'OK':'CHAIN_WRONG',j.scripts.prepublishOnly==='npm run verify'?'PP_OK':'PP_DRIFT')})"
    ```
    Expected output (three lines, each ending `OK PP_OK`):
    ```
    standards 2.5.1 OK PP_OK
    components 2.6.0 OK PP_OK
    engine 2.5.2 OK PP_OK
    ```

    Also confirm components Route A1 install landed:
    ```
    node -e "const j=require('./packages/components/package.json');console.log('@types/node:',j.devDependencies&&j.devDependencies['@types/node'])"
    ```
    Expected: `@types/node: ^22.10.2`. If missing, 33-03 was incomplete — STOP and rerun 33-03.

    Then run all three verify pipelines sequentially. Each MUST exit 0. Use `set -e` so the first failure halts:
    ```
    set -e
    npm run verify -w @holmdigital/standards
    npm run verify -w @holmdigital/components
    npm run verify -w @holmdigital/engine
    echo "ALL VERIFY EXIT 0"
    ```

    If any package's verify fails, STOP — the failure must be fixed in the responsible Wave-1 plan (rerun gap closure for that plan), not patched over here. Document which package failed and copy the relevant tail to a checkpoint message.

    Atomic commit: NONE for this task — read-only verification.
  </action>
  <verify>
    <automated>set -e; npm run verify -w @holmdigital/standards 2>&1 | tail -5 && npm run verify -w @holmdigital/components 2>&1 | tail -5 && npm run verify -w @holmdigital/engine 2>&1 | tail -5 && echo "ALL VERIFY EXIT 0"</automated>
  </verify>
  <done>
    - All three sequential `npm run verify -w @holmdigital/<each>` exit 0
    - All three `prepublishOnly` scripts unchanged (literal `npm run verify`)
    - All three `verify` scripts contain the new chain in the correct order
    - `packages/components/package.json` devDependencies includes `@types/node: ^22.10.2` (Route A1 confirmed landed)
  </done>
</task>

<task type="auto">
  <name>Task 2: PATCH bumps + CHANGELOG entries across all 3 packages</name>
  <files>
    packages/standards/package.json,
    packages/components/package.json,
    packages/engine/package.json,
    packages/standards/CHANGELOG.md,
    packages/components/CHANGELOG.md,
    packages/engine/CHANGELOG.md
  </files>
  <read_first>
    - packages/standards/CHANGELOG.md (top 20 lines — note the `## 2.5.1` heading style with `### Patch Changes`)
    - packages/engine/CHANGELOG.md (top 20 lines — same Changesets style)
    - packages/components/CHANGELOG.md (top 20 lines — `## 2.6.0 — 2026-05-12` style)
    - .planning/phases/33-lint-typecheck-verify-gates/33-02-SUMMARY.md (check for `--skipLibCheck` flag)
    - .planning/phases/33-lint-typecheck-verify-gates/33-03-SUMMARY.md (check for unexpected category flags)
  </read_first>
  <action>
    **Bump 1 — standards 2.5.1 → 2.5.2:**

    Edit `packages/standards/package.json`: change the single `"version"` line from `"2.5.1"` to `"2.5.2"`.

    Prepend to `packages/standards/CHANGELOG.md` (above the existing `## 2.5.1` heading, after the `# @holmdigital/standards` title):
    ```
    ## 2.5.2

    ### Patch Changes

    - PUB-09 (Phase 33): `verify` script now chains `lint` (eslint) and `typecheck` (tsc --noEmit) before `check:exports` / `check:types` / `test:ci`. `prepublishOnly` unchanged — npm publish now fails if lint or typecheck reports errors. No source changes; current source was already clean against both new gates.

    ```

    **Bump 2 — components 2.6.0 → 2.6.1:**

    Edit `packages/components/package.json`: change `"version"` from `"2.6.0"` to `"2.6.1"`.

    Prepend to `packages/components/CHANGELOG.md` (above the existing `## 2.6.0` heading, after the `# @holmdigital/components` title):
    ```
    ## 2.6.1 — 2026-05-12

    ### Changed — Publish hygiene (PUB-09, Phase 33)

    - `verify` script now chains `lint` (eslint) and `typecheck` (tsc --noEmit) before `check:exports` / `check:types` / `test:ci`. `prepublishOnly` unchanged — `npm publish` now fails if lint or typecheck reports errors.
    - Added `@types/node@^22.10.2` to devDependencies (matches engine + standards monorepo pin). Dev-side only — does not affect published runtime bundle.
    - Resolved 27 pre-existing `tsc --noEmit` errors across 5 categories (all dev-side; public API byte-equivalent to 2.6.0):
      1. Node.js types — `@types/node` resolution for `node:fs` / `node:path` / `node:url` / `__dirname` in test files (Route A1: devDependency install, no tsconfig change)
      2. vitest-axe matcher type augmentation — `declare module 'vitest'` for `toHaveNoViolations` in `src/_test/setup.ts`
      3. Read-only `ref.current` assignment removed in `RadioGroup.test.tsx` and `SkipLink.test.tsx` (kept the parallel `captured = node` assertion target)
      4. Removed unused `@ts-expect-error` directive in `expectKeyboardSequence.test.ts`
      5. Narrowed `LiveRegionLocale` resolution in `_i18n/live-region-strings.ts` at the two locale-fallback sites (`getAnnouncement` + `getDateAnnouncement`)
    - No public exports changed. No test behavior changed. No `as any` casts introduced.

    ```

    (If 33-03 summary flagged additional categories, append them as item 6+ with the same dev-side framing.)

    **Bump 3 — engine 2.5.2 → 2.5.3:**

    Edit `packages/engine/package.json`: change `"version"` from `"2.5.2"` to `"2.5.3"`. (Baseline 2.5.2 already shipped Section 504 routing per CLAUDE.md inline notes — this PATCH stacks on top.)

    Prepend to `packages/engine/CHANGELOG.md` (above the existing `## 2.5.2` heading, after `# @holmdigital/engine`):
    ```
    ## 2.5.3

    ### Patch Changes

    - PUB-09 (Phase 33): `verify` script now chains `lint` (eslint) and `typecheck` (tsc --noEmit) before `check:exports` / `check:types` / `test:ci`. `prepublishOnly` unchanged — `npm publish` now fails if lint or typecheck reports errors.
    - Resolved 2 pre-existing lint errors in `src/core/regulatory-scanner.ts`:
      - Declared `__ENGINE_VERSION__` as an ESLint readonly global (was flagged by `no-undef`; the type was already declared via `src/globals.d.ts`).
      - Swapped `// @ts-ignore` for `// @ts-expect-error` with rationale comment (per `@typescript-eslint/ban-ts-comment` rule defaults).
    - No source-behavior changes. Public API byte-equivalent to 2.5.2.

    ```

    **If 33-02 summary flagged TS2724 + `--skipLibCheck`**, append BEFORE the closing line break:
    ```
    - Known compromise: `typecheck` script runs `tsc --noEmit --skipLibCheck` because of pre-existing puppeteer types drift (TS2724). The flag suppresses third-party `.d.ts` validation only; project source is fully type-checked. Revisit when puppeteer or its `@types/puppeteer` consumer ships a compatible types update.
    ```

    Atomic commits (one per package — keeps blame readable and lets a single bump get reverted independently):

    1. `git commit -m "chore(standards): 2.5.1 → 2.5.2 + CHANGELOG (PUB-09)" -- packages/standards/package.json packages/standards/CHANGELOG.md`
    2. `git commit -m "chore(components): 2.6.0 → 2.6.1 + CHANGELOG (PUB-09)" -- packages/components/package.json packages/components/CHANGELOG.md`
    3. `git commit -m "chore(engine): 2.5.2 → 2.5.3 + CHANGELOG (PUB-09)" -- packages/engine/package.json packages/engine/CHANGELOG.md`

    Swedish characters (å, ä, ö) must be preserved if present in any file (CLAUDE.md MEMORY rule). The CHANGELOG bodies above contain none; double-check the existing entries above are not corrupted by editor settings.
  </action>
  <verify>
    <automated>node -e "const ok=['standards','components','engine'].every(p=>{const v=require('./packages/'+p+'/package.json').version;const expected={standards:'2.5.2',components:'2.6.1',engine:'2.5.3'}[p];const cl=require('fs').readFileSync('packages/'+p+'/CHANGELOG.md','utf8');const head=cl.split('\\n').slice(0,30).join('\\n');const cok=head.includes('## '+expected);const pubok=head.includes('PUB-09');if(v!==expected){console.error(p,'wrong version:',v,'expected',expected);return false}if(!cok){console.error(p,'CHANGELOG missing',expected);return false}if(!pubok){console.error(p,'CHANGELOG missing PUB-09');return false}return true});if(!ok){process.exit(1)};console.log('all three bumped + CHANGELOGed')"</automated>
  </verify>
  <done>
    - `packages/standards/package.json` version is `"2.5.2"`; CHANGELOG top entry is `## 2.5.2` containing `PUB-09`
    - `packages/components/package.json` version is `"2.6.1"`; CHANGELOG top entry is `## 2.6.1 — 2026-05-12` containing `PUB-09` and `Phase 33`
    - `packages/engine/package.json` version is `"2.5.3"`; CHANGELOG top entry is `## 2.5.3` containing `PUB-09`
    - All three commits scoped via `--` pathspec
    - All three `prepublishOnly` scripts still literally `npm run verify`
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md + STATE.md to reflect Phase 33 COMPLETE</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (find the Phase 33 block — currently around lines 247–256)
    - .planning/STATE.md (current position + last_activity fields)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`:
    1. Tick the Phase 33 checkbox in the milestone overview (search for `Phase 33` near the top of the v0.7 milestone block — if there's a header-level `- [ ]` checkbox, change to `- [x]`).
    2. Under the Phase 33 detail block (lines ~247–256), if there is a `**Plans:**` listing with `[ ]` items for `33-01-PLAN.md` ... `33-04-PLAN.md`, change each to `[x]`.
    3. Append a `**Completed:** 2026-05-12` line at the end of the Phase 33 detail block.

    Edit `.planning/STATE.md`:
    1. Update `stopped_at:` to: `Phase 33 COMPLETE — PUB-09 closed. 4 plans across 2 waves shipped (Wave 1 standards/engine/components verify wire-up + lint/typecheck fixes; Wave 2 sequential verify + 3-package PATCH bumps standards 2.5.1→2.5.2 / components 2.6.0→2.6.1 / engine 2.5.2→2.5.3 + CHANGELOG). All 3 packages now gate publish on lint + typecheck. v0.7 publish-gating effort complete.`
    2. Update `last_updated:` to current timestamp (use the format already in the file).
    3. Update `last_activity:` to `2026-05-12`.
    4. Update `progress.completed_phases` and `completed_plans` accordingly.
    5. Under `### Pending Todos`, mark `PUB-09` as resolved (or remove if listed).

    Atomic commit:
    `git commit -m "docs: Phase 33 COMPLETE — PUB-09 closed; v0.7 publish-gating done" -- .planning/ROADMAP.md .planning/STATE.md`
  </action>
  <verify>
    <automated>grep -c "Phase 33" .planning/ROADMAP.md | tr -d '\n' && echo " (Phase 33 references) " && grep -c "PUB-09" .planning/STATE.md | tr -d '\n' && echo " (PUB-09 in STATE)"</automated>
  </verify>
  <done>
    - ROADMAP.md Phase 33 entries marked complete
    - STATE.md `stopped_at` reflects Phase 33 closure
    - STATE.md `progress` counters updated
    - Atomic commit scoped via `--` pathspec
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final pre-publish smoke + checkpoint for Phase 33 closure</name>
  <what-built>
    Phase 33 closure delivered:
    - 3 packages now gate publish on `lint` + `typecheck` via extended `verify` chain
    - 27 pre-existing components-package tsc errors + 2 engine-package lint errors resolved as in-scope cleanup
    - PATCH bumps: standards 2.5.1→2.5.2, components 2.6.0→2.6.1, engine 2.5.2→2.5.3, each with PUB-09 CHANGELOG entry
    - ROADMAP + STATE reflect closure
  </what-built>
  <how-to-verify>
    Run from the repo root:

    1. Re-run all three verify pipelines in fresh shells to confirm no caching effects:
       ```
       npm run verify -w @holmdigital/standards
       npm run verify -w @holmdigital/components
       npm run verify -w @holmdigital/engine
       ```
       All three must exit 0.

    2. Confirm SC#5 by simulating publish dry-run on each (does NOT actually publish):
       ```
       npm publish -w @holmdigital/standards --dry-run
       npm publish -w @holmdigital/components --dry-run
       npm publish -w @holmdigital/engine --dry-run
       ```
       Each dry-run triggers `prepublishOnly` → `verify` → must exit 0. If any fails, SC#5 fails.

    3. Visually inspect each package.json:
       ```
       node -e "['standards','components','engine'].forEach(p=>{const j=require('./packages/'+p+'/package.json');console.log(p,j.version,'\\n  verify:',j.scripts.verify,'\\n  prepublishOnly:',j.scripts.prepublishOnly)})"
       ```
       Expected: 2.5.2 / 2.6.1 / 2.5.3, each `verify` containing the new chain, each `prepublishOnly` literal `npm run verify`.

    4. Visually inspect each CHANGELOG.md top entry — should mention `PUB-09` and the new chain.

    5. `git log --oneline -10` — should show:
       - Task 3 commit (docs)
       - 3 chore bumps (one per package)
       - 33-03 / 33-02 / 33-01 commits beneath
       - Plus the regulatory-scanner.ts fix commit and components fix commit

    6. Confirm no Swedish characters were corrupted by editor encoding in any file touched this phase: `grep -l "ÃƒÂ¥\|ÃƒÂ¤\|ÃƒÂ¶" packages/*/CHANGELOG.md packages/components/src/SkipLink/SkipLink.tsx 2>&1 | head -5` should return empty.

    Then signal: publish-ready, or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 33 closed and PUB-09 resolved, or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
- All three `npm run verify -w @holmdigital/<each>` exit 0
- All three `npm publish --dry-run -w @holmdigital/<each>` exit 0 (proves prepublishOnly → verify gate works)
- `node -e "['standards','components','engine'].forEach(p=>{const v=require('./packages/'+p+'/package.json').version;console.log(p,v)})"` prints `standards 2.5.2` / `components 2.6.1` / `engine 2.5.3`
- Each CHANGELOG.md top entry contains literal `PUB-09`
- No Swedish character corruption in touched files (CLAUDE.md MEMORY rule)
</verification>

<success_criteria>
PUB-09 closed. v0.7 publish-gating effort (Phase 33) complete. All 3 packages now gate publish on lint + typecheck. PATCH-bumped + CHANGELOGed. ROADMAP/STATE reflect closure. Project is ready to publish all three packages from this commit.
</success_criteria>

<output>
Create `.planning/phases/33-lint-typecheck-verify-gates/33-04-SUMMARY.md` recording:
- Output of the three sequential `npm run verify` runs (tail only)
- Output of the three `npm publish --dry-run` runs (tail only)
- Final version triplet: standards 2.5.2 / components 2.6.1 / engine 2.5.3
- Version-bump rationale (PATCH not MINOR — deviation from Phase 32 precedent justified by "no source artifacts added; only scripts and dev-side fixes". Phase 32 MINOR was a deliberate release-train marker for documented test-coverage features; Phase 33 is dev-tooling cleanup so PATCH-honest.)
- Note on the engine baseline: the spec brief mentioned bumping 2.5.1 → 2.5.2 but the package was already at 2.5.2 (Section 504 routing already shipped per CLAUDE.md inline notes). This plan stacks a fresh 2.5.2 → 2.5.3 patch. Document explicitly so the v0.7 ship notes are correct.
- Any TS2724 `--skipLibCheck` carry-over from 33-02
- Any unexpected category from 33-03
- Confirmation `prepublishOnly` strings are byte-equivalent to pre-phase
</output>
