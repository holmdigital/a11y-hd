---
phase: 26-publish-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/LiveRegion/LiveRegion.tsx
  - .gitignore
  - .planning/phases/22-test-infra-and-first-7-components/deferred-items.md
autonomous: true
requirements:
  - LIVEREGION-FIX
  - PUB-03
task_count: 3
must_haves:
  truths:
    - "packages/components DTS build succeeds end-to-end (no TS2503 on LiveRegion.tsx:37)"
    - "packages/standards/dist is no longer tracked by git (drift source eliminated)"
    - "5 LiveRegion tests + all 439 component tests still pass after the fix"
    - "Phase 22 deferred-items.md no longer lists the LiveRegion TS2503 as open"
  artifacts:
    - path: packages/components/src/LiveRegion/LiveRegion.tsx
      provides: "ReturnType<typeof setTimeout> type at line 37 (no NodeJS namespace ref)"
      contains: "ReturnType<typeof setTimeout>"
    - path: .gitignore
      provides: "Explicit packages/*/dist/ rule alongside existing dist/ glob"
      contains: "packages/*/dist/"
  key_links:
    - from: packages/components/src/LiveRegion/LiveRegion.tsx
      to: "tsup --dts pipeline"
      via: "type that resolves under lib: ['dom', 'esnext'] without @types/node"
      pattern: "useRef<ReturnType<typeof setTimeout>>"
    - from: .gitignore
      to: packages/standards/dist/
      via: "dist/ glob (existing line 8) + explicit packages/*/dist/ line"
      pattern: "packages/\\*/dist/"
---

<objective>
Land the keystone fixes that unblock every subsequent Phase 26 plan: (1) the long-deferred `LiveRegion.tsx:37` TS2503 that has been failing tsup's DTS step since Phase 22, and (2) the `packages/standards/dist/` git-tracking drift that has been polluting `git status` since the package was first published. Without (1), `attw --pack .` in Plan 26-05 cannot succeed (no complete dist). Without (2), Plan 26-05's clean-build invariant cannot hold.

Purpose: Make the entire pipeline reach a known-clean baseline before parallel work in Wave 2 begins. This plan is small but blocks everything.
Output: One-line type change, one untracked-dist commit, one gitignore tightening, one deferred-items cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-publish-hygiene/26-CONTEXT.md
@.planning/phases/26-publish-hygiene/26-RESEARCH.md
@packages/components/src/LiveRegion/LiveRegion.tsx
@.gitignore

<interfaces>
<!-- Current shape at LiveRegion.tsx:37 (the only line that changes): -->
```ts
const timeoutRef = useRef<NodeJS.Timeout>();
```
<!-- Target shape (single-line replacement; no imports added, no @types/node): -->
```ts
const timeoutRef = useRef<ReturnType<typeof setTimeout>>();
```
<!-- Rationale: setTimeout returns `number` in lib.dom.d.ts and `NodeJS.Timeout` in @types/node.
     `ReturnType<typeof setTimeout>` resolves to the correct platform-specific type WITHOUT
     pulling NodeJS namespace into the components package. -->

<!-- Current `.gitignore` line 7-8: -->
```
# Build output
dist/
```
<!-- The bare `dist/` glob matches recursively (so `packages/*/dist/` is already covered),
     but ROADMAP success criterion #2 mentions an explicit `packages/*/dist/` rule. We
     add the explicit line for documentation clarity; it is redundant with `dist/` and that
     redundancy is intentional. -->

<!-- Current tracked files in packages/standards/dist/ (per researcher git ls-files): -->
```
packages/standards/dist/index.d.mts
packages/standards/dist/index.d.ts
packages/standards/dist/index.js
packages/standards/dist/index.mjs
```
<!-- `git rm --cached -r` preserves the working-tree copies; only stops future tracking. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix LiveRegion.tsx:37 TS2503 (D-01)</name>
  <files>packages/components/src/LiveRegion/LiveRegion.tsx</files>
  <behavior>
    - LiveRegion.tsx line 37 declares `timeoutRef` with a type that resolves correctly under tsc's DTS emission step without referencing the NodeJS namespace.
    - The 5 existing LiveRegion tests continue to pass (no behavioural change — `setTimeout` return type is structurally compatible with the old `NodeJS.Timeout` in all places where `timeoutRef.current` is consumed at lines 42 and 49).
  </behavior>
  <action>
    Edit `packages/components/src/LiveRegion/LiveRegion.tsx` line 37 ONLY. Replace exactly:
    ```ts
    const timeoutRef = useRef<NodeJS.Timeout>();
    ```
    with:
    ```ts
    const timeoutRef = useRef<ReturnType<typeof setTimeout>>();
    ```
    Do NOT add any imports. Do NOT add `@types/node` to any package.json. Do NOT modify the consumer sites at lines 42 (`clearTimeout(timeoutRef.current)`) or 49 (`timeoutRef.current = setTimeout(...)`) — both are already compatible with the new type because `clearTimeout` accepts `ReturnType<typeof setTimeout> | undefined` and `setTimeout` returns exactly `ReturnType<typeof setTimeout>`.

    Per D-01 this is the smallest possible fix that unblocks the DTS build. Refactoring the timeout pattern is explicitly out of scope.
  </action>
  <verify>
    <automated>cd packages/components && npx vitest run src/LiveRegion --reporter=default 2>&amp;1 | tail -20 && npm run build 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - LiveRegion.tsx:37 contains `useRef&lt;ReturnType&lt;typeof setTimeout&gt;&gt;()` exactly.
    - `npx vitest run src/LiveRegion` reports 5 tests passing.
    - `npm run build -w @holmdigital/components` exits 0 with no DTS errors (the previous TS2503 on this line is gone). All 30 entries (1 index + 29 components) emit `.js`, `.mjs`, AND `.d.ts` + `.d.mts` files.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Untrack packages/standards/dist/ and tighten .gitignore (PUB-03 part 1)</name>
  <files>.gitignore, packages/standards/dist/* (untracked, working-tree preserved)</files>
  <action>
    Two ordered git operations:

    1. Untrack the 4 standards dist files without deleting them on disk:
       ```bash
       git rm --cached -r packages/standards/dist/
       ```
       This is a non-destructive operation per the safety protocol: working-tree copies of `index.d.mts`, `index.d.ts`, `index.js`, `index.mjs` remain, but git stops tracking them. Subsequent `git status` must show them under "Untracked files" briefly, then nothing once the `.gitignore` rule matches them.

    2. Append an explicit per-package rule to `.gitignore` after the existing `dist/` line. Find the "# Build output" block (currently lines 7-9):
       ```
       # Build output
       dist/
       build/
       ```
       Replace with:
       ```
       # Build output
       dist/
       packages/*/dist/
       build/
       ```
       This explicit line is REDUNDANT with the existing `dist/` rule (which already matches recursively) but documents the per-package intent stated in ROADMAP success criterion #2. Keep both lines — the bare `dist/` covers in-package nested cases; the `packages/*/dist/` line surfaces intent in a grep.

    Verify with `git status` that no `packages/*/dist/*` paths appear under "Changes" or "Untracked files" after both operations. The 4 files must remain on disk (`ls packages/standards/dist/` shows them) but be invisible to git.

    Per researcher Open Q (none specific to this task; standard git mechanic). Do NOT add a `postinstall: npm run build` hook — that would slow every install. Plan 26-05's `verify` chain rebuilds `dist/` before any consumer step.
  </action>
  <verify>
    <automated>git ls-files packages/standards/dist/ | grep . &amp;&amp; echo "FAIL: still tracked" &amp;&amp; exit 1 || echo "ok: untracked"; ls packages/standards/dist/*.{js,mjs,d.ts,d.mts} &gt; /dev/null 2>&amp;1 &amp;&amp; echo "ok: working-tree preserved" || (echo "FAIL: working-tree files gone" &amp;&amp; exit 1); grep -E "^packages/\*/dist/$" .gitignore &gt; /dev/null &amp;&amp; echo "ok: explicit rule present" || (echo "FAIL: explicit rule missing" &amp;&amp; exit 1)</automated>
  </verify>
  <done>
    - `git ls-files packages/standards/dist/` outputs nothing (4 files no longer tracked).
    - `packages/standards/dist/{index.js,index.mjs,index.d.ts,index.d.mts}` exist on disk (preserved).
    - `.gitignore` contains both `dist/` and `packages/*/dist/` lines (explicit doc clarity).
    - `git status` shows no changes related to `packages/*/dist/*`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Close the Phase 22 LiveRegion deferred-items entry</name>
  <files>.planning/phases/22-test-infra-and-first-7-components/deferred-items.md</files>
  <action>
    The LiveRegion TS2503 issue has been carried in Phase 22's `deferred-items.md` since the phase shipped. Task 1 resolves it. Remove the corresponding entry from `deferred-items.md`:

    1. Read `.planning/phases/22-test-infra-and-first-7-components/deferred-items.md`.
    2. Locate the section/entry referencing `LiveRegion.tsx:37` and/or `NodeJS.Timeout` and/or `TS2503`. (Exact heading text is unknown without reading — the executor reads the file then removes the matching block.)
    3. Either delete the entry entirely OR move it under a "Resolved" sub-heading with a back-reference: `Resolved by Phase 26 Plan 01 (D-01) on 2026-05-11`. Prefer deletion if the file has a "Resolved" history convention already; otherwise add the back-reference inline.

    If `deferred-items.md` does NOT exist at that path OR contains no LiveRegion reference, this task is a no-op — document the absence in the task summary and proceed. Do NOT create the file if missing.
  </action>
  <verify>
    <automated>grep -E "LiveRegion.*TS2503|NodeJS\.Timeout.*LiveRegion|LiveRegion\.tsx:37" .planning/phases/22-test-infra-and-first-7-components/deferred-items.md 2>&amp;1 | grep -v "Resolved" | grep . &amp;&amp; echo "FAIL: open entry remains" &amp;&amp; exit 1 || echo "ok: no open LiveRegion entry"</automated>
  </verify>
  <done>
    - No OPEN entry in `deferred-items.md` references `LiveRegion.tsx:37` / `NodeJS.Timeout` / `TS2503`.
    - If an entry was removed or marked resolved, the change references "Phase 26 Plan 01 / D-01".
  </done>
</task>

</tasks>

<verification>
End-to-end check for the plan:

```bash
# 1. Type fix is the exact single-line replacement
grep -n "useRef<ReturnType<typeof setTimeout>>" packages/components/src/LiveRegion/LiveRegion.tsx
grep -n "useRef<NodeJS.Timeout>" packages/components/src/LiveRegion/LiveRegion.tsx && echo "FAIL: old type remains" && exit 1 || echo "ok"

# 2. Component build succeeds end-to-end (DTS no longer fails)
npm run build -w @holmdigital/components

# 3. Component tests still pass (439 baseline)
npm run test:ci -w @holmdigital/components

# 4. Git state: standards/dist untracked, working tree preserved
git ls-files packages/standards/dist/   # empty
ls packages/standards/dist/*.js packages/standards/dist/*.mjs packages/standards/dist/*.d.ts packages/standards/dist/*.d.mts  # all 4 present
```
</verification>

<success_criteria>
- LiveRegion.tsx line 37 holds the new type literal exactly; no `NodeJS.Timeout` references anywhere in the file.
- `npm run build -w @holmdigital/components` exits 0 with `.d.ts` + `.d.mts` emitted for ALL 30 entries (1 + 29 components).
- 27 test files / 439 component tests remain green.
- `git ls-files packages/standards/dist/` is empty; the 4 dist files exist on disk.
- `.gitignore` contains both `dist/` and `packages/*/dist/`.
- Phase 22 `deferred-items.md` no longer has an open LiveRegion TS2503 entry.
- Phase 26 Wave 2 (plans 02, 03, 04) is now unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/26-publish-hygiene/26-01-SUMMARY.md` per the standard summary template. Include:
- Diff of LiveRegion.tsx:37 (one-line change).
- Git output proving standards/dist is untracked (`git ls-files packages/standards/dist/` empty).
- `.gitignore` diff (new explicit line).
- Outcome of the Phase 22 deferred-items edit (deleted vs marked resolved vs no-op).
- Confirmation that `npm run build -w @holmdigital/components` exits 0 and 439 tests stay green.
</output>
