---
phase: 34-klarsprak-plain-language-report
plan: 05
type: execute
wave: 3
depends_on: ["34-02", "34-04"]
files_modified:
  - packages/engine/src/cli/index.ts
  - packages/engine/src/reporting/html-template.ts
  - packages/engine/src/reporting/html-template.test.ts
  - .changeset/standards-plain-language-copy.md
  - .changeset/engine-plain-report.md
autonomous: false
requirements: [PLAIN-04, PLAIN-05]
nyquist_compliant: true
wave_0_complete: true
user_setup:
  - service: johancask.com test scan
    why: "D-09 delivery gate: Karin reviews the rendered plain terminal + PDF before npm release"
    env_vars: []
    dashboard_config:
      - task: "Run npx hd-a11y-scan https://johancask.com --plain and --plain --pdf karin.pdf; Karin approves tone/layout"
        location: "local terminal + generated PDF file"

must_haves:
  truths:
    - "hd-a11y-scan <url> --plain sets audience='plain'; --audience plain is equivalent; default stays developer (PLAIN-04)"
    - "Flag precedence is json > light > plain: the plain branch sits before the final else dashboard, after the json and light branches (D-12)"
    - "--plain --json yields JSON (with plainLanguage data); --plain --light yields light output (D-12)"
    - "--plain --pdf produces a plain-language PDF mirroring the terminal: opening + impact-sorted list (5 fields + badge) + neutral closing + footer with URL/date/version; NO score, NO WCAG/DIGG tables, NO legal sections (D-08)"
    - "generateReportHTML(result, sector, audience='developer') has a defaulted third param so existing two-arg callers compile unchanged; the developer PDF is byte-for-byte unchanged (D-08)"
    - "The D-13 snapshot baseline (captured by plan 04 BEFORE this plan's html-template change) still passes WITHOUT any snapshot update after the audience param + plain template land — this is the mechanical proof of D-08's byte-for-byte promise (D-13)"
    - "The plain PDF footer's tool version comes from getEngineVersion() — the build-time __ENGINE_VERSION__ injected via tsup define from packages/engine/package.json — and equals the engine package version, never the root or standards version (D-16)"
    - "Two changesets exist: standards minor + engine minor (per CLAUDE.md versioning; standards 2.6.1->2.7.0, engine 2.5.6->2.6.0)"
    - "Exact wordings (opening, closing, empty-state, the 8 English texts) are finalized only at the Task 4 human checkpoint: johancask.com terminal + PDF presented for Karin's editorial approval, with the Version Packages PR merge as the release gate — npm publish never runs inside the phase (D-09)"
  artifacts:
    - path: "packages/engine/src/cli/index.ts"
      provides: "--audience / --plain flags, options merge + type cast, plain print branch, plain PDF branch"
      contains: "--plain"
    - path: "packages/engine/src/reporting/html-template.ts"
      provides: "audience param + plain HTML template"
      contains: "audience"
    - path: ".changeset/standards-plain-language-copy.md"
      provides: "standards minor changeset"
      contains: "@holmdigital/standards"
    - path: ".changeset/engine-plain-report.md"
      provides: "engine minor changeset"
      contains: "@holmdigital/engine"
  key_links:
    - from: "packages/engine/src/cli/index.ts"
      to: "renderPlainReport"
      via: "dynamic import in plain print branch"
      pattern: "import\\('\\.\\./reporting/plain-report'\\)"
    - from: "packages/engine/src/cli/index.ts"
      to: "generateReportHTML(result, sector, 'plain')"
      via: "audience arg in PDF branch"
      pattern: "'plain'"
    - from: "packages/engine/src/reporting/html-template.ts"
      to: "getEngineVersion()"
      via: "plain footer version source (D-16 — __ENGINE_VERSION__ from engine package.json)"
      pattern: "getEngineVersion\\(\\)"
---

<objective>
Wire the renderer into the CLI and PDF pipeline, then deliver for review. Add the `--audience <developer|plain>` flag and `--plain` alias, merge them into options with the correct precedence, insert the plain print branch in the correct position (D-12: after json and light, before the dashboard else), add the plain PDF branch, give `generateReportHTML` a defaulted `audience` param plus a plain HTML template that mirrors the terminal (D-08), write the two minor changesets, and run the johancask.com test scan for Karin's approval (D-09).

Two review-mandated locks apply here: the D-13 snapshot baseline (captured by plan 04 in wave 2, before this plan touches html-template.ts) MUST still pass without a snapshot update after the audience param lands — that is the mechanical proof of D-08's byte-for-byte developer-PDF promise. And the plain footer's version (D-16) MUST come from `getEngineVersion()`, the build-time `__ENGINE_VERSION__` injected via tsup define from packages/engine/package.json — never a root or standards version, never a runtime package.json read.

This plan is wave 3: it depends on plan 02 (enrichment data in --json / reports) and plan 04 (the renderPlainReport function it imports + the D-13 baseline it must not break).

Purpose: This plan turns the renderer and data into an actual user-invokable feature and then hands it to the human gate. The developer report and developer PDF must remain byte-for-byte unchanged (default audience), now mechanically proven via D-13.
Output: CLI flags + branches; plain HTML template; two changesets; a johancask.com terminal + PDF scan presented for D-09 approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-klarsprak-plain-language-report/34-CONTEXT.md
@.planning/phases/34-klarsprak-plain-language-report/34-RESEARCH.md
@.planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md
@.planning/phases/34-klarsprak-plain-language-report/daniel-engine-klarsprak-kontextgranskning-2026-06-11.md
@.planning/phases/34-klarsprak-plain-language-report/34-02-SUMMARY.md
@.planning/phases/34-klarsprak-plain-language-report/34-04-SUMMARY.md
@packages/engine/src/cli/index.ts
@packages/engine/src/reporting/html-template.ts

<interfaces>
<!-- Verified by direct source read 2026-06-11; CLI line positions re-confirmed by kontextgranskning (rad 203/218/220/240). -->

CLI options declaration block (cli/index.ts lines 45-67) — chained `.option()` calls; the last is `.option('--light', '...')`. Add `--audience <mode>` (with default 'developer') and `--plain` after it.

CLI options merge block (lines 75-120) — `cliOptions.X ?? fileConfig.X ?? default` pattern (e.g. line 97 `light: cliOptions.light ?? fileConfig.light ?? false`). The block ends with an `as ScannerOptions & { ...; light: boolean; }` type cast (lines 98-120). cosmiconfig already loads fileConfig so `audience: "plain"` in .a11yrc works through this same merge.

CLI print chain (lines 203-241): `if (options.json && options.light) {...} else if (options.json) {...} else if (options.light) {...} else { /* CLI DASHBOARD */ }`. D-12 insertion: a new `else if (options.audience === 'plain')` branch BETWEEN the light branch and the final else (before line ~240 per kontextgranskning).

CLI PDF block (lines 172-177): `if (options.pdf) { ... const html = generateReportHTML(result, options.sector as 'public' | 'private'); await generatePDF(html, options.pdf); ... }`. Branch the html line on audience.

Dynamic-import pattern already used in the CLI (e.g. github-actions reporter): `const { x } = await import('../reporting/x');`. Use it for renderPlainReport.

generateReportHTML signature (html-template.ts line 27): `export function generateReportHTML(result: ScanResult, sector: 'public' | 'private' = 'public'): string`. Add third param `audience: 'developer' | 'plain' = 'developer'` (defaulted — Pitfall 5: existing line-174 two-arg caller must still compile). The developer template renders counts + remediation text + a footer using t('report.footer', { version: getEngineVersion() }); it does NOT render raw failingNodes HTML. formatDate uses LOCALE_TO_INTL[getCurrentLang()].

D-13 regression lock (from plan 04): `packages/engine/src/reporting/html-template.test.ts` + `__snapshots__/html-template.test.ts.snap` exist and snapshot the developer HTML via TWO-arg calls. After this plan's html-template edits, those tests must pass WITHOUT `--update`/`-u` and the .snap file must be byte-unchanged (git diff clean). If the snapshot fails, the developer template was altered — fix the template, never the snapshot.

D-16 version source: `getEngineVersion()` (export of '../core/regulatory-scanner') returns the `__ENGINE_VERSION__` global injected from packages/engine/package.json by tsup.config.ts:13 (build) and vitest.config.ts:8 (tests). regulatory-scanner.test.ts:52-56 already proves it equals engine pkg.version. The plain footer MUST use getEngineVersion() — no `require('../../package.json')`, no fs read, no standards/root version.

Plain template must mirror the terminal (D-08): opening (t('plain.intro_framing') + t('plain.sorted_by')) + impact-sorted numbered list (the same levelOf/sort logic as the renderer; 5 fields via t('plain.what_happens') etc. + clear-text badge via t('plain.badge_*')) + neutral closing (t('plain.closing')) + a footer with result.url, formatted result.timestamp, and getEngineVersion() (D-16). NO score, NO WCAG/DIGG tables, NO legal-context sections.

Changeset format (from git history analog, retrieved earlier):
```
---
"@holmdigital/standards": minor
---

**Title** (date): description of new public API + deferred items.
```
The .changeset/ directory is currently empty (prior changesets consumed by Version Packages). Current versions: standards 2.6.1, engine 2.5.6. Next: standards 2.7.0, engine 2.6.0. components UNTOUCHED (no changeset).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --audience/--plain flags, options merge + type cast, plain print branch (D-12), plain PDF branch</name>
  <files>packages/engine/src/cli/index.ts</files>
  <read_first>
    - packages/engine/src/cli/index.ts (lines 45-67 options block; 75-120 merge + cast; 172-177 PDF block; 203-241 print chain)
    - .planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md (exact insertion excerpts for all four CLI edit points)
    - .planning/phases/34-klarsprak-plain-language-report/34-CONTEXT.md (D-12 precedence, D-08 PDF audience arg)
    - .planning/phases/34-klarsprak-plain-language-report/34-04-SUMMARY.md (renderPlainReport signature as actually shipped)
  </read_first>
  <action>
    Four edits in cli/index.ts:
    (1) Options block: after `.option('--light', ...)` add `.option('--audience <mode>', 'Output audience: developer (default) or plain', 'developer')` and `.option('--plain', 'Alias for --audience plain (klarspraakslaege for non-technical recipients)')`.
    (2) Merge block: add `plain: cliOptions.plain ?? fileConfig.plain ?? false,` and `audience: cliOptions.plain ? 'plain' : (cliOptions.audience || fileConfig.audience || 'developer'),`. In the `as ScannerOptions & { ... }` cast, add `plain: boolean;` and `audience: 'developer' | 'plain';`.
    (3) Print chain: insert `} else if (options.audience === 'plain') { const { renderPlainReport } = await import('../reporting/plain-report'); renderPlainReport(result, options.lang); ` BETWEEN the `else if (options.light)` block and the final `else {` dashboard block (D-12 — json and light branches stay above; dashboard stays the final else). Do NOT reorder the json/light branches.
    (4) PDF block: change the html assignment to branch on audience: when `options.audience === 'plain'`, call `generateReportHTML(result, options.sector as 'public' | 'private', 'plain')`; otherwise the existing two-arg call. Keep the rest of the if(options.pdf) block unchanged.

    No `as any`. The developer path (no flags) must be untouched in behavior.
  </action>
  <verify>
    <automated>npm run typecheck -w @holmdigital/engine</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck -w @holmdigital/engine` exits 0 (audience field typed; cast updated).
    - Source assertion: cli/index.ts contains `.option('--plain'` and `.option('--audience <mode>'`.
    - Source assertion: the merge block contains `audience: cliOptions.plain ? 'plain' :` and the cast contains `audience: 'developer' | 'plain';`.
    - Source assertion (D-12): the `else if (options.audience === 'plain')` branch appears AFTER `else if (options.light)` and BEFORE the final `else {` dashboard block (verify line ordering).
    - Source assertion: the PDF branch passes `'plain'` as the third arg to generateReportHTML only when audience is plain; the two-arg developer call path remains.
    - Source assertion: no `as any` introduced in cli/index.ts.
  </acceptance_criteria>
  <done>CLI flags, merge, cast, D-12 print branch, and plain PDF branch wired; engine typecheck green; developer path untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add audience param + plain HTML template to generateReportHTML (D-08); D-13 snapshot must stay green untouched; D-16 footer version source</name>
  <files>packages/engine/src/reporting/html-template.ts, packages/engine/src/reporting/html-template.test.ts</files>
  <read_first>
    - packages/engine/src/reporting/html-template.ts (line 27 signature; the developer template body, footer pattern, formatDate, LOCALE_TO_INTL; confirm it does NOT render raw failingNodes HTML)
    - packages/engine/src/reporting/html-template.test.ts (the D-13 snapshot baseline from plan 04 — the regression lock this task must NOT invalidate)
    - packages/engine/src/reporting/plain-report.ts (the levelOf/sort logic, badge mapping, t() keys — mirror them in the plain HTML template)
    - .planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md (html-template plain-branch + generatePlainReportHTML excerpt)
    - .planning/phases/34-klarsprak-plain-language-report/34-CONTEXT.md (D-08 — what the plain PDF includes and excludes; D-13, D-16 — Granskningstillägg)
  </read_first>
  <action>
    Change the signature to `export function generateReportHTML(result: ScanResult, sector: 'public' | 'private' = 'public', audience: 'developer' | 'plain' = 'developer'): string`. The default keeps the line-174 two-arg caller compiling (Pitfall 5). At the TOP of the function body, before `const criticalCount = ...`, add `if (audience === 'plain') { return generatePlainReportHTML(result); }`. Then add a private `function generatePlainReportHTML(result: ScanResult): string` in the same file.

    D-13 constraint: the developer template code below the new guard must not change by a single byte. After the edit, run the test suite WITHOUT any snapshot-update flag — the plan-04 D-13 snapshot must pass as-is and `__snapshots__/html-template.test.ts.snap` must show no diff. If the snapshot fails, the developer HTML changed: fix the template edit, NEVER regenerate the snapshot.

    The plain template mirrors the terminal (D-08): a valid HTML document (DOCTYPE, charset UTF-8, lang from getCurrentLang()) with title t('plain.report_title', { url: result.url }); body = opening paragraph (t('plain.intro_framing'), then t('plain.intro_found', {count, unit}) + t('plain.sorted_by')); an impact-sorted numbered list using the SAME levelOf + IMPACT_ORDER + RISK_TO_IMPACT logic as plain-report.ts (extract or duplicate the maps), each item showing the clear-text badge (t('plain.badge_*')) and the five fields with t() labels (t('plain.what_happens') etc.) and report.plainLanguage values (fallback to report.remediation.description when plainLanguage is undefined); a neutral closing paragraph (t('plain.closing')); and a discreet footer containing result.url, the formatted result.timestamp (reuse formatDate / LOCALE_TO_INTL), and the tool version. D-16: the version MUST come from `getEngineVersion()` (the build-time `__ENGINE_VERSION__` injected via tsup define from packages/engine/package.json) — no `require`/fs read of any package.json at runtime, no root or standards version. Do NOT include result.score, the score block, WCAG criteria tables, DIGG/diggRisk tables, or any legalContext section (D-08).

    HTML-escape any dynamic value interpolated into markup that originates from user input or scanned content — at minimum result.url (user-supplied via CLI) — to prevent HTML injection in the PDF (reuse an escape helper if html-template.ts has one; otherwise escape <, >, & on the url and any plainLanguage/remediation strings rendered into element bodies).

    Extend html-template.test.ts (the plan-04 file) with a new describe block "generateReportHTML plain template (D-08/D-16)" — do NOT touch the existing D-13 describe block or the snapshot file. Add: (a) an assertion that generateReportHTML(fixture, 'public', 'plain') returns a string containing plain chrome (e.g. the plain.report_title text) and NOT containing the developer score-block substring; (b) a D-16 assertion that the plain HTML contains getEngineVersion()'s value — mirroring regulatory-scanner.test.ts:55, read engine package.json's version in the test and assert the footer contains exactly that string (proves footer version === engine pkg.version, not root/standards).
  </action>
  <verify>
    <automated>npm run test:ci -w @holmdigital/engine</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:ci -w @holmdigital/engine` exits 0 — including the plan-04 D-13 snapshot tests, run WITHOUT any update flag.
    - Behavior assertion (D-13): `git diff --stat packages/engine/src/reporting/__snapshots__/html-template.test.ts.snap` reports no changes after this task — the snapshot file is byte-identical to plan 04's committed baseline (mechanical proof of D-08 byte-for-byte).
    - Source assertion: signature is `generateReportHTML(result: ScanResult, sector: 'public' | 'private' = 'public', audience: 'developer' | 'plain' = 'developer'): string`.
    - Source assertion: the function body's first statement after the opening brace is the `if (audience === 'plain') return generatePlainReportHTML(result);` guard.
    - Source assertion: generatePlainReportHTML contains t('plain.report_title'), t('plain.closing'), getEngineVersion(), and result.url in the footer; it contains NO reference to result.score, no WCAG-criteria table, no legalContext.
    - Source assertion (D-16): the plain footer's version expression is `getEngineVersion()` — zero occurrences of `require('../../package.json')`, `readFileSync`, or any other package.json read in html-template.ts.
    - Behavior assertion (D-16): the new test asserts the plain HTML contains the version string read from packages/engine/package.json (engine pkg.version, per the regulatory-scanner.test.ts:55 pattern) — proving the footer shows the engine version, not root or standards.
    - Behavior assertion: the plain-template assertion confirms generateReportHTML(fixture, 'public', 'plain') contains plain chrome and NOT the developer score block substring.
    - Source assertion: result.url is HTML-escaped before interpolation into the plain template markup.
  </acceptance_criteria>
  <done>generateReportHTML has defaulted audience param + plain template mirroring the terminal; D-13 snapshot green and byte-unchanged; D-16 footer version proven equal to engine package.json version; url escaped; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Write the two minor changesets (standards + engine)</name>
  <files>.changeset/standards-plain-language-copy.md, .changeset/engine-plain-report.md</files>
  <read_first>
    - .planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md (.changeset format from git-history analog)
    - packages/standards/package.json (confirm current version 2.6.1)
    - packages/engine/package.json (confirm current version 2.5.6)
    - .planning/phases/34-klarsprak-plain-language-report/34-CONTEXT.md (D-02 deferred languages note for the changeset body)
  </read_first>
  <action>
    Create `.changeset/standards-plain-language-copy.md` with front matter `"@holmdigital/standards": minor` and a body describing the new public API: PlainLanguageCopy interface, BusinessImpactLevel type, plainLanguage optional field on ConvergenceRule and RegulatoryReport, 8 Swedish + 8 English plain-language texts, EN-fallback in generateRegulatoryReport. Note that real translations for de/fr/es/nl/fi/dk/no are deferred (D-02).

    Create `.changeset/engine-plain-report.md` with front matter `"@holmdigital/engine": minor` and a body describing: renderPlainReport terminal renderer, --plain / --audience plain CLI flags, plain-language PDF mode via generateReportHTML audience param, plain.* i18n chrome in all 9 locale files (en/sv real, other 7 English-valued pending native review), and the D-13 developer-PDF snapshot guard.

    Do NOT add a components changeset (untouched this phase). Use the Write tool. Keep Swedish characters correct if any appear in the changeset body.
  </action>
  <verify>
    <automated>npx changeset status --since=HEAD~1 2>&1 | Select-String -Pattern "standards|engine" -SimpleMatch</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `.changeset/standards-plain-language-copy.md` exists with front matter line `"@holmdigital/standards": minor` and mentions PlainLanguageCopy and BusinessImpactLevel.
    - Source assertion: `.changeset/engine-plain-report.md` exists with front matter line `"@holmdigital/engine": minor` and mentions renderPlainReport and --plain.
    - Source assertion: no changeset references `@holmdigital/components`.
    - Behavior assertion: `npx changeset status` lists standards and engine as pending minor bumps (standards 2.6.1->2.7.0, engine 2.5.6->2.6.0) and does NOT list components.
  </acceptance_criteria>
  <done>Two minor changesets created (standards + engine); components excluded; changeset status reflects the two minor bumps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: D-09 delivery gate — johancask.com plain terminal + PDF for Karin approval</name>
  <files>(no source files — human verification + editorial sign-off)</files>
  <action>
    This is a blocking human-verification checkpoint, not an automated task. Before reaching it, confirm all automated gates are green: `npm run verify -w @holmdigital/standards` and `npm run verify -w @holmdigital/engine` both exit 0 (D-10.1-4, D-13 snapshot byte-unchanged, D-16 version assertion, PLAIN-02 enrichment + EN fallback, renderer structure tests, lint zero-warning). Then run the johancask.com scans listed under how-to-verify and present the terminal output + the generated karin.pdf to the user for Karin's editorial sign-off on tone and layout. Do NOT run npm publish — release happens via the Version Packages PR after approval (D-09).
  </action>
  <what-built>
    The complete plain-language mode: `--plain` / `--audience plain` CLI flags, the terminal renderer, the plain-language PDF, all chrome localized via t(), and the two minor changesets. All automated guards green: D-10.1-4, D-13 developer-PDF snapshot (byte-unchanged), D-16 footer version source, PLAIN-02 enrichment + EN fallback, renderer structure tests, full verify in both packages.
  </what-built>
  <how-to-verify>
    Run the D-09 delivery scan locally and have Karin review tone + layout (this is the editorial approval gate; exact wordings of opening / closing / empty-state / English texts were drafted during execution per tone rules and are finalized here):

    1. Terminal: `npx hd-a11y-scan https://johancask.com --plain`
       Confirm: opening framing with NO "15-40" number claim and NO blame; impact-sorted list (Stoppar köp items first, Värt att putsa last); each row shows the four labeled fields in business-first order plus a clear-text badge in the right color (red.bold / red / yellow / gray); a neutral closing line with no sales CTA; NO compliance score anywhere.
    2. Swedish encoding: confirm å/ä/ö render correctly in the terminal (e.g. "Försämrar upplevelsen", "köp").
    3. PDF: `npx hd-a11y-scan https://johancask.com --plain --pdf karin.pdf` then open karin.pdf.
       Confirm: same content as terminal (opening + sorted 5-field list + badges + neutral closing); a discreet footer with the URL, scan date, and tool version matching packages/engine/package.json (D-16); NO score, NO WCAG/DIGG tables, NO legal sections.
    4. Precedence sanity: `npx hd-a11y-scan https://johancask.com --plain --json` outputs JSON (containing plainLanguage) NOT the plain report; `--plain --light` outputs the light report (D-12).
    5. Developer report unchanged: `npx hd-a11y-scan https://johancask.com` still shows the developer dashboard with score; `npx hd-a11y-scan https://johancask.com --pdf dev.pdf` produces the developer PDF as before (D-13 snapshot already proves the HTML is byte-identical).

    Karin's approval of the terminal + PDF (and subsequently the Version Packages PR merge) is the release gate per D-09. Do NOT publish to npm in this plan — release happens via the Version Packages PR after approval.
  </how-to-verify>
  <verify>
    <human-check>Karin reviews the johancask.com plain terminal output and karin.pdf; confirms tone (no blame, no 15-40 claim, no sales CTA), business-first 5-field layout, correct badges/colors, å/ä/ö encoding, footer with URL/date/engine-version (D-16), and absence of score/WCAG/legal sections; precedence (--plain --json -> JSON, --plain --light -> light) and unchanged developer report/PDF (D-13) verified.</human-check>
  </verify>
  <resume-signal>Type "approved" (Karin signed off on tone + layout for both terminal and PDF), or describe the wording/layout issues to fix.</resume-signal>
  <done>Karin approved the plain terminal + PDF via the D-09 gate; phase ready for the Version Packages release PR (no npm publish performed in this plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (url) → PDF HTML | result.url is user-supplied via CLI and flows into the plain HTML template; unescaped interpolation would allow HTML injection into the generated PDF. |
| scanned-page content → plain PDF | plainLanguage is internal authored data; remediation.description is internal; the plain template (like the developer template) does NOT render raw failingNodes HTML, keeping the injection surface to dynamic scalar values only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08 | Tampering | PDF HTML injection via result.url in plain template | mitigate | Task 2 escapes <, >, & on result.url (and on any plainLanguage/remediation strings rendered into element bodies) before interpolation; reuses existing html-template escaping pattern |
| T-34-09 | Information disclosure | score/WCAG/legal data leaking into the customer-facing plain PDF | mitigate | D-08 forbids them; acceptance criterion asserts no result.score / WCAG table / legalContext in generatePlainReportHTML |
| T-34-12 | Tampering | silent developer-PDF regression via the audience param edit | mitigate | D-13: plan-04 snapshot must pass without update and the .snap file must be git-diff clean after Task 2 |
| T-34-13 | Spoofing | misleading footer version (wrong package.json in the monorepo) | mitigate | D-16: footer uses getEngineVersion() (__ENGINE_VERSION__ from engine package.json via tsup/vitest define); test asserts footer === engine pkg.version |
| T-34-10 | Elevation of privilege | accidental npm publish before Karin approval | accept | Release is gated behind the Version Packages PR merge (D-09); this plan explicitly does NOT run npm publish; checkpoint blocks on human approval |
| T-34-SC | Tampering | npm installs | accept | Zero new dependencies (RESEARCH Package Legitimacy Audit); changesets only configure version bumps |
</threat_model>

<verification>
- `npm run typecheck -w @holmdigital/engine` exits 0
- `npm run verify -w @holmdigital/standards && npm run verify -w @holmdigital/engine` both exit 0 (full chain, lint zero-warning, no `as any`)
- D-13: snapshot tests pass without update; `__snapshots__/html-template.test.ts.snap` byte-unchanged vs plan 04 baseline
- D-16: plain footer version assertion green (equals packages/engine/package.json version)
- `npx changeset status` lists standards + engine minor bumps, not components
- Manual D-09 gate: johancask.com terminal + PDF approved by Karin
</verification>

<success_criteria>
- --plain / --audience plain work; default stays developer; precedence json > light > plain (D-12)
- Plain PDF mirrors terminal; developer PDF byte-for-byte unchanged, proven by the untouched D-13 snapshot
- generateReportHTML audience param defaulted (two-arg callers compile)
- Plain footer version sourced from getEngineVersion()/__ENGINE_VERSION__ and equals engine package.json version (D-16)
- Two minor changesets (standards 2.7.0, engine 2.6.0); components untouched
- result.url escaped in plain template
- Karin approves terminal + PDF via D-09 gate (no npm publish in this plan)
</success_criteria>

<output>
Create `.planning/phases/34-klarsprak-plain-language-report/34-05-SUMMARY.md` when done
</output>
