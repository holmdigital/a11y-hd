---
phase: 34-klarsprak-plain-language-report
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - packages/standards/src/index.ts
  - packages/standards/src/index.test.ts
autonomous: true
requirements: [PLAIN-02]
nyquist_compliant: true
wave_0_complete: true

must_haves:
  truths:
    - "generateRegulatoryReport copies plainLanguage explicitly (no spread) and returns it for sv when the rule has text (PLAIN-02)"
    - "generateRegulatoryReport falls back to the EN plainLanguage at enrichment time when the requested language rule lacks the field (D-03)"
    - "The fallback happens in generateRegulatoryReport, never in a renderer (D-03 single-lookup invariant)"
    - "plainLanguage flows into EnrichedReport / --json output additively for every matched report (D-11)"
    - "A rule with no plainLanguage in any language returns plainLanguage: undefined (degrades to remediation.description downstream)"
  artifacts:
    - path: "packages/standards/src/index.ts"
      provides: "generateRegulatoryReport with explicit plainLanguage copy + EN fallback"
      contains: "plainLanguage"
    - path: "packages/standards/src/index.test.ts"
      provides: "PLAIN-02 enrichment + D-03 EN-fallback assertions"
      contains: "plainLanguage enrichment"
  key_links:
    - from: "packages/standards/src/index.ts"
      to: "getConvergenceRule(ruleId, 'en')"
      via: "EN fallback nullish-coalescing"
      pattern: "getConvergenceRule\\(ruleId, 'en'\\)"
    - from: "generateRegulatoryReport"
      to: "RegulatoryReport.plainLanguage"
      via: "explicit field in returned object"
      pattern: "plainLanguage,"
---

<objective>
Wire the plain-language data into the enrichment path: add an explicit `plainLanguage` copy (with the D-03 English fallback) to `generateRegulatoryReport` in `@holmdigital/standards`. Because `enrichResults` in the engine already spreads `{...report}`, this single change makes `plainLanguage` flow to terminal, `--json`, and `--pdf` output with zero engine changes (D-11).

Purpose: This is the single most failure-prone integration point in the phase (RESEARCH Pitfall 1) — the function picks fields explicitly rather than spreading, so a new field that is not manually added is silently dropped. Getting this right means every consumer downstream "just works."
Output: Modified `generateRegulatoryReport`; PLAIN-02 + D-03 enrichment assertions in `index.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-klarsprak-plain-language-report/34-CONTEXT.md
@.planning/phases/34-klarsprak-plain-language-report/34-RESEARCH.md
@.planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md
@.planning/phases/34-klarsprak-plain-language-report/34-01-SUMMARY.md

<interfaces>
<!-- Verified by direct source read 2026-06-11. -->

Current generateRegulatoryReport (index.ts lines 274-289) — explicit-field copy, NO spread. The returned object lists every field by name and ends with `testability: rule.testability,`. After plan 01, ConvergenceRule and RegulatoryReport both carry `plainLanguage?: PlainLanguageCopy`.

getConvergenceRule(ruleId, lang) and getData(lang) (index.ts lines 130-172) — getData('en') always returns the cached EN rules array with no network/fs call, so getConvergenceRule(ruleId, 'en') is a free cache hit. The EN fallback adds no I/O.

enrichResults in the engine (regulatory-scanner.ts lines 318-332) — already does `{ ...report, holmdigitalInsight: {...}, legalContext, failingNodes }`. The spread means plainLanguage flows through with ZERO engine changes once it is on RegulatoryReport. Do NOT touch the engine in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit plainLanguage copy with D-03 EN fallback to generateRegulatoryReport</name>
  <files>packages/standards/src/index.ts, packages/standards/src/index.test.ts</files>
  <read_first>
    - packages/standards/src/index.ts (lines 270-289 generateRegulatoryReport — the exact function to modify; lines 130-172 getData/getConvergenceRule to confirm EN is a cache hit)
    - packages/standards/src/index.test.ts (existing describe structure + the D-10 blocks added in plan 01)
    - .planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md (the verbatim modified-function excerpt)
    - .planning/phases/34-klarsprak-plain-language-report/34-RESEARCH.md (Pitfall 1 — explicit-field copy; Anti-Patterns — no spread)
  </read_first>
  <behavior>
    - generateRegulatoryReport('form-labels', 'sv') returns an object whose plainLanguage.impactLevel === 'stoppar-kop'.
    - generateRegulatoryReport('form-labels', 'de') returns plainLanguage with impactLevel === 'stoppar-kop' (de has no text → EN fallback, D-03).
    - generateRegulatoryReport for a rule with no plainLanguage in any language (e.g. 'focus-order' if present, otherwise any non-PLAIN_ID rule) returns plainLanguage === undefined.
  </behavior>
  <action>
    In generateRegulatoryReport: before the `return {`, compute `const plainLanguage = rule.plainLanguage ?? (lang !== 'en' ? getConvergenceRule(ruleId, 'en')?.plainLanguage : undefined);` (D-03 EN fallback at enrichment time). Add `plainLanguage,` as the LAST field in the returned object, after `testability: rule.testability,`. Do NOT convert the function to a spread (`...rule`) — keep the explicit-field pattern intact per RESEARCH Anti-Patterns. Do NOT add any helper like `getPlainLanguageCopy` or a `RULES_BY_LANG` constant — those are scrapped per D-03.

    In index.test.ts: add a describe block "generateRegulatoryReport plainLanguage enrichment (PLAIN-02)" with three it() blocks per the behavior section: (1) sv returns plainLanguage with impactLevel 'stoppar-kop'; (2) de returns EN-fallback plainLanguage with impactLevel 'stoppar-kop' (D-03); (3) a rule outside the 8-rule set returns plainLanguage undefined. Choose the undefined-case ruleId by confirming against rules.en.json that it has no plainLanguage block (any non-PLAIN_ID rule qualifies).
  </action>
  <verify>
    <automated>npm run test:ci -w @holmdigital/standards</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:ci -w @holmdigital/standards` exits 0 with the PLAIN-02 describe block green.
    - Source assertion: `generateRegulatoryReport` contains the nullish-coalescing fallback expression referencing `getConvergenceRule(ruleId, 'en')` and `lang !== 'en'`.
    - Source assertion: the returned object in `generateRegulatoryReport` ends with `plainLanguage,` after `testability:`; the function body contains no `...rule` spread.
    - Source assertion: no `getPlainLanguageCopy` or `RULES_BY_LANG` identifier exists anywhere in index.ts (scrapped per D-03).
    - Behavior assertion: the D-03 EN-fallback test (`'de'` lang) passes, proving fallback happens at enrichment, not render.
  </acceptance_criteria>
  <done>generateRegulatoryReport returns plainLanguage with EN fallback; PLAIN-02 + D-03 tests green; no scrapped helpers introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| standards data lookup → RegulatoryReport | Internal data-retrieval; ruleId values originate from the scan result, not external user input. No new boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | EN fallback silently returning wrong-language text | mitigate | D-03 fallback is deterministic (only falls back to 'en', never another language); PLAIN-02 test asserts the de→en path returns the exact EN impactLevel |
| T-34-SC | Tampering | npm installs | accept | Zero new dependencies (RESEARCH Package Legitimacy Audit); reuses existing getConvergenceRule cache |
</threat_model>

<verification>
- `npm run test:ci -w @holmdigital/standards` exits 0 (PLAIN-02 + all D-10 guards green)
- `npm run verify -w @holmdigital/standards` exits 0 (lint zero-warning, no `as any`)
</verification>

<success_criteria>
- generateRegulatoryReport copies plainLanguage explicitly with D-03 EN fallback
- plainLanguage flows to EnrichedReport / --json additively (D-11) via the existing engine spread (no engine changes)
- A rule with no text in any language returns plainLanguage undefined
- No scrapped helpers (getPlainLanguageCopy, RULES_BY_LANG) introduced
</success_criteria>

<output>
Create `.planning/phases/34-klarsprak-plain-language-report/34-02-SUMMARY.md` when done
</output>
