---
phase: 34-klarsprak-plain-language-report
plan: 04
type: execute
wave: 2
depends_on: ["34-01", "34-03"]
files_modified:
  - packages/engine/src/reporting/plain-report.ts
  - packages/engine/src/reporting/plain-report.test.ts
  - packages/engine/src/reporting/html-template.test.ts
autonomous: true
requirements: [PLAIN-03]
nyquist_compliant: true
wave_0_complete: true

must_haves:
  truths:
    - "A snapshot baseline of the developer HTML exists BEFORE the audience param lands: html-template.test.ts snapshots generateReportHTML(result, sector) called WITHOUT a third argument, captured in this wave so plan 05's html-template change is guarded against regression (D-13 — no D-10 guard proves D-08's byte-for-byte promise otherwise)"
    - "renderPlainReport(result, lang) renders reports sorted by business impact: stoppar-kop before hindrar before forsamrar before putsning (PLAIN-03)"
    - "Each rendered line shows the five fields in business-first order (what happens / who is affected / what it costs / how to fix) plus a clear-text impact badge"
    - "The renderer reads report.plainLanguage directly and never calls getConvergenceRule or any standards lookup at print time (D-03 single-lookup invariant)"
    - "When a report has no plainLanguage, the renderer falls back to remediation.description without throwing (D-10.4)"
    - "Empty result (0 reports) renders the t('plain.empty_state') chrome, not a crash (D-10.4)"
    - "No compliance score appears in the plain terminal output (D-05); no hardcoded Swedish/English literals in the renderer — all chrome via t() (D-01)"
  artifacts:
    - path: "packages/engine/src/reporting/html-template.test.ts"
      provides: "D-13 developer-PDF snapshot baseline (two-arg generateReportHTML call)"
      contains: "toMatchSnapshot"
    - path: "packages/engine/src/reporting/plain-report.ts"
      provides: "renderPlainReport(result, lang) terminal renderer"
      exports: ["renderPlainReport"]
      min_lines: 40
    - path: "packages/engine/src/reporting/plain-report.test.ts"
      provides: "D-10.4 renderer structure tests (sort, badge, fallback, empty-state)"
      contains: "renderPlainReport"
  key_links:
    - from: "packages/engine/src/reporting/html-template.test.ts"
      to: "generateReportHTML(result, sector)"
      via: "two-arg snapshot call (D-13 — no third argument)"
      pattern: "generateReportHTML\\("
    - from: "packages/engine/src/reporting/plain-report.ts"
      to: "report.plainLanguage"
      via: "direct property read (no secondary lookup)"
      pattern: "\\.plainLanguage"
    - from: "packages/engine/src/reporting/plain-report.ts"
      to: "t('plain.*')"
      via: "i18n chrome strings"
      pattern: "t\\('plain\\."
---

<objective>
Build the terminal renderer `packages/engine/src/reporting/plain-report.ts` and its D-10.4 structure tests, AND capture the D-13 developer-PDF snapshot baseline. The snapshot test on `generateReportHTML(result, sector)` (two args, no `audience`) is captured in THIS wave — before plan 05 (wave 3) adds the `audience` parameter — so the byte-for-byte D-08 promise is mechanically guarded exactly where the regression could sneak in.

`renderPlainReport(result, lang)` reads `report.plainLanguage` directly (set at enrichment by plan 02), sorts by business impact, renders five business-first fields plus a clear-text chalk-colored badge per row, and uses `t('plain.*')` for ALL chrome (no hardcoded strings, D-01). No compliance score (D-05).

This plan is wave 2: it depends on plan 01 (the PlainLanguageCopy / BusinessImpactLevel types) and plan 03 (the plain.* locale keys). The data path (plan 02) lands in parallel; the renderer reads the field shape, which plan 01 guarantees.

Purpose: This is the user-facing payload of the phase — the report a non-technical recipient actually reads — plus the regression lock that lets plan 05 modify html-template.ts safely.
Output: `html-template.test.ts` (new — D-13 snapshot baseline); `plain-report.ts` (new renderer); `plain-report.test.ts` (new — Wave 0 gap closed: sort order, badge mapping, fallback, empty-state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-klarsprak-plain-language-report/34-CONTEXT.md
@.planning/phases/34-klarsprak-plain-language-report/34-RESEARCH.md
@.planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md
@.planning/phases/34-klarsprak-plain-language-report/daniel-engine-klarsprak-kontextgranskning-2026-06-11.md
@.planning/phases/34-klarsprak-plain-language-report/34-01-SUMMARY.md
@.planning/phases/34-klarsprak-plain-language-report/34-03-SUMMARY.md
@packages/engine/src/reporting/junit-generator.ts
@packages/engine/src/reporting/junit-generator.test.ts

<interfaces>
<!-- Verified by direct source read 2026-06-11. -->

ScanResult and getEngineVersion export from '../core/regulatory-scanner'. EnrichedReport and the new BusinessImpactLevel / PlainLanguageCopy export from '@holmdigital/standards' (after plan 01). chalk default-imports from 'chalk' (engine dep ^5.3.0). t() and setLanguage() import from '../i18n'.

generateReportHTML(result, sector) — current two-arg signature at html-template.ts line 27. Its output is deterministic given: a fixed fixture (fixed url, timestamp, stats, score), setLanguage('en') (formatDate uses LOCALE_TO_INTL[getCurrentLang()]), and the version string. getEngineVersion() returns the build-time `__ENGINE_VERSION__` global, injected from packages/engine/package.json by BOTH tsup.config.ts:13 (build) and vitest.config.ts:8 (tests) — regulatory-scanner.test.ts:52-56 already asserts it equals engine pkg.version. For a version-bump-proof snapshot, normalize the version out: `html.replaceAll(getEngineVersion(), '__VERSION__')` before toMatchSnapshot().

Each EnrichedReport carries: ruleId, holmdigitalInsight.diggRisk ('low'|'medium'|'high'|'critical'), remediation.description, and (when matched) plainLanguage?: { headline, whatHappens, whoIsAffected, businessImpact, howToFix, impactLevel }.

Impact sort order (rank): stoppar-kop=0, hindrar=1, forsamrar=2, putsning=3.
Derivation fallback for rules WITHOUT explicit impactLevel (future rules; the 8 here are explicit): critical→stoppar-kop, high→hindrar, medium→forsamrar, low→putsning. levelOf(r) = r.plainLanguage?.impactLevel ?? RISK_TO_IMPACT[r.holmdigitalInsight.diggRisk] ?? 'putsning'.
Badge chalk mapping: stoppar-kop→chalk.red.bold, hindrar→chalk.red, forsamrar→chalk.yellow, putsning→chalk.gray.

Badge clear-text comes from t() keys authored in plan 03: t('plain.badge_stoppar_kop') etc. — map BusinessImpactLevel to the badge key, never print "level A/AA".

Chrome key reference (from plan 03 en.json/sv.json): plain.what_happens, plain.who_is_affected, plain.business_impact, plain.how_to_fix, plain.intro_framing, plain.intro_found ({count} {unit}), plain.intro_unit_singular, plain.intro_unit_plural, plain.sorted_by, plain.closing, plain.empty_state.

junit-generator.test.ts (read it) — the canonical mockResult fixture and the `as unknown as ScanResult['reports'][number]` widening pattern. Reuse both verbatim. NEVER `as any` (PUB-09).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the D-13 developer-PDF snapshot baseline (two-arg generateReportHTML, BEFORE the audience param exists)</name>
  <files>packages/engine/src/reporting/html-template.test.ts</files>
  <read_first>
    - packages/engine/src/reporting/html-template.ts (current two-arg generateReportHTML — the function being baselined; formatDate/getCurrentLang determinism)
    - packages/engine/src/reporting/junit-generator.test.ts (mockResult fixture shape + partial-report widening pattern to reuse)
    - packages/engine/src/core/regulatory-scanner.test.ts (lines 52-56 — the existing getEngineVersion()/pkg.version assertion pattern)
    - packages/engine/vitest.config.ts (confirms __ENGINE_VERSION__ define is active under vitest)
    - .planning/phases/34-klarsprak-plain-language-report/daniel-engine-klarsprak-kontextgranskning-2026-06-11.md (D-13 rationale — point 1 under "Att lägga till")
  </read_first>
  <action>
    Create packages/engine/src/reporting/html-template.test.ts (new file — no html-template test exists today). Import { describe, it, expect, beforeAll } from 'vitest', { generateReportHTML } from './html-template', { ScanResult, getEngineVersion } from '../core/regulatory-scanner', { setLanguage } from '../i18n', axeCore from 'axe-core'.

    Determinism setup: beforeAll calls setLanguage('en') (formatDate depends on getCurrentLang()). Build TWO fixtures copying junit-generator.test.ts's mockResult shape verbatim (fixed url 'https://example.com', fixed timestamp '2026-02-08T01:51:29Z', fixed stats/score/complianceStatus): (a) an empty-reports result, and (b) a result with one report fixture (use the junit-generator.test.ts failure-report shape with `as unknown as ScanResult['reports'][number]` widening — NOT `as any`).

    D-13 snapshot tests — describe block "generateReportHTML developer baseline (D-13)": two it() blocks, each calling generateReportHTML with exactly TWO arguments — `generateReportHTML(fixture, 'public')` — never a third argument. Normalize the version out of the HTML before snapshotting so future version bumps do not invalidate the baseline: `const normalized = html.replaceAll(getEngineVersion(), '__VERSION__');` then `expect(normalized).toMatchSnapshot();`. Run the test once to write the baseline snapshot file (`__snapshots__/html-template.test.ts.snap`) and commit BOTH the test and the snapshot — the committed snapshot IS the regression lock plan 05 asserts against.
  </action>
  <verify>
    <automated>npm run test:ci -w @holmdigital/engine</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:ci -w @holmdigital/engine` exits 0 with the D-13 describe block green (baseline written on first run, matched on re-run).
    - Source assertion: html-template.test.ts contains `toMatchSnapshot` and every generateReportHTML call site passes exactly two arguments (zero occurrences of a three-arg call) — D-13's "UTAN tredje argument".
    - Source assertion: the snapshot file `packages/engine/src/reporting/__snapshots__/html-template.test.ts.snap` exists on disk after the run.
    - Source assertion: the test normalizes getEngineVersion() to a stable token before snapshotting (version-bump-proof), and beforeAll calls setLanguage('en') (locale-deterministic).
    - Source assertion: fixtures use `as unknown as ScanResult['reports'][number]`; zero occurrences of `as any` in the file.
  </acceptance_criteria>
  <done>D-13 baseline locked: html-template.test.ts + committed snapshot capture the developer HTML via two-arg calls, in wave 2, before plan 05 touches html-template.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write plain-report.test.ts covering sort order, badge mapping, fallback, and empty-state (Wave 0 gap)</name>
  <files>packages/engine/src/reporting/plain-report.test.ts</files>
  <read_first>
    - packages/engine/src/reporting/junit-generator.test.ts (mockResult fixture, partial-report widening pattern, spy/assert style)
    - .planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md (plain-report.test.ts analog excerpts, fixture shape with plainLanguage)
    - .planning/phases/34-klarsprak-plain-language-report/34-RESEARCH.md (Validation Architecture — the 4 D-10.4 behaviors)
    - .planning/phases/34-klarsprak-plain-language-report/34-03-SUMMARY.md (the exact plain.* key names that landed, so the empty-state assertion targets the right key)
  </read_first>
  <behavior>
    - Sort: given reports with impactLevel putsning, stoppar-kop, forsamrar, the rendered output order is stoppar-kop first, putsning last (assert via capturing console.log output and checking the order the headlines/ruleIds appear).
    - Badge mapping: a stoppar-kop report produces output that went through chalk.red.bold; assert the badge clear-text from t('plain.badge_stoppar_kop') appears in output.
    - Fallback: a report with plainLanguage undefined does not throw and the rendered body includes its remediation.description text.
    - Empty-state: result.reports = [] renders the t('plain.empty_state') string and does not print any per-row content.
  </behavior>
  <action>
    Create plain-report.test.ts. Import { describe, it, expect, vi } from 'vitest', { renderPlainReport } from './plain-report', { ScanResult, getEngineVersion } from '../core/regulatory-scanner', axeCore from 'axe-core'. Build the mockResult fixture by copying junit-generator.test.ts's shape verbatim (url, timestamp, metadata, reports, stats, score, complianceStatus). Construct report fixtures with the plainLanguage block, widening each with `as unknown as ScanResult['reports'][number]` (NOT `as any`).

    Spy on console.log with `vi.spyOn(console, 'log').mockImplementation(() => {})`, call renderPlainReport, then assert against the captured `mock.calls` joined into one string. Restore the spy after each test. Write four it() blocks per the behavior section. Since the renderer uses t(), call setLanguage('sv') or setLanguage('en') in the test as needed (import setLanguage from '../i18n') so the badge/empty-state assertions match the expected language's chrome. These tests will be RED until Task 3 creates the renderer — that is the intended TDD order.
  </action>
  <verify>
    <automated>npm run test:ci -w @holmdigital/engine</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: plain-report.test.ts exists and contains four it() blocks covering sort order, badge mapping, fallback (remediation.description), and empty-state.
    - Source assertion: report fixtures use `as unknown as ScanResult['reports'][number]`; zero occurrences of `as any` in the file.
    - Behavior assertion: before Task 3, `npm run test:ci -w @holmdigital/engine` reports the plain-report tests FAILING (module not found / function undefined) — confirms RED state.
  </acceptance_criteria>
  <done>plain-report.test.ts exists with 4 structure tests in the RED state, PUB-09-compliant fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Implement renderPlainReport with impact sort, business-first fields, chalk badges, t() chrome, and fallbacks</name>
  <files>packages/engine/src/reporting/plain-report.ts</files>
  <read_first>
    - packages/engine/src/reporting/junit-generator.ts (reporter-reading-ScanResult role analog, import style)
    - packages/engine/src/cli/index.ts (lines 220-239 the light-output chalk block — chalk usage pattern; line 134 t() usage)
    - packages/engine/src/i18n/index.ts (t() signature + interpolation)
    - .planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md (renderPlainReport core pattern: IMPACT_ORDER, RISK_TO_IMPACT, BADGE_CHALK, sort)
    - .planning/phases/34-klarsprak-plain-language-report/34-CONTEXT.md (D-01 no hardcoded strings, D-05 no score, badge clear-text rule)
  </read_first>
  <action>
    Create plain-report.ts exporting `export function renderPlainReport(result: ScanResult, lang: string = 'en'): void`. Import chalk, t (from '../i18n'), ScanResult (from '../core/regulatory-scanner'), and types BusinessImpactLevel + EnrichedReport (from '@holmdigital/standards'). Define module-scope const maps: IMPACT_ORDER (stoppar-kop:0, hindrar:1, forsamrar:2, putsning:3), RISK_TO_IMPACT (critical→stoppar-kop, high→hindrar, medium→forsamrar, low→putsning) as the future-rule derivation fallback, BADGE_CHALK (stoppar-kop→chalk.red.bold, hindrar→chalk.red, forsamrar→chalk.yellow, putsning→chalk.gray), and a BADGE_KEY map from BusinessImpactLevel to the t() key string ('plain.badge_stoppar_kop' etc.).

    Behavior: if result.reports.length === 0, print t('plain.empty_state') and return. Otherwise: print the opening (t('plain.intro_framing'), then t('plain.intro_found', { count, unit }) where unit is t('plain.intro_unit_singular'|'plural') by count, then t('plain.sorted_by')). Compute levelOf(r) = r.plainLanguage?.impactLevel ?? RISK_TO_IMPACT[r.holmdigitalInsight.diggRisk] ?? 'putsning'. Sort a copy of result.reports ascending by IMPACT_ORDER[levelOf(r)]. For each report, print a numbered row: the badge via BADGE_CHALK[level](t(BADGE_KEY[level])), then the four labeled fields IN THIS ORDER using t() labels — t('plain.what_happens') / t('plain.who_is_affected') / t('plain.business_impact') / t('plain.how_to_fix') — with the values from report.plainLanguage (whatHappens/whoIsAffected/businessImpact/howToFix) when present, else fall back to report.remediation.description for the body (D-10.4 fallback). Finish with t('plain.closing').

    Do NOT print result.score or any score number (D-05). Do NOT call getConvergenceRule or any standards function (D-03 — read report.plainLanguage directly). Do NOT put any Swedish or English string literal in the file — every human-readable string is a t() call (D-01). No `as any`. Keep the renderer void + console.log (matches the light-output and the test's console spy).
  </action>
  <verify>
    <automated>npm run test:ci -w @holmdigital/engine</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:ci -w @holmdigital/engine` exits 0 with all four plain-report structure tests GREEN (and the D-13 snapshot from Task 1 still green).
    - Source assertion: plain-report.ts exports renderPlainReport and contains the BADGE_CHALK map with chalk.red.bold for stoppar-kop.
    - Source assertion: plain-report.ts contains `.plainLanguage` direct reads and ZERO occurrences of `getConvergenceRule` (D-03 invariant).
    - Source assertion: zero string literals containing Swedish characters (å/ä/ö) or hardcoded English field labels in plain-report.ts — every label/badge/opening is a `t('plain.*')` call (D-01).
    - Source assertion: no `result.score` reference and no `as any` in plain-report.ts (D-05, PUB-09).
    - Behavior assertion: the sort test confirms stoppar-kop renders before putsning; the empty-state test confirms t('plain.empty_state') output for 0 reports.
  </acceptance_criteria>
  <done>renderPlainReport implemented; 4 structure tests green; no score, no hardcoded strings, no secondary lookup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanned-page content → chalk terminal output | report.plainLanguage is internal authored data; remediation.description (fallback) is internal; but failingNodes content (if ever rendered) could carry scanned-page HTML. This renderer prints only plainLanguage/remediation text + t() chrome — no raw page HTML — so no escape-sequence injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | Tampering | terminal escape-sequence injection via scanned-page content flowing into chalk | mitigate | renderer prints only internal authored text (plainLanguage / remediation.description) and t() chrome; it does NOT print failingNodes/raw page HTML — acceptance criterion forbids raw-node rendering implicitly (only the 5 plainLanguage fields + remediation fallback are printed) |
| T-34-07 | Information disclosure | score leaking into plain report | mitigate | D-05 forbids score; acceptance criterion asserts no `result.score` reference in plain-report.ts |
| T-34-12 | Tampering | silent developer-PDF regression when plan 05 adds the audience param | mitigate | D-13 snapshot baseline captured here (wave 2, two-arg call) and committed; plan 05 asserts it unchanged without snapshot update |
| T-34-SC | Tampering | npm installs | accept | Zero new dependencies; chalk already in engine deps (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run test:ci -w @holmdigital/engine` exits 0 (D-13 snapshot baseline + 4 plain-report structure tests green)
- `npm run verify -w @holmdigital/engine` exits 0 (lint zero-warning, no `as any`, typecheck)
- Snapshot file `packages/engine/src/reporting/__snapshots__/html-template.test.ts.snap` exists and is committed
</verification>

<success_criteria>
- D-13 developer-HTML snapshot baseline captured via two-arg generateReportHTML calls, in this wave (before plan 05 touches html-template.ts)
- renderPlainReport sorts by business impact, renders 5 business-first fields + clear-text badge
- Reads report.plainLanguage directly; no standards lookup at print time
- Fallback to remediation.description; empty-state handled
- No score (D-05); all chrome via t() (D-01); no `as any`
</success_criteria>

<output>
Create `.planning/phases/34-klarsprak-plain-language-report/34-04-SUMMARY.md` when done
</output>
