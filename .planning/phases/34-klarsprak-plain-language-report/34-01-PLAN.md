---
phase: 34-klarsprak-plain-language-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/standards/src/types.ts
  - packages/standards/src/index.ts
  - packages/standards/data/rules.sv.json
  - packages/standards/data/rules.en.json
  - packages/standards/src/index.test.ts
autonomous: true
requirements: [PLAIN-01, PLAIN-06]
nyquist_compliant: true
wave_0_complete: true

must_haves:
  truths:
    - "PlainLanguageCopy interface and BusinessImpactLevel type are exported from @holmdigital/standards (D-01 keys, English-keyed)"
    - "BOTH existing types are explicitly extended: ConvergenceRule AND RegulatoryReport each carry plainLanguage?: PlainLanguageCopy — not just the new interface created (D-15; enrichment does not compile otherwise)"
    - "All 8 target ruleIds carry a plainLanguage block with the exact D-04 impactLevel in BOTH rules.sv.json AND rules.en.json (D-02, PLAIN-06)"
    - "Swedish texts contain correct a-ring/a-umlaut/o-umlaut and zero mojibake (D-10.1)"
    - "No plainLanguage text in either language contains an em dash, en dash, or percent sign (D-10.2)"
    - "The same 8 ruleIds have identical impactLevel values across both files (D-10.3)"
  artifacts:
    - path: "packages/standards/src/types.ts"
      provides: "PlainLanguageCopy interface, BusinessImpactLevel type, plainLanguage? on ConvergenceRule and RegulatoryReport (D-15)"
      contains: "export interface PlainLanguageCopy"
    - path: "packages/standards/data/rules.sv.json"
      provides: "8 Swedish plainLanguage blocks with explicit impactLevel"
      contains: "\"plainLanguage\""
    - path: "packages/standards/data/rules.en.json"
      provides: "8 English plainLanguage blocks with explicit impactLevel"
      contains: "\"plainLanguage\""
    - path: "packages/standards/src/index.test.ts"
      provides: "D-10.1 encoding guard, D-10.2 tone lint, D-10.3 sv/en parity"
      contains: "plainLanguage encoding guard"
  key_links:
    - from: "packages/standards/src/index.ts"
      to: "PlainLanguageCopy"
      via: "export type re-export block"
      pattern: "PlainLanguageCopy"
    - from: "packages/standards/src/types.ts"
      to: "ConvergenceRule.plainLanguage"
      via: "optional field referencing PlainLanguageCopy (D-15)"
      pattern: "plainLanguage\\?: PlainLanguageCopy"
    - from: "packages/standards/src/types.ts"
      to: "RegulatoryReport.plainLanguage"
      via: "optional field referencing PlainLanguageCopy (D-15)"
      pattern: "plainLanguage\\?: PlainLanguageCopy"
---

<objective>
Establish the data foundation for the plain-language report: the `PlainLanguageCopy` interface and `BusinessImpactLevel` type in `@holmdigital/standards`, the optional `plainLanguage` field on BOTH existing types `ConvergenceRule` and `RegulatoryReport` (D-15 — explicit extension of both, or enrichment will not compile), the 8 Swedish AND 8 English plain-language texts in the rules JSON files (PLAIN-06 extended per D-02), and the three mechanical data guards (D-10.1 encoding, D-10.2 tone, D-10.3 sv/en parity).

Purpose: Every downstream plan (enrichment, renderer, CLI, PDF) consumes these types and this data. This plan is the contract layer — types and data land first, fully tested, so the render layer has a stable shape to read.
Output: New public API in standards (`PlainLanguageCopy`, `BusinessImpactLevel`); `plainLanguage?` on two existing types; 16 plainLanguage blocks (8 sv + 8 en); 3 new vitest describe blocks in `index.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-klarsprak-plain-language-report/34-CONTEXT.md
@.planning/phases/34-klarsprak-plain-language-report/34-RESEARCH.md
@.planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md
@.planning/phases/34-klarsprak-plain-language-report/klarsprakslager-engine.md
@.planning/phases/34-klarsprak-plain-language-report/daniel-engine-klarsprak-kontextgranskning-2026-06-11.md

<interfaces>
<!-- Verified by direct source read 2026-06-11; re-confirmed by KarinsTeam kontextgranskning (same date): impactLevel/plainLanguage have 0 hits in code — everything is additive. -->

Current standards/src/types.ts (lines 5-9) — union-type pattern, insert BusinessImpactLevel after DiggRisk:
```
export type WCAGLevel = 'A' | 'AA' | 'AAA';
export type DiggRisk = 'low' | 'medium' | 'high' | 'critical';
export type EAAImpact = 'none' | 'low' | 'medium' | 'high' | 'critical';
```

Current ConvergenceRule (types.ts lines 20-37) ends with the optional `legalContext?: LegalContext;` field — the new `plainLanguage?` field follows the same optional-for-backward-compat pattern.

Current generateRegulatoryReport returned-object shape (index.ts lines 278-288) — the RegulatoryReport interface mirrors these fields and ends with `testability: Testability;`.

Current type re-export block (index.ts lines 103-128) — `export type { ConvergenceRule, ..., RegulatoryReport, ..., DiggRisk, EAAImpact, ... };`. Add the two new type names here.

D-15 (kontextgranskning): the field must be added to BOTH `ConvergenceRule` AND `RegulatoryReport`. The review calls this "trivialt i koden, lätt att missa i planen" — it is the compile gate for plan 02's enrichment.

The D-04 impactLevel table (canonical — these values are editorial, NOT language-specific, and MUST be byte-identical in sv and en; kontextgranskning confirmed derivation from actual diggRisk values gives the wrong level for 6 of 8):
| ruleId | impactLevel |
| form-labels | stoppar-kop |
| alt-text | hindrar |
| name-role-value | hindrar |
| keyboard-accessible | hindrar |
| color-contrast | forsamrar |
| link-purpose | forsamrar |
| heading-order | putsning |
| language-of-page | putsning |
</interfaces>

<source_texts>
<!-- The 8 Swedish source texts live in klarsprakslager-engine.md (read it). Map editorial sections to ruleIds and English-keyed fields as follows. The Swedish wording is the source of truth for sv; the headline is at Claude's discretion within tone rules (the source uses section titles as de-facto headlines). -->

Editorial-section → ruleId mapping (semantic ids confirmed present in both JSON files):
- "Produktbilderna saknar beskrivning" → alt-text (NOT image-alt — that axe raw id does not exist in the data, per kontextgranskning)
- "Knapptexten är för svag mot bakgrunden" → color-contrast
- "Fälten i kassan saknar tydliga etiketter" → form-labels
- "Länkar som bara säger läs mer" → link-purpose
- "Ikonknappar utan namn" → name-role-value
- "Sajten går inte att använda utan mus" → keyboard-accessible
- "Rubrikerna hänger inte ihop" → heading-order
- "Sidan säger inte vilket språk den är på" → language-of-page

English-keyed field mapping (the Swedish-keyed PlainCopy in the source doc is SCRAPPED — use English keys):
- headline   ← the editorial section title, reworded as a customer-facing headline
- whatHappens ← "Vad händer"
- whoIsAffected ← "Vem påverkas"
- businessImpact ← "Affärsrisk"
- howToFix ← "Så fixar du"
- impactLevel ← the D-04 table value (NOT the source doc's "Allvarlighet" line — D-04 overrides for 6 of 8)
</source_texts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PlainLanguageCopy interface, BusinessImpactLevel type, plainLanguage? on BOTH existing types (D-15) + Wave 0 data guards</name>
  <files>packages/standards/src/types.ts, packages/standards/src/index.ts, packages/standards/src/index.test.ts</files>
  <read_first>
    - packages/standards/src/types.ts (current type definitions — see exact insertion points)
    - packages/standards/src/index.ts (lines 103-128 type re-export block; lines 270-289 generateRegulatoryReport — DO NOT modify the function in this plan, only the export block)
    - packages/standards/src/index.test.ts (existing describe-block structure, import style, AJV usage)
    - packages/standards/data/rules.sv.json (confirm color-contrast rule object shape before editing)
    - .planning/phases/34-klarsprak-plain-language-report/34-PATTERNS.md (exact type/test code excerpts)
  </read_first>
  <behavior>
    - D-10.1 encoding guard: for each of the 8 PLAIN_IDS, every string field in rulesSv plainLanguage matches /Ã/ === false (no mojibake). Test FAILS before data lands, passes after.
    - D-10.2 tone lint: for both sv and en, every plainLanguage string field contains no em dash (U+2014), no en dash (U+2013), no percent sign.
    - D-10.3 parity: all 8 PLAIN_IDS have plainLanguage defined in both rulesSv and rulesEn, with identical impactLevel per ruleId.
  </behavior>
  <action>
    In types.ts: add `export type BusinessImpactLevel = 'stoppar-kop' | 'hindrar' | 'forsamrar' | 'putsning';` immediately after the `DiggRisk` type (line 7). Add `export interface PlainLanguageCopy` with exactly these required fields (all string except the last): `headline`, `whatHappens`, `whoIsAffected`, `businessImpact`, `howToFix`, and `impactLevel: BusinessImpactLevel`. Note: the source reference doc marks `impactLevel?` optional, but for these 8 explicit rules it is REQUIRED in the interface per D-04 (the derivation fallback for future rules lives in the engine renderer, not in this type).

    D-15 — explicit type extension of BOTH existing types (the kontextgranskning flags this as easy to miss; enrichment in plan 02 will not compile without it): add `plainLanguage?: PlainLanguageCopy;` (1) to `ConvergenceRule` after `legalContext?: LegalContext;`, AND (2) to `RegulatoryReport` after `testability: Testability;`. Two separate edits, two separate type declarations — do not stop after the first. Use `interface` for PlainLanguageCopy and `type` for BusinessImpactLevel per CLAUDE.md convention.

    In index.ts: add `PlainLanguageCopy` and `BusinessImpactLevel` to the existing `export type { ... }` re-export block (lines 103-128). Do NOT modify `generateRegulatoryReport` here — that is plan 02's responsibility.

    In index.test.ts: prepend `import rulesSv from '../data/rules.sv.json';` and `import rulesEn from '../data/rules.en.json';` and add `import type { ConvergenceRule } from './types';` if not already imported. Append three describe blocks: "plainLanguage encoding guard (D-10.1)", "plainLanguage tone lint (D-10.2)", "plainLanguage sv/en parity (D-10.3)". Use the exact MOJIBAKE `/Ã/`, DASH `/[—–]/`, PERCENT `/%/` regexes and the PLAIN_IDS array from PATTERNS.md. Cast JSON imports as `ConvergenceRule[]` (NOT `as any[]` — PUB-09 zero-as-any rule). These tests will fail until Task 2 lands the data — that is the intended RED state for this task's TDD pairing with Task 2.
  </action>
  <verify>
    <automated>npm run typecheck -w @holmdigital/standards</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck -w @holmdigital/standards` exits 0 (interface/type compile; optional fields accepted).
    - Source assertion: `packages/standards/src/types.ts` contains `export interface PlainLanguageCopy` and `export type BusinessImpactLevel = 'stoppar-kop' | 'hindrar' | 'forsamrar' | 'putsning';`.
    - Source assertion (D-15, ConvergenceRule): the `ConvergenceRule` interface body in types.ts contains `plainLanguage?: PlainLanguageCopy;` (located after `legalContext?: LegalContext;`).
    - Source assertion (D-15, RegulatoryReport): the `RegulatoryReport` interface body in types.ts contains `plainLanguage?: PlainLanguageCopy;` (located after `testability: Testability;`) — BOTH existing types are extended, verified independently of each other.
    - Source assertion: `packages/standards/src/index.ts` export block contains `PlainLanguageCopy` and `BusinessImpactLevel`.
    - Source assertion: `index.test.ts` contains describe blocks titled with `D-10.1`, `D-10.2`, and `D-10.3`, and casts JSON imports as `ConvergenceRule[]` with zero occurrences of `as any`.
    - Behavior assertion: running `npm run test:ci -w @holmdigital/standards` shows the three new D-10 describe blocks FAILING (data not yet present) — confirms guards are wired before data lands.
  </acceptance_criteria>
  <done>Types compile; BOTH ConvergenceRule and RegulatoryReport carry plainLanguage? (D-15); export block updated; three data-guard describe blocks exist and are RED pending Task 2 data.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 8 Swedish + 8 English plainLanguage blocks with D-04 impactLevel</name>
  <files>packages/standards/data/rules.sv.json, packages/standards/data/rules.en.json</files>
  <read_first>
    - .planning/phases/34-klarsprak-plain-language-report/klarsprakslager-engine.md (the 8 Swedish source texts, tone rules, the editorial Allvarlighet per text)
    - packages/standards/data/rules.sv.json (locate each of the 8 target ruleId objects; observe field ordering)
    - packages/standards/data/rules.en.json (locate the same 8 ruleId objects)
    - .planning/phases/34-klarsprak-plain-language-report/34-CONTEXT.md (D-04 table, D-02 English tone rules, deferred-ideas list)
  </read_first>
  <action>
    Using the Write/Edit tool (NEVER Bash heredoc — Pitfall 4 mojibake risk), add a `plainLanguage` block to each of the 8 target ruleId objects in rules.sv.json with English keys (`headline`, `whatHappens`, `whoIsAffected`, `businessImpact`, `howToFix`, `impactLevel`). The Swedish wording comes from klarsprakslager-engine.md mapped via the source_texts section in context: whatHappens←"Vad händer", whoIsAffected←"Vem påverkas", businessImpact←"Affärsrisk", howToFix←"Så fixar du". Set `impactLevel` to the EXACT D-04 value per ruleId: form-labels=stoppar-kop, alt-text=hindrar, name-role-value=hindrar, keyboard-accessible=hindrar, color-contrast=forsamrar, link-purpose=forsamrar, heading-order=putsning, language-of-page=putsning. The source doc's "Allvarlighet" lines are OVERRIDDEN by D-04 (they disagree for 6 of 8 — D-04 wins; kontextgranskning confirmed against actual diggRisk data).

    Then write the 8 English plainLanguage blocks in rules.en.json for the same 8 ruleIds. English follows the same tone rules per D-02: you-address ("your customer"), business-impact-before-technology, NO em/en dashes, NO percent signs, no blame, no invented statistics. The impactLevel MUST be byte-identical to the sv value for each ruleId (D-10.3 parity — these are editorial, not language-specific).

    Swedish text MUST use raw UTF-8 å/ä/ö (e.g. "Försämrar", "påverkas", "köp") — never mojibake. Do not use em dashes or en dashes in either language; replace with commas, periods, or separate sentences. Do not use the word "image-alt" anywhere — the semantic id is `alt-text`. Do NOT add texts for any of the 7 other languages (de/fr/es/nl/fi/dk/no) — those are explicitly deferred in CONTEXT.md.
  </action>
  <verify>
    <automated>npm run test:ci -w @holmdigital/standards</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:ci -w @holmdigital/standards` exits 0 with the D-10.1, D-10.2, and D-10.3 describe blocks all GREEN.
    - Behavior assertion (D-10.1): zero matches of `/Ã/` in any of the 8 sv plainLanguage string fields.
    - Behavior assertion (D-10.2): zero matches of `/[—–]/` and zero `%` in any sv OR en plainLanguage field.
    - Behavior assertion (D-10.3): all 8 PLAIN_IDS have plainLanguage in both files with matching impactLevel.
    - Source assertion: `rules.sv.json` form-labels plainLanguage.impactLevel === "stoppar-kop"; alt-text === "hindrar"; heading-order === "putsning" (spot-check the D-04 corrections).
    - Source assertion: a Swedish text field contains a literal å/ä/ö character (e.g. the color-contrast or language-of-page block) — proves encoding survived the write.
    - Source assertion: the 7 non-sv/en rules.*.json files are unchanged (no plainLanguage added — deferred).
  </acceptance_criteria>
  <done>16 plainLanguage blocks (8 sv + 8 en) present with correct D-04 impactLevel; all three D-10 data guards green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editorial text → JSON data files | Hand-authored Swedish/English text written into rules.*.json; risk is encoding corruption, not injection (data is static, not user-supplied) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | plainLanguage text fields (mojibake / wrong-encoding corruption) | mitigate | D-10.1 encoding guard (vitest /Ã/ assertion) blocks commit if any text is mis-encoded; Write tool used, never Bash heredoc |
| T-34-02 | Information disclosure | plainLanguage text (invented statistics misleading customers) | mitigate | D-10.2 tone lint forbids `%`; texts are qualitative or source-backed per honesty rule |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies in this phase (RESEARCH Package Legitimacy Audit confirms none); nothing to install |
</threat_model>

<verification>
- `npm run typecheck -w @holmdigital/standards` exits 0
- `npm run test:ci -w @holmdigital/standards` exits 0 with D-10.1/D-10.2/D-10.3 green
- `npm run verify -w @holmdigital/standards` exits 0 (full chain: build + lint + typecheck + exports + tests; lint must stay zero-warning, no `as any`)
</verification>

<success_criteria>
- PlainLanguageCopy interface + BusinessImpactLevel type exported from @holmdigital/standards
- plainLanguage? optional on BOTH ConvergenceRule AND RegulatoryReport — two independent source assertions (D-15)
- 8 sv + 8 en plainLanguage blocks present with exact D-04 impactLevel values
- D-10.1, D-10.2, D-10.3 guards green
- No new npm dependencies; lint zero-warning; no `as any`
</success_criteria>

<output>
Create `.planning/phases/34-klarsprak-plain-language-report/34-01-SUMMARY.md` when done
</output>
