---
phase: 32-tc-15-remaining-component-test-coverage
plan: 08
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-03, 32-04, 32-05, 32-06, 32-07]
files_modified:
  - packages/components/src/Tooltip/Tooltip.test.tsx
  - packages/components/package.json
  - packages/components/CHANGELOG.md
autonomous: false
requirements: [TC-15]
must_haves:
  truths:
    - "Tooltip.test.tsx is audited against the TC-15 contract; gaps (Tier 1 describe missing, no axe smoke, fireEvent D-02a violation) are closed via APPENDED tests OR documented as accepted under a Tooltip-specific waiver"
    - "All 8 TC-15 component test files (Card, Skeleton, Heading, SkipLink, ProgressBar, Switch, Pagination, Tooltip) pass the Phase 22 template-setter pillars: WCAG-SC JSDoc header, Tier 1 describe, Tier 2 describe, ≥1 expectNoAxeViolations, D-02a anti-pattern grep clean"
    - "Total components-package *.test.tsx file count is 29 (was 22; +7 new files: Card, Skeleton, Heading, SkipLink, ProgressBar, Switch, Pagination). Tooltip.test.tsx existed pre-phase and is augmented in place"
    - "test:wcag-headers script reports 29 test files all carrying the marker (was 22; ROADMAP success #5's '24 → 32' arithmetic was off — actual baseline is 22 and actual target is 29; recorded in summary)"
    - "ROADMAP success criterion #1 ('8 new test files') is interpreted as 7 new + 1 augmented; recorded in summary with rationale"
    - "npm run verify -w @holmdigital/components exits 0 end-to-end (build + check:exports + check:types + test:ci including check:no-tailwind-leak + check:no-test-leak + test:wcag-headers)"
    - "packages/components/package.json version bumped 2.5.0 → 2.6.0 (MINOR — Phase 22/24/30/31 precedent: every test-coverage milestone in this project's history has been MINOR; rationale: published exports remain byte-equivalent, but a CHANGELOG entry per release is the project norm and MINOR keeps the dependent-publisher signal honest)"
    - "CHANGELOG.md gains a `## 2.6.0` entry describing TC-15 closure"
  artifacts:
    - path: "packages/components/src/Tooltip/Tooltip.test.tsx"
      provides: "Tier 1+2 contract proof for Tooltip"
      contains: "Tier 1: Table Stakes"
      min_lines: 165
    - path: "packages/components/package.json"
      provides: "Components package version 2.6.0"
      contains: "\"version\": \"2.6.0\""
    - path: "packages/components/CHANGELOG.md"
      provides: "TC-15 release note"
      contains: "## 2.6.0"
  key_links:
    - from: "packages/components/CHANGELOG.md"
      to: "packages/components/package.json"
      via: "shared version string"
      pattern: "2\\.6\\.0"
---

<objective>
Close TC-15. Audit the pre-existing Tooltip.test.tsx (165 lines, rich SC 1.4.13 coverage) against the Phase 22 template-setter contract; close gaps via APPEND-only edits (preserve the existing SC 1.4.13 suite verbatim); then run the full components-package verify, bump the version 2.5.0 → 2.6.0, and write a CHANGELOG entry.

This plan also reconciles two ROADMAP arithmetic discrepancies discovered during planning:
- **Success #4 (test-file count "28 → 36"):** actual current count is 22 colocated `.test.tsx` files (excluding `_test/` helpers + `index.test.ts` + `date-utils.test.ts` + regression test + useFocusTrap test). After this phase the count is 29 (22 + 7 new). Real target = 29, not 36. Recorded in the summary.
- **Success #5 (`test:wcag-headers` count "24 → 32"):** the wcag-headers script walks `*.test.tsx` under `src/` excluding `_test/`. Current count = 22; target = 29. Recorded in the summary.

Purpose: Closes the TC-15 milestone; bumps + ships.
Output: Augmented Tooltip test (if needed), version bump, CHANGELOG entry, green verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-test-infra-and-first-7-components/22-CONTEXT.md
@.planning/phases/32-tc-15-remaining-component-test-coverage/32-01-SUMMARY.md
@.planning/phases/32-tc-15-remaining-component-test-coverage/32-02-SUMMARY.md
@.planning/phases/32-tc-15-remaining-component-test-coverage/32-03-SUMMARY.md
@.planning/phases/32-tc-15-remaining-component-test-coverage/32-04-SUMMARY.md
@.planning/phases/32-tc-15-remaining-component-test-coverage/32-05-SUMMARY.md
@.planning/phases/32-tc-15-remaining-component-test-coverage/32-06-SUMMARY.md
@.planning/phases/32-tc-15-remaining-component-test-coverage/32-07-SUMMARY.md
@packages/components/src/Tooltip/Tooltip.test.tsx
@packages/components/src/Tooltip/Tooltip.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/_test/helpers.ts
@packages/components/package.json
@packages/components/CHANGELOG.md

<interfaces>
Pre-phase Tooltip.test.tsx audit (planner findings, executor must re-grep to confirm):

WHAT'S ALREADY THERE (preserve verbatim):
- `// @vitest-environment jsdom` on line 1 ✓
- JSDoc with `WCAG SCs covered:` listing 1.4.13 / 2.1.1 / 4.1.2 ✓ (passes check-wcag-headers.mjs already)
- 8 `it(` blocks across Dismissible / Hoverable / Persistent / aria-describedby sub-describes ✓
- Strong SC 1.4.13 contract: Escape closes without moving focus, hover-bridge keeps tooltip open, aria-describedby attach/detach lifecycle ✓ — this is Tier 2 by any reasonable reading

WHAT'S MISSING vs the Phase 22 template-setter shape:
1. NO top-level `describe('Tier 1: Table Stakes', ...)` block — current top-level describe is `describe('Tooltip — WCAG 2.1 SC 1.4.13 (Content on Hover or Focus)', ...)`. Phase 22 D-08 mandates the literal Tier-1 / Tier-2 describe names.
2. NO `expectNoAxeViolations` smoke (ROADMAP success #3 requires ≥ 1 per file).
3. USES `fireEvent` heavily (12+ calls) — D-02a anti-pattern violation per Phase 22 conventions. Tooltip pre-dates the D-02a rule; the existing tests verify a real, subtle race-condition surface that user-event v14 cannot easily reproduce under fake timers (`vi.useFakeTimers()` + `user.keyboard()` deadlocks per STATE.md Phase 27-01 note). Therefore: **accept the existing fireEvent usage under a documented Tooltip-specific waiver** rather than rewrite the working tests (the alternative would risk losing coverage of the Escape-doesn't-poison-dismissed-flag, Hover-bridge, and reopen-after-blur races).

DECISION TREE for executor (audit FIRST, then act):

Run these checks against the current Tooltip.test.tsx HEAD:
- `head -n 30 packages/components/src/Tooltip/Tooltip.test.tsx | grep -c 'WCAG SCs covered:'` — must be 1 (already passes)
- `grep -c "describe('Tier 1: Table Stakes'" packages/components/src/Tooltip/Tooltip.test.tsx` — currently 0
- `grep -c "describe('Tier 2:" packages/components/src/Tooltip/Tooltip.test.tsx` — currently 0
- `grep -c 'expectNoAxeViolations' packages/components/src/Tooltip/Tooltip.test.tsx` — currently 0
- `grep -c 'fireEvent' packages/components/src/Tooltip/Tooltip.test.tsx` — currently ~12 (the D-02a violation surface)

Then take the AUGMENT path (do NOT rewrite the existing SC 1.4.13 suite):

1. APPEND a new `describe('Tier 1: Table Stakes', ...)` block AFTER the existing top-level describe block. Use only `render` + `screen` + `userEvent` (no `fireEvent`). 3 `it(` blocks: mount, ref pass-through to TooltipTrigger child (if applicable — Tooltip.tsx is the source of truth; do NOT assume forwardRef without checking), className passthrough on TooltipContent.
2. APPEND a new `describe('Tier 2: A11y Differentiators (axe-clean + role=tooltip)', ...)` block with 2 `it(` blocks:
   - `tooltip element exposes role="tooltip" when open` — render + focus trigger + assert `screen.getByRole('tooltip')` exists
   - `axe-clean for trigger + open tooltip` — render + focus trigger + `expectNoAxeViolations(container)` (≥ 1 axe smoke per ROADMAP success #3)
3. Add an `## Implementation note: D-02a waiver` JSDoc comment IMMEDIATELY ABOVE the existing top-level describe explaining why the legacy SC 1.4.13 block keeps `fireEvent` — point at STATE.md Phase 27-01 fake-timer+user-event v14 deadlock note.
4. The D-02a grep gate from other plans (`querySelector|configureAxe|toMatchSnapshot|fireEvent` count) IS NOT applied to Tooltip.test.tsx as a hard gate. Instead, the gate is: **the NEW Tier 1 + Tier 2 APPENDED blocks contain ZERO of those four tokens**. The legacy block keeps its fireEvent usage under the documented waiver. The summary must record the file-wide fireEvent count and explain why it is preserved.

Tooltip.tsx exports (need to verify before writing tests — executor MUST read Tooltip.tsx first):
- Likely: `Tooltip`, `TooltipTrigger`, `TooltipContent` (per existing test imports on line 10)
- Check whether any of them is `forwardRef` — if not, skip the ref test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit Tooltip.test.tsx and APPEND Tier 1 + Tier 2 blocks + axe smoke</name>
  <files>packages/components/src/Tooltip/Tooltip.test.tsx</files>
  <read_first>
    - packages/components/src/Tooltip/Tooltip.test.tsx (the existing 165 lines — audit BEFORE writing)
    - packages/components/src/Tooltip/Tooltip.tsx (the exports + ref policy — read to know what to test)
    - packages/components/src/Button/Button.test.tsx (template-setter)
    - packages/components/src/_test/helpers.ts (expectNoAxeViolations)
    - .planning/STATE.md (Phase 27-01 fake-timer + user-event v14 deadlock note — informs the D-02a waiver rationale)
  </read_first>
  <action>
    Step 1 — audit. Run the planner-stated greps; if any current state differs from the planner's findings (e.g. Tier 1 block already exists, fireEvent count is 0, axe smoke already present), STOP and update this plan's `<interfaces>` block before continuing.

    Step 2 — APPEND (do NOT modify the existing top-level describe or any of its `it(` blocks). After the closing `});` of the existing top-level describe (around line 165), add:

    1. A JSDoc block:
       ```
       /**
        * Implementation note: D-02a waiver for the legacy SC 1.4.13 block above.
        * The existing tests use fireEvent because the contract under test
        * (Escape-doesn't-poison-dismissed-flag, hover-bridge cancel, reopen-after-blur)
        * is timing-sensitive and the fake-timers + @testing-library/user-event v14 + vitest 4 combo
        * deadlocks (see STATE.md Plan 27-01 deviations). The NEW Tier 1 + Tier 2 blocks
        * below are D-02a-clean (no fireEvent / querySelector / configureAxe / toMatchSnapshot)
        * and satisfy the Phase 22 template-setter contract for TC-15.
        */
       ```

    2. A new top-level describe:
       ```ts
       describe('Tier 1: Table Stakes', () => {
         it('mounts and renders the trigger child', () => {
           render(<Tooltip>...trigger + content...</Tooltip>);
           expect(screen.getByRole('button', { name: /open settings/i })).toBeInTheDocument();
         });

         it('does not render the tooltip content before interaction', () => {
           render(<Tooltip>...</Tooltip>);
           expect(screen.queryByRole('tooltip')).toBeNull();
         });

         it('TooltipContent renders the provided text when open', async () => {
           const user = userEvent.setup();
           render(<Tooltip>...</Tooltip>);
           const trigger = screen.getByRole('button', { name: /open settings/i });
           trigger.focus(); // direct DOM focus — not fireEvent
           expect(screen.getByRole('tooltip')).toHaveTextContent(/settings tooltip/i);
         });
       });
       ```

       IMPORTANT: use real timers in the new block (`vi.useRealTimers()` in a `beforeEach`, or simply do NOT call `vi.useFakeTimers()` — the legacy block scopes its `useFakeTimers/useRealTimers` to its own `beforeEach/afterEach`, so the new describe inherits real timers by default).

    3. A new top-level describe:
       ```ts
       describe('Tier 2: A11y Differentiators (role=tooltip + axe smoke)', () => {
         it('exposes role="tooltip" when open', () => {
           render(<Tooltip>...</Tooltip>);
           const trigger = screen.getByRole('button', { name: /open settings/i });
           trigger.focus();
           expect(screen.getByRole('tooltip')).toBeInTheDocument();
         });

         it('axe-clean for trigger with open tooltip', async () => {
           const { container } = render(<Tooltip>...</Tooltip>);
           screen.getByRole('button', { name: /open settings/i }).focus();
           await expectNoAxeViolations(container);
         });
       });
       ```

    4. Add imports at the top (only if not already present):
       - `screen` from `@testing-library/react` (already imports `render, fireEvent, act, cleanup` — append `screen`)
       - `userEvent` from `@testing-library/user-event`
       - `expectNoAxeViolations` from `../_test/helpers`

    Use the exact `Tooltip` / `TooltipTrigger` / `TooltipContent` render shape from the existing `renderTooltip` helper (lines 12–19 of the current file). You may share that helper across the new blocks — just call it inside each new `it(`.

    Atomic commit: `test(components): audit Tooltip.test.tsx — append Tier 1 + Tier 2 + axe smoke (TC-15)`.
  </action>
  <verify>
    <automated>npm run test -w @holmdigital/components -- src/Tooltip/Tooltip.test.tsx --run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('Tier 1: Table Stakes'" packages/components/src/Tooltip/Tooltip.test.tsx` returns 1
    - `grep -c "describe('Tier 2:" packages/components/src/Tooltip/Tooltip.test.tsx` returns 1
    - `grep -c 'expectNoAxeViolations' packages/components/src/Tooltip/Tooltip.test.tsx` returns ≥ 1
    - `grep -c 'D-02a waiver' packages/components/src/Tooltip/Tooltip.test.tsx` returns 1 (the waiver JSDoc must mention "D-02a waiver" literally)
    - The legacy SC 1.4.13 top-level describe block remains byte-equivalent: `git diff packages/components/src/Tooltip/Tooltip.test.tsx -- lines 1-165` (approximately) shows only header-import additions, no content deletions in lines 23–165
    - `npm run test -w @holmdigital/components -- Tooltip.test --run` exits 0
    - `npm run test:wcag-headers -w @holmdigital/components` exits 0
  </acceptance_criteria>
  <done>Tooltip test suite now satisfies the four Phase 22 pillars (header + Tier 1 + Tier 2 + axe smoke); legacy SC 1.4.13 block preserved under a documented D-02a waiver.</done>
</task>

<task type="auto">
  <name>Task 2: Bump components package to 2.6.0 and write CHANGELOG entry</name>
  <files>packages/components/package.json, packages/components/CHANGELOG.md</files>
  <read_first>
    - packages/components/package.json (version line)
    - packages/components/CHANGELOG.md (format reference — current entries 2.4.0 and 2.5.0)
    - .planning/STATE.md (Phase 22/24/30/31 MINOR-bump precedent)
  </read_first>
  <action>
    Edit `packages/components/package.json`: change the single `"version"` line from `"2.5.0"` to `"2.6.0"`. No other changes.

    Prepend to `packages/components/CHANGELOG.md` (above the existing `## 2.5.0` entry):

    ```
    ## 2.6.0 — 2026-05-12

    ### Added — Test coverage (TC-15, Phase 32)

    - **Tier 1 + Tier 2 test suites for the 7 remaining uncovered components**:
      - Card, Skeleton, Heading, SkipLink, ProgressBar, Switch, Pagination
      - Each new `*.test.tsx` follows the Phase 22 template-setter shape:
        - `// @vitest-environment jsdom` first line
        - `WCAG SCs covered:` JSDoc header
        - `describe('Tier 1: Table Stakes', …)` + `describe('Tier 2: …', …)`
        - ≥ 1 `expectNoAxeViolations` smoke per file
        - D-02a anti-pattern grep gate clean (zero `querySelector` / `configureAxe` / `toMatchSnapshot` / `fireEvent` in the new files)
    - **Tooltip.test.tsx audited** and augmented in place: appended Tier 1 + Tier 2 blocks + axe smoke. Legacy SC 1.4.13 hover/escape/aria-describedby suite preserved under a documented D-02a waiver (fake-timer + user-event v14 + vitest 4 deadlock; race-condition surface is the tighter constraint).
    - **Test-file count: 22 → 29** colocated component `*.test.tsx` (+7 new files). `check-wcag-headers.mjs` enumerates 29 files all carrying the marker.

    ### ROADMAP arithmetic reconciliation

    - Success criterion #4 stated "28 → 36" test files; actual baseline was 22 (excluding `_test/` helpers, `index.test.ts`, `date-utils.test.ts`, `*.regression.test.tsx`, and `useFocusTrap.test.tsx`); actual target is 29. ROADMAP updated.
    - Success criterion #5 stated `test:wcag-headers` "24 → 32"; actual baseline 22, target 29. ROADMAP updated.
    - Success criterion #1 stated "8 new test files"; actual: 7 new + 1 augmented (Tooltip already existed).

    ### Not changed

    - No source modifications. Public API byte-equivalent to 2.5.0. MINOR bump per project precedent (Phase 22/24/30/31): every test-coverage milestone in this codebase ships as MINOR even when source is untouched, so that downstream consumers see an honest release-train tick.
    ```

    Atomic commit: `chore(components): bump 2.5.0 → 2.6.0 + CHANGELOG for TC-15 (Phase 32)`.
  </action>
  <verify>
    <automated>node -e "const p=require('./packages/components/package.json');if(p.version!=='2.6.0'){console.error('bad version:',p.version);process.exit(1)};const fs=require('fs');const cl=fs.readFileSync('packages/components/CHANGELOG.md','utf8');if(!cl.includes('## 2.6.0')){console.error('missing 2.6.0 entry');process.exit(1)};if(!cl.includes('TC-15')){console.error('missing TC-15 in CHANGELOG');process.exit(1)};console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/components/package.json` version field is `"2.6.0"`
    - `packages/components/CHANGELOG.md` starts with `# @holmdigital/components` then a `## 2.6.0 — 2026-05-12` entry above the existing `## 2.5.0`
    - The 2.6.0 entry contains literal strings `TC-15`, `Phase 32`, `D-02a`, `Tooltip`, and lists all 7 new components (Card, Skeleton, Heading, SkipLink, ProgressBar, Switch, Pagination)
    - Swedish characters (å, ä, ö) preserved if present anywhere in the file (CLAUDE.md rule)
  </acceptance_criteria>
  <done>Version + CHANGELOG ready for publish.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full verify + visual smoke for TC-15 closure</name>
  <what-built>
    All 8 TC-15 component test files complete (7 new + 1 augmented). Components package bumped to 2.6.0 with CHANGELOG entry. Running the full verify pipeline confirms test:ci (vitest + wcag-headers + no-tailwind-leak + no-test-leak), build, check:exports, check:types all green.
  </what-built>
  <how-to-verify>
    Run from the repo root:

    1. `npm run verify -w @holmdigital/components` — must exit 0
    2. `npm run test:wcag-headers -w @holmdigital/components` — must exit 0 AND report `29 test file(s) all carry the marker.` (or similar — the exact number must be 29)
    3. Confirm test count in vitest summary: should be roughly +50 to +100 vs. the pre-phase baseline (each new file: ~7–13 it() blocks; 7 files; rough range 50–90 new test cases). If it lands outside [+40, +110], surface to the user — likely indicates over- or under-coverage in one of the new files.
    4. Visually inspect:
       - `packages/components/package.json` shows `"version": "2.6.0"`
       - `packages/components/CHANGELOG.md` top entry is `## 2.6.0 — 2026-05-12` with TC-15 / Phase 32 details
       - `git diff --stat packages/components/src/Tooltip/Tooltip.test.tsx` shows additions only (no deletions in lines 23–165)
       - `git status` shows: 7 new test files + Tooltip.test.tsx modified + package.json + CHANGELOG.md + ROADMAP.md (if you also tick the Phase 32 checkbox) + STATE.md (if updated to reflect Phase 32 complete)

    Then signal to proceed with commit + publish guidance.
  </how-to-verify>
  <resume-signal>Type "approved" to mark TC-15 closed, or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
- `npm run verify -w @holmdigital/components` exits 0
- `npm run test:wcag-headers -w @holmdigital/components` exits 0 and reports 29 test files
- `node -e "console.log(require('./packages/components/package.json').version)"` prints `2.6.0`
- `head -n 5 packages/components/CHANGELOG.md` shows the 2.6.0 entry
</verification>

<success_criteria>
TC-15 closed. All 8 components carry Tier 1 + Tier 2 contract proofs. Components package shipped at 2.6.0 with a clean CHANGELOG entry. Test count up 22 → 29 files. ROADMAP arithmetic reconciled and documented. Ready for v0.7's last phase (Phase 33: lint + typecheck gates).
</success_criteria>

<output>
Create `.planning/phases/32-tc-15-remaining-component-test-coverage/32-08-SUMMARY.md` recording:
- Final per-file `it(` counts (7 new files + Tooltip augmented count)
- Total components-package test file count (expected 29)
- Total vitest test-case delta vs pre-phase baseline
- Tooltip waiver rationale (cite STATE.md Phase 27-01)
- The two ROADMAP-arithmetic reconciliations (22 → 29, not 28 → 36 or 24 → 32)
- Version bump decision: MINOR 2.5.0 → 2.6.0 per Phase 22/24/30/31 precedent
- Any deviations from the planned shape
</output>
</content>
</invoke>