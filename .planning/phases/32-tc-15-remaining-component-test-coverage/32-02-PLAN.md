---
phase: 32-tc-15-remaining-component-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/Skeleton/Skeleton.test.tsx
autonomous: true
requirements: [TC-15]
must_haves:
  truths:
    - "Skeleton.test.tsx exists colocated with Skeleton.tsx and follows the Phase 22 template-setter shape"
    - "First 30 lines contain the literal marker `WCAG SCs covered:`"
    - "Two top-level describes: Tier 1: Table Stakes and Tier 2: A11y Differentiators"
    - "Tests cover: variant render (text/circular/rectangular), width/height pass-through, animation class application (pulse/wave/none), className passthrough, aria-hidden=true presence (Skeleton is purely decorative — AT must NOT see it), axe-clean smoke"
    - "Uses expectNoAxeViolations; zero querySelector / configureAxe / toMatchSnapshot / fireEvent (D-02a)"
    - "5–10 it() blocks per D-02"
  artifacts:
    - path: "packages/components/src/Skeleton/Skeleton.test.tsx"
      provides: "Skeleton Tier 1+2 contract (TC-15)"
      contains: "Tier 1: Table Stakes"
      min_lines: 90
  key_links:
    - from: "Skeleton.test.tsx"
      to: "_test/helpers"
      via: "import"
      pattern: "from '\\.\\./_test/helpers'"
    - from: "Skeleton.test.tsx"
      to: "./Skeleton"
      via: "import { Skeleton }"
      pattern: "from '\\./Skeleton'"
---

<objective>
Add the Tier 1 + Tier 2 test suite for Skeleton (TC-15). Skeleton is purely decorative and renders a `<span aria-hidden="true">` — Tier 2 verifies the AT-invisibility contract and the variant/animation API.

Purpose: Closes TC-15 row for Skeleton; brings 2 of 7 new test files online in Wave 1.
Output: One colocated `.test.tsx` (~90–130 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-test-infra-and-first-7-components/22-05-PLAN.md
@packages/components/src/Skeleton/Skeleton.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/HelpText/HelpText.test.tsx
@packages/components/src/_test/helpers.ts

<interfaces>
SkeletonProps (from Skeleton.tsx):
- variant?: 'text' | 'circular' | 'rectangular'  (default 'text')
- width?: string | number
- height?: string | number
- animation?: 'pulse' | 'wave' | 'none'  (default 'pulse')
- className?: string

Render shape (React.FC, NOT forwardRef):
- Returns a fragment: <><style>{...}</style><span className="hd-skeleton {animClass} {consumer}" style={...} aria-hidden="true" /></>
- The visible element is the second child (the span); `<style>` injects keyframes.
- animation='pulse' → class includes `hd-skeleton-pulse`
- animation='wave'  → class includes `hd-skeleton-wave`
- animation='none'  → no animation class (just `hd-skeleton`)
- variant='circular' → borderRadius: '50%' in inline style
- variant='text'     → width defaults to '100%', height to '1em' if not provided

Skeleton is React.FC — do NOT add ref-forwarding test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Skeleton.test.tsx (TC-15)</name>
  <files>packages/components/src/Skeleton/Skeleton.test.tsx</files>
  <read_first>
    - packages/components/src/Skeleton/Skeleton.tsx (props + render shape)
    - packages/components/src/Button/Button.test.tsx (template-setter)
    - packages/components/src/HelpText/HelpText.test.tsx (simple-component reference)
    - packages/components/src/_test/helpers.ts (expectNoAxeViolations signature)
  </read_first>
  <behavior>
    - Tier 1: renders without crashing (default variant 'text'); className passes through additively; width/height accept both string and number; renders one visible element (the span)
    - Tier 2: span carries `aria-hidden="true"` (AT must not announce the placeholder); each variant renders without crashing (parametrize via `it.each`); each animation value applies the correct class hook (`hd-skeleton-pulse` / `hd-skeleton-wave` / no animation class for `none`); axe-clean smoke for at least one configuration (default + circular)
  </behavior>
  <action>
    Create `packages/components/src/Skeleton/Skeleton.test.tsx`. Required shape:

    1. Line 1: `// @vitest-environment jsdom`
    2. JSDoc header (lines 2–18) with `WCAG SCs covered:` and at minimum:
       - 1.1.1 Non-text Content — decorative placeholder marked `aria-hidden="true"` so AT skips it
       - 4.1.2 Name, Role, Value — axe-clean across variants and animations
    3. Imports:
       ```ts
       import { render, screen } from '@testing-library/react';
       import { describe, it, expect } from 'vitest';
       import { Skeleton } from './Skeleton';
       import { expectNoAxeViolations } from '../_test/helpers';
       ```
    4. Two top-level describes: `Tier 1: Table Stakes` and `Tier 2: A11y Differentiators (Decorative Placeholder)`.

    Tier 1 `it(` blocks (target 3–4):
    - `mounts and renders the placeholder span` — `const { container } = render(<Skeleton data-testid="sk" />)`; assert there is at least one element with class `hd-skeleton` in the container (use `container.getElementsByClassName('hd-skeleton').length` — NOT querySelector — to satisfy D-02a; the helper API does not forbid `getElementsByClassName`)
    - `accepts string width and height` — `render(<Skeleton width="200px" height="40px" />)`; locate the `hd-skeleton` span via `container.getElementsByClassName('hd-skeleton')[0]` and assert `(el as HTMLElement).style.width === '200px'` and `style.height === '40px'`
    - `accepts numeric width and height` — same as above with `width={200} height={40}`; React inlines as `200px`/`40px`
    - `appends consumer className additively` — `render(<Skeleton className="extra" />)`; assert the placeholder element has class `extra` AND class `hd-skeleton`

    Tier 2 `it(` blocks (target 4–5):
    - `is hidden from assistive technology (aria-hidden="true")` — render default Skeleton; locate the placeholder span; assert `toHaveAttribute('aria-hidden', 'true')` — THIS IS THE CORE TIER-2 CONTRACT
    - `renders each variant without crashing` — `it.each(['text', 'circular', 'rectangular'] as const)('variant=%s mounts', (variant) => { render(<Skeleton variant={variant} />); })` — no assertion beyond non-throw is required for variant geometry (jsdom cannot measure layout)
    - `applies the animation class hook for pulse` — render `<Skeleton animation="pulse" />`; assert placeholder has class `hd-skeleton-pulse`
    - `applies the animation class hook for wave` — render `<Skeleton animation="wave" />`; assert placeholder has class `hd-skeleton-wave`
    - `applies no animation class when animation="none"` — render `<Skeleton animation="none" />`; assert placeholder does NOT have class `hd-skeleton-pulse` AND does NOT have class `hd-skeleton-wave`
    - `axe-clean for default skeleton` — `const { container } = render(<Skeleton />); await expectNoAxeViolations(container);`
    - `axe-clean for circular variant` — `const { container } = render(<Skeleton variant="circular" width={40} height={40} />); await expectNoAxeViolations(container);`

    Total: 7–10 `it(` blocks. NO `fireEvent`, NO `querySelector`, NO `configureAxe`, NO `toMatchSnapshot`. `getElementsByClassName` IS allowed (it is not on the D-02a grep blocklist) — Plan 22-05 carry-forward.

    Atomic commit: `test(components): add Skeleton Tier 1+2 suite (TC-15)`.
  </action>
  <verify>
    <automated>npm run test -w @holmdigital/components -- src/Skeleton/Skeleton.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/components/src/Skeleton/Skeleton.test.tsx` exists
    - `head -n 30 packages/components/src/Skeleton/Skeleton.test.tsx | grep -c 'WCAG SCs covered:'` returns 1
    - `grep -c "describe('Tier 1: Table Stakes'" packages/components/src/Skeleton/Skeleton.test.tsx` returns 1
    - `grep -c "describe('Tier 2:" packages/components/src/Skeleton/Skeleton.test.tsx` returns 1
    - `grep -c "expectNoAxeViolations" packages/components/src/Skeleton/Skeleton.test.tsx` returns ≥ 2
    - `grep -v '^[[:space:]]*\*' packages/components/src/Skeleton/Skeleton.test.tsx | grep -v '^[[:space:]]*//' | grep -cE 'querySelector|configureAxe|toMatchSnapshot|fireEvent'` returns 0
    - `grep -c "aria-hidden" packages/components/src/Skeleton/Skeleton.test.tsx` returns ≥ 1 (Tier-2 contract proof)
    - `grep -c '^[[:space:]]*it(' packages/components/src/Skeleton/Skeleton.test.tsx` returns ≥ 5 and ≤ 12
    - `npm run test -w @holmdigital/components -- Skeleton.test --run` exits 0
    - `npm run test:wcag-headers -w @holmdigital/components` exits 0
  </acceptance_criteria>
  <done>Skeleton test suite green; aria-hidden contract pinned; D-02a clean.</done>
</task>

</tasks>

<verification>
- `npm run test -w @holmdigital/components -- Skeleton.test --run` exits 0
- D-02a anti-pattern grep gates clean
- WCAG header marker present
</verification>

<success_criteria>
Skeleton has Tier 1 + Tier 2 contract coverage. 2 of 7 new TC-15 test files complete.
</success_criteria>

<output>
Create `.planning/phases/32-tc-15-remaining-component-test-coverage/32-02-SUMMARY.md` recording final `it(` count and any deviations.
</output>
</content>
</invoke>