---
phase: 32-tc-15-remaining-component-test-coverage
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/components/src/ProgressBar/ProgressBar.test.tsx
autonomous: true
requirements: [TC-15]
must_haves:
  truths:
    - "ProgressBar.test.tsx exists colocated with ProgressBar.tsx and follows the Phase 22 template-setter shape"
    - "First 30 lines contain `WCAG SCs covered:`"
    - "Two top-level describes: Tier 1 + Tier 2"
    - "Tests cover: role=progressbar, aria-valuenow/min/max reflect props, aria-label === label prop (REQUIRED), aria-valuetext when valueText prop is set, value clamping at min and max boundaries, percentage rendering when showValueLabel=true, variant prop accepted (smoke), className passthrough, axe-clean"
    - "Uses expectNoAxeViolations; zero querySelector / configureAxe / toMatchSnapshot / fireEvent (D-02a)"
    - "6–12 it() blocks per D-02"
  artifacts:
    - path: "packages/components/src/ProgressBar/ProgressBar.test.tsx"
      provides: "ProgressBar Tier 1+2 contract (TC-15)"
      contains: "Tier 1: Table Stakes"
      min_lines: 110
  key_links:
    - from: "ProgressBar.test.tsx"
      to: "_test/helpers"
      via: "import"
      pattern: "from '\\.\\./_test/helpers'"
    - from: "ProgressBar.test.tsx"
      to: "./ProgressBar"
      via: "import { ProgressBar }"
      pattern: "from '\\./ProgressBar'"
---

<objective>
Add the Tier 1 + Tier 2 test suite for ProgressBar (TC-15). ProgressBar's Tier-2 contract is the `progressbar` ARIA role + the aria-valuenow/min/max numeric contract (SC 4.1.2 Name, Role, Value).

Purpose: Closes TC-15 row for ProgressBar; brings 5 of 7 new test files online in Wave 2.
Output: One colocated `.test.tsx` (~110–160 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-test-infra-and-first-7-components/22-05-PLAN.md
@packages/components/src/ProgressBar/ProgressBar.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/_test/helpers.ts

<interfaces>
ProgressBarProps (from ProgressBar.tsx):
- value: number                                   (required)
- min?: number                                    (default 0)
- max?: number                                    (default 100)
- label: string                                   (required — used for aria-label)
- showValueLabel?: boolean                        (default false; renders "<percentage>%" beside label)
- valueText?: string                              (optional; sets aria-valuetext, e.g. "Step 2 of 4")
- variant?: 'primary' | 'success' | 'warning' | 'danger'  (default 'primary')
- className?: string

ProgressBar is React.FC (NOT forwardRef) — do NOT add a ref-forwarding test.

Render shape:
- Wrapper <div className={`hd-progress-root ${className}`}>
-   <div id={`progress-label-<slugified label>`}><span>{label}</span>{showValueLabel && <span>{percentage}%</span>}</div>
-   <div role="progressbar" aria-valuenow={clamp(value)} aria-valuemin={min} aria-valuemax={max} aria-label={label} aria-valuetext={valueText}>
-     <div /> (the filled bar)
-   </div>
- </div>

Clamping: clampedValue = Math.min(Math.max(value, min), max); percentage = round((clampedValue - min) / (max - min) * 100).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProgressBar.test.tsx (TC-15)</name>
  <files>packages/components/src/ProgressBar/ProgressBar.test.tsx</files>
  <read_first>
    - packages/components/src/ProgressBar/ProgressBar.tsx (props + clamping math + ARIA wiring)
    - packages/components/src/Button/Button.test.tsx (template-setter)
    - packages/components/src/_test/helpers.ts (expectNoAxeViolations)
  </read_first>
  <behavior>
    - Tier 1: renders without crashing; renders the label text once; className passes through to the wrapper additively; showValueLabel=true renders the percentage string; default min=0/max=100 are applied when not provided
    - Tier 2: the progressbar element has role="progressbar"; aria-label === label prop; aria-valuenow === value (when value is in range); aria-valuemin === min; aria-valuemax === max; value above max is clamped to max in aria-valuenow; value below min is clamped to min; aria-valuetext is set when valueText prop is provided AND omitted when not provided; axe-clean for default and for indeterminate-shape (here: at 0%, at 100%, with valueText)
  </behavior>
  <action>
    Create `packages/components/src/ProgressBar/ProgressBar.test.tsx`. Required shape:

    1. Line 1: `// @vitest-environment jsdom`
    2. JSDoc header (lines 2–20) with `WCAG SCs covered:` and at minimum:
       - 1.3.1 Info and Relationships — progressbar role + numeric ARIA wiring expose state to AT
       - 4.1.2 Name, Role, Value — role=progressbar, aria-valuenow/min/max, aria-label, aria-valuetext — the entire Tier-2 contract
       - 1.4.3 Contrast (Minimum) — variants render distinctly (real-browser axe-contrast deferred — same caveat as Button.test.tsx)
    3. Imports:
       ```ts
       import { render, screen } from '@testing-library/react';
       import { describe, it, expect } from 'vitest';
       import { ProgressBar } from './ProgressBar';
       import { expectNoAxeViolations } from '../_test/helpers';
       ```
    4. Two top-level describes: `Tier 1: Table Stakes` and `Tier 2: A11y Differentiators (progressbar role + numeric ARIA)`.

    Tier 1 `it(` blocks (target 3–4):
    - `mounts and renders without crashing` — `render(<ProgressBar value={50} label="Upload" />)`; assert `screen.getByRole('progressbar')` is in the document
    - `renders the label text` — `render(<ProgressBar value={50} label="Upload" />)`; assert `screen.getByText('Upload')` is in the document (label appears as a text node in the visible label `<div>`)
    - `shows the percentage value when showValueLabel is true` — `render(<ProgressBar value={42} label="Upload" showValueLabel />)`; assert `screen.getByText('42%')` is in the document
    - `omits the percentage value when showValueLabel is false (default)` — render without `showValueLabel`; assert `screen.queryByText(/\d+%/)` is null
    - `appends consumer className to the wrapper additively` — `const { container } = render(<ProgressBar value={50} label="Upload" className="extra-class" />)`; assert the wrapper (first child of container) has class `extra-class` AND class `hd-progress-root`

    Tier 2 `it(` blocks (target 5–8):
    - `exposes role="progressbar"` — assert `screen.getByRole('progressbar')` returns an element with `toHaveAttribute('role', 'progressbar')` (the implicit `getByRole` already proves this — but make the assertion explicit so future role drift trips a fast test)
    - `aria-label equals the label prop` — `render(<ProgressBar value={50} label="Upload progress" />)`; assert `screen.getByRole('progressbar')` `toHaveAttribute('aria-label', 'Upload progress')`
    - `aria-valuenow reflects value when in range` — `render(<ProgressBar value={42} label="x" />)`; assert `toHaveAttribute('aria-valuenow', '42')`
    - `aria-valuemin and aria-valuemax default to 0 and 100` — `render(<ProgressBar value={50} label="x" />)`; assert `toHaveAttribute('aria-valuemin', '0')` AND `toHaveAttribute('aria-valuemax', '100')`
    - `aria-valuemin and aria-valuemax reflect custom min/max props` — `render(<ProgressBar value={5} min={1} max={10} label="x" />)`; assert valuemin='1' and valuemax='10' and valuenow='5'
    - `clamps value above max to max in aria-valuenow` — `render(<ProgressBar value={150} max={100} label="x" />)`; assert aria-valuenow='100'
    - `clamps value below min to min in aria-valuenow` — `render(<ProgressBar value={-10} min={0} label="x" />)`; assert aria-valuenow='0'
    - `sets aria-valuetext when valueText prop is provided` — `render(<ProgressBar value={2} max={4} label="Step progress" valueText="Step 2 of 4" />)`; assert progressbar `toHaveAttribute('aria-valuetext', 'Step 2 of 4')`
    - `does not set aria-valuetext when valueText is omitted` — `render(<ProgressBar value={50} label="x" />)`; assert progressbar element does NOT have an `aria-valuetext` attribute (use `not.toHaveAttribute('aria-valuetext')`)
    - `axe-clean at 0%` — `const { container } = render(<ProgressBar value={0} label="Upload" />); await expectNoAxeViolations(container);`
    - `axe-clean at 100% with valueText` — `const { container } = render(<ProgressBar value={4} max={4} label="Steps" valueText="Done" showValueLabel />); await expectNoAxeViolations(container);`

    Total: 10–13 `it(` blocks. NO `fireEvent`, NO `querySelector`, NO `configureAxe`, NO `toMatchSnapshot`. Variant smoke can be parametrized via `it.each(['primary','success','warning','danger'] as const)` as ONE additional Tier-2 `it(` if you want to push to 13.

    Atomic commit: `test(components): add ProgressBar Tier 1+2 suite (TC-15)`.
  </action>
  <verify>
    <automated>npm run test -w @holmdigital/components -- src/ProgressBar/ProgressBar.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/components/src/ProgressBar/ProgressBar.test.tsx` exists
    - `head -n 30 packages/components/src/ProgressBar/ProgressBar.test.tsx | grep -c 'WCAG SCs covered:'` returns 1
    - `grep -c "describe('Tier 1: Table Stakes'" packages/components/src/ProgressBar/ProgressBar.test.tsx` returns 1
    - `grep -c "describe('Tier 2:" packages/components/src/ProgressBar/ProgressBar.test.tsx` returns 1
    - `grep -c "expectNoAxeViolations" packages/components/src/ProgressBar/ProgressBar.test.tsx` returns ≥ 2
    - `grep -c "aria-valuenow" packages/components/src/ProgressBar/ProgressBar.test.tsx` returns ≥ 3 (clamp + range tests)
    - `grep -c "aria-valuetext" packages/components/src/ProgressBar/ProgressBar.test.tsx` returns ≥ 2 (present + absent assertions)
    - `grep -v '^[[:space:]]*\*' packages/components/src/ProgressBar/ProgressBar.test.tsx | grep -v '^[[:space:]]*//' | grep -cE 'querySelector|configureAxe|toMatchSnapshot|fireEvent'` returns 0
    - `grep -c '^[[:space:]]*it(' packages/components/src/ProgressBar/ProgressBar.test.tsx` returns ≥ 8 and ≤ 14
    - `npm run test -w @holmdigital/components -- ProgressBar.test --run` exits 0
    - `npm run test:wcag-headers -w @holmdigital/components` exits 0
  </acceptance_criteria>
  <done>ProgressBar test suite green; numeric ARIA + clamping pinned; D-02a clean.</done>
</task>

</tasks>

<verification>
- `npm run test -w @holmdigital/components -- ProgressBar.test --run` exits 0
- D-02a grep gates clean
- All four valuenow/min/max/valuetext assertions present
</verification>

<success_criteria>
ProgressBar has Tier 1 + Tier 2 contract coverage; numeric ARIA + value clamping + indeterminate-vs-determinate distinction (via valueText presence) pinned. 5 of 7 new TC-15 test files complete.
</success_criteria>

<output>
Create `.planning/phases/32-tc-15-remaining-component-test-coverage/32-05-SUMMARY.md` recording final `it(` count and any deviations.
</output>
</content>
</invoke>