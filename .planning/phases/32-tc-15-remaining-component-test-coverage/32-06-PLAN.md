---
phase: 32-tc-15-remaining-component-test-coverage
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/components/src/Switch/Switch.test.tsx
autonomous: true
requirements: [TC-15]
must_haves:
  truths:
    - "Switch.test.tsx exists colocated with Switch.tsx and follows the Phase 22 template-setter shape"
    - "First 30 lines contain `WCAG SCs covered:`"
    - "Two top-level describes: Tier 1 + Tier 2"
    - "Tests cover: role=switch, aria-checked reflects checked prop, Space toggles via onCheckedChange (NOT typing literal space), Enter toggles via onCheckedChange, click toggles, disabled blocks click AND keyboard AND has disabled DOM attribute, label htmlFor wires to the switch id, custom id is honored, axe-clean"
    - "Click-keyboard parity (D-02a): every click test has a paired keyboard test"
    - "Uses expectNoAxeViolations + userEvent.keyboard; zero querySelector / configureAxe / toMatchSnapshot / fireEvent (D-02a)"
    - "8–13 it() blocks per D-02"
  artifacts:
    - path: "packages/components/src/Switch/Switch.test.tsx"
      provides: "Switch Tier 1+2 contract (TC-15)"
      contains: "Tier 1: Table Stakes"
      min_lines: 120
  key_links:
    - from: "Switch.test.tsx"
      to: "_test/helpers"
      via: "import"
      pattern: "from '\\.\\./_test/helpers'"
    - from: "Switch.test.tsx"
      to: "./Switch"
      via: "import { Switch }"
      pattern: "from '\\./Switch'"
---

<objective>
Add the Tier 1 + Tier 2 test suite for Switch (TC-15). Switch's Tier-2 contract is the APG Switch pattern: role="switch" + aria-checked + Space-toggle (without inserting a literal space) + Enter-toggle. This is the SC 2.1.1 Keyboard differentiator.

Purpose: Closes TC-15 row for Switch; brings 6 of 7 new test files online in Wave 2.
Output: One colocated `.test.tsx` (~120–170 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-test-infra-and-first-7-components/22-05-PLAN.md
@packages/components/src/Switch/Switch.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/Accordion/Accordion.test.tsx
@packages/components/src/_test/helpers.ts

<interfaces>
SwitchProps (from Switch.tsx — interface is NOT exported; the `Switch` value export is):
- checked: boolean                                  (required)
- onCheckedChange: (checked: boolean) => void       (required)
- disabled?: boolean                                (default false)
- label?: string                                    (optional; renders an associated <label htmlFor>)
- id?: string                                       (optional; auto-generated if not provided)

Switch is a plain function component (NOT forwardRef). Do NOT add a ref test.

Render shape:
- <div className="flex items-center gap-3">
-   <button type="button" role="switch" aria-checked={checked} id={id||generated} disabled={disabled}
-           onClick={() => !disabled && onCheckedChange(!checked)}
-           onKeyDown={(e) => { if (!disabled && (e.key === 'Enter' || e.key === ' ')) { e.preventDefault(); onCheckedChange(!checked); } }}>
-     <span /> (visual thumb)
-   </button>
-   {label && <label htmlFor={id||generated} onClick={handleClick}>{label}</label>}
- </div>

CRITICAL TIER-2 PROOF: pressing Space must call onCheckedChange exactly once with `!checked`. userEvent.keyboard('{ }') OR '{Space}' on a focused <button role="switch"> — the component's onKeyDown prevents default, so no literal space character is appended anywhere. The TEST must assert (a) onCheckedChange called once, (b) no text node anywhere contains a stray space we did not put there. Asserting (a) is sufficient; (b) is over-engineering.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Switch.test.tsx (TC-15)</name>
  <files>packages/components/src/Switch/Switch.test.tsx</files>
  <read_first>
    - packages/components/src/Switch/Switch.tsx (props + onKeyDown handler — Space/Enter both preventDefault)
    - packages/components/src/Button/Button.test.tsx (template-setter — Space/Enter paired tests, D-02a click+keyboard pairing)
    - packages/components/src/Accordion/Accordion.test.tsx (Tier-2 keyboard reference)
    - packages/components/src/_test/helpers.ts (expectNoAxeViolations)
  </read_first>
  <behavior>
    - Tier 1: renders without crashing; renders a button with role="switch"; label prop renders an associated `<label>` that points to the switch id; custom id is used verbatim when provided; auto-generated id is non-empty when id is omitted
    - Tier 2: aria-checked="true" when checked={true}; aria-checked="false" when checked={false}; clicking calls onCheckedChange(!checked) once; pressing Space (focused) calls onCheckedChange(!checked) once — and does NOT insert a literal space (no untrusted text mutation); pressing Enter (focused) calls onCheckedChange(!checked) once; disabled={true} blocks click (onCheckedChange not called) AND blocks Space AND blocks Enter AND sets DOM `disabled` attribute; axe-clean for both checked and unchecked states
  </behavior>
  <action>
    Create `packages/components/src/Switch/Switch.test.tsx`. Required shape:

    1. Line 1: `// @vitest-environment jsdom`
    2. JSDoc header (lines 2–20) with `WCAG SCs covered:` and at minimum:
       - 2.1.1 Keyboard — Space and Enter both toggle the switch; disabled blocks both; this is the WHOLE Tier-2 contract
       - 4.1.2 Name, Role, Value — role="switch", aria-checked, accessible name from associated label
       - 3.3.2 Labels or Instructions — label htmlFor wires to switch id
    3. Imports:
       ```ts
       import { useState } from 'react';
       import { render, screen } from '@testing-library/react';
       import userEvent from '@testing-library/user-event';
       import { describe, it, expect, vi } from 'vitest';
       import { Switch } from './Switch';
       import { expectNoAxeViolations } from '../_test/helpers';
       ```
    4. Two top-level describes: `Tier 1: Table Stakes` and `Tier 2: A11y Differentiators (APG Switch — Space/Enter toggle)`.

    Tier 1 `it(` blocks (target 4–5):
    - `mounts and renders a switch button` — `render(<Switch checked={false} onCheckedChange={() => {}} />)`; assert `screen.getByRole('switch')` is in the document
    - `renders the label and associates it with the switch via htmlFor` — `render(<Switch checked={false} onCheckedChange={() => {}} label="Notifications" id="notif" />)`; assert `screen.getByRole('switch', { name: /notifications/i })` is in the document; also assert `screen.getByText('Notifications').getAttribute('for') === 'notif'` (label htmlFor → DOM attribute is `for`)
    - `honors a custom id prop` — render with `id="my-switch"`; assert switch has `id="my-switch"`
    - `generates a non-empty id when id is omitted` — render without id; assert switch's `id` attribute is a non-empty string (`.id.length > 0`)
    - `applies the disabled DOM attribute when disabled={true}` — render with `disabled`; assert `screen.getByRole('switch').hasAttribute('disabled') === true`

    Tier 2 `it(` blocks (target 6–9):
    - `aria-checked="true" when checked={true}` — `render(<Switch checked={true} onCheckedChange={() => {}} />)`; assert `toHaveAttribute('aria-checked', 'true')`
    - `aria-checked="false" when checked={false}` — same with `checked={false}`; assert aria-checked='false'
    - `click calls onCheckedChange with !checked` — `const onChange = vi.fn(); render(<Switch checked={false} onCheckedChange={onChange} />); const user = userEvent.setup(); await user.click(screen.getByRole('switch'));` assert `onChange` was called with `true` and called once
    - `Space toggles via onCheckedChange (no literal space inserted)` — render Switch focused (use `<button>` parent setup or just `await user.tab()` to land on it), then `await user.keyboard('{ }')` (user-event v14 syntax for Space) OR `await user.keyboard(' ')`; assert `onChange` called with `true` and called once. PAIRED with the click test per D-02a.
    - `Enter toggles via onCheckedChange` — same setup with `await user.keyboard('{Enter}')`; assert one call with `true`
    - `disabled blocks click` — `const onChange = vi.fn(); render(<Switch checked={false} onCheckedChange={onChange} disabled />); await user.click(screen.getByRole('switch'));` assert `onChange` NOT called (`expect(onChange).not.toHaveBeenCalled()`)
    - `disabled blocks Space and Enter` — same `disabled` render; tab to focus, send `{ }` and `{Enter}`; assert `onChange` NOT called for either (paired keyboard half of the click test — D-02a)
    - `controlled flip: checked={true} → click → onCheckedChange(false)` — render with `checked={true}`; click; assert onChange called with `false`
    - `axe-clean for unchecked switch with label` — `const { container } = render(<Switch checked={false} onCheckedChange={() => {}} label="Notify" />); await expectNoAxeViolations(container);`
    - `axe-clean for checked switch with label` — same with `checked={true}`

    Total: 10–14 `it(` blocks. Every click test has a paired keyboard test (D-02a). NO `fireEvent`, NO `querySelector`, NO `configureAxe`, NO `toMatchSnapshot`. Use `userEvent.setup()` + `user.keyboard(...)`. Reach focus via either `await user.tab()` (the switch button is the only focusable in the tree) OR `element.focus()` on the result of `screen.getByRole('switch')`.

    Atomic commit: `test(components): add Switch Tier 1+2 suite (TC-15)`.
  </action>
  <verify>
    <automated>npm run test -w @holmdigital/components -- src/Switch/Switch.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/components/src/Switch/Switch.test.tsx` exists
    - `head -n 30 packages/components/src/Switch/Switch.test.tsx | grep -c 'WCAG SCs covered:'` returns 1
    - `grep -c "describe('Tier 1: Table Stakes'" packages/components/src/Switch/Switch.test.tsx` returns 1
    - `grep -c "describe('Tier 2:" packages/components/src/Switch/Switch.test.tsx` returns 1
    - `grep -c "expectNoAxeViolations" packages/components/src/Switch/Switch.test.tsx` returns ≥ 2
    - `grep -c "aria-checked" packages/components/src/Switch/Switch.test.tsx` returns ≥ 2
    - `grep -c "user.keyboard" packages/components/src/Switch/Switch.test.tsx` returns ≥ 3 (Space + Enter + disabled keyboard)
    - `grep -v '^[[:space:]]*\*' packages/components/src/Switch/Switch.test.tsx | grep -v '^[[:space:]]*//' | grep -cE 'querySelector|configureAxe|toMatchSnapshot|fireEvent'` returns 0
    - `grep -c '^[[:space:]]*it(' packages/components/src/Switch/Switch.test.tsx` returns ≥ 8 and ≤ 15
    - `npm run test -w @holmdigital/components -- Switch.test --run` exits 0
    - `npm run test:wcag-headers -w @holmdigital/components` exits 0
  </acceptance_criteria>
  <done>Switch test suite green; APG Switch keyboard + aria-checked + disabled-blocking pinned; D-02a click/keyboard parity preserved.</done>
</task>

</tasks>

<verification>
- `npm run test -w @holmdigital/components -- Switch.test --run` exits 0
- D-02a grep gates clean
- Click + keyboard paired (no orphan click test)
</verification>

<success_criteria>
Switch has Tier 1 + Tier 2 contract coverage; the APG Switch Space/Enter toggle and disabled-blocking are pinned as regression tests. 6 of 7 new TC-15 test files complete.
</success_criteria>

<output>
Create `.planning/phases/32-tc-15-remaining-component-test-coverage/32-06-SUMMARY.md` recording final `it(` count and any deviations.
</output>
</content>
</invoke>