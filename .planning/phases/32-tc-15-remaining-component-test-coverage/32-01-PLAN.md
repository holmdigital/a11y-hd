---
phase: 32-tc-15-remaining-component-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/Card/Card.test.tsx
autonomous: true
requirements: [TC-15]
must_haves:
  truths:
    - "Card.test.tsx exists colocated with Card.tsx and follows the Phase 22 template-setter shape (Button.test.tsx)"
    - "First 30 lines contain the literal marker `WCAG SCs covered:` so check-wcag-headers.mjs passes"
    - "Two top-level describes: Tier 1: Table Stakes and Tier 2: A11y Differentiators"
    - "Tests cover: title-rendering, href stretched-link composition (title becomes anchor when href is set, stretched-link span is aria-hidden), `as` prop renders div/article/section/li, className passthrough, axe-clean (with title only / with href / with all slots)"
    - "Uses expectNoAxeViolations from ../_test/helpers; zero querySelector, configureAxe, toMatchSnapshot, fireEvent (D-02a anti-pattern gates)"
    - "5–10 it() blocks per D-02 (Card has no keyboard surface — Tier 2 focuses on composition + ARIA wire-up of the stretched-link pattern)"
  artifacts:
    - path: "packages/components/src/Card/Card.test.tsx"
      provides: "Card Tier 1+2 contract (TC-15)"
      contains: "Tier 1: Table Stakes"
      min_lines: 90
  key_links:
    - from: "Card.test.tsx"
      to: "_test/helpers"
      via: "import"
      pattern: "from '\\.\\./_test/helpers'"
    - from: "Card.test.tsx"
      to: "./Card"
      via: "import { Card }"
      pattern: "from '\\./Card'"
---

<objective>
Add the Tier 1 + Tier 2 test suite for Card (TC-15). Card has no keyboard surface, so Tier 2 focuses on the stretched-link composition (href-makes-title-an-anchor, stretched-link span is aria-hidden) and the `as` polymorphism. Mirror the Button.test.tsx file shape exactly.

Purpose: Closes TC-15 row for Card; brings 1 of 7 new test files online in Wave 1.
Output: One colocated `.test.tsx` (~90–140 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-test-infra-and-first-7-components/22-05-PLAN.md
@.planning/phases/22-test-infra-and-first-7-components/22-CONTEXT.md
@packages/components/TESTING-CONVENTIONS.md
@packages/components/src/Card/Card.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/HelpText/HelpText.test.tsx
@packages/components/src/_test/helpers.ts
@packages/components/src/_test/setup.ts

<interfaces>
CardProps (from Card.tsx):
- title?: React.ReactNode
- href?: string                   // when present: title wraps in <a>, plus an aria-hidden stretched-link <span>
- children: React.ReactNode       // rendered inside .hd-card-content <div>
- className?: string              // appended to container element
- as?: 'div' | 'article' | 'section' | 'li'  (default 'div')

Render shape:
- <Component className={className}>
-   {title && <h3>{href ? <a href={href}><span aria-hidden="true" />{title}</a> : title}</h3>}
-   <div>{children}</div>
- </Component>

Card is React.FC (NOT forwardRef) — do NOT add a ref-forwarding test for Card.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Card.test.tsx (TC-15)</name>
  <files>packages/components/src/Card/Card.test.tsx</files>
  <read_first>
    - packages/components/src/Card/Card.tsx (props + render shape — no keyboard, no ref)
    - packages/components/src/Button/Button.test.tsx (template-setter — copy header + describe layout verbatim)
    - packages/components/src/HelpText/HelpText.test.tsx (simple-component reference — similar shape to Card)
    - packages/components/src/_test/helpers.ts (helper signatures)
    - packages/components/TESTING-CONVENTIONS.md (Tier grammar + D-02a anti-pattern list)
  </read_first>
  <behavior>
    - Tier 1: renders title text + children; renders without crashing when no title is provided; className passes through additively (consumer class survives); `as` prop renders the requested element (assert via `container.firstChild` `tagName` OR by role for `article`/`section` if axe permits — fall back to `getByText(title).closest(...)` if needed without using querySelector)
    - Tier 2: when `href` is provided, the title renders as an anchor with `href={href}` and the accessible name matches the title; when `href` is provided, a `aria-hidden="true"` span sits inside the anchor (stretched-link pattern proof — do NOT use querySelector; use `screen.getByRole('link').firstElementChild` + `toHaveAttribute('aria-hidden', 'true')` per Plan 22-05 pattern); when no `href`, no `<a>` is rendered
    - Axe smoke: ≥ 2 `expectNoAxeViolations` calls — one for the plain (title + children) shape, one for the href stretched-link shape
  </behavior>
  <action>
    Create `packages/components/src/Card/Card.test.tsx`. Required shape (copy Button.test.tsx structure):

    1. Line 1: `// @vitest-environment jsdom`
    2. JSDoc block (lines 2–18 approx) starting `/**` and containing `WCAG SCs covered:` plus at minimum:
       - 1.3.1 Info and Relationships — title renders as an `<h3>`; stretched-link span is aria-hidden so AT does not double-announce the title
       - 2.4.4 Link Purpose (In Context) — when href is set, the link's accessible name is the title text
       - 4.1.2 Name, Role, Value — `as` prop preserves semantic role; axe-clean across configurations
    3. Imports:
       ```ts
       import { render, screen } from '@testing-library/react';
       import { describe, it, expect } from 'vitest';
       import { Card } from './Card';
       import { expectNoAxeViolations } from '../_test/helpers';
       ```
    4. Two top-level describes: `describe('Tier 1: Table Stakes', () => { ... })` and `describe('Tier 2: A11y Differentiators (Stretched-Link Composition)', () => { ... })`.

    Tier 1 `it(` blocks (target 4–5):
    - `mounts and renders children` — `render(<Card>Body</Card>)`; assert `screen.getByText('Body')` is in the document
    - `renders the title as an h3 when title is provided` — `render(<Card title="Card title">Body</Card>)`; assert `screen.getByRole('heading', { level: 3, name: /card title/i })` is in the document
    - `omits the title heading when title is not provided` — `render(<Card>Only body</Card>)`; assert `screen.queryByRole('heading')` is `null`
    - `appends consumer className additively (does not strip)` — render with `className="extra"`; assert the container element has class `extra` via `toHaveClass('extra')` on `screen.getByText('Body').parentElement` (the content `<div>`'s parent IS the container `<Component>`)
    - `as="article" renders an <article> element` — render `<Card as="article">x</Card>`; assert `container.firstChild` (returned by render) has `tagName === 'ARTICLE'` (per Plan 22-05 guidance: walk via `firstChild` instead of querySelector; jsdom returns the root); parametrize with `it.each(['div','article','section','li'])`

    Tier 2 `it(` blocks (target 3–4):
    - `href turns the title into a link with the title text as its accessible name` — `render(<Card title="Open" href="/x">body</Card>)`; assert `screen.getByRole('link', { name: /open/i })` is in the document and `toHaveAttribute('href', '/x')`
    - `href places an aria-hidden stretched-link span as the first child of the anchor` — `render(<Card title="Open" href="/x">body</Card>)`; const link = `screen.getByRole('link', { name: /open/i })`; assert `link.firstElementChild` `toHaveAttribute('aria-hidden', 'true')` AND `link.firstElementChild?.tagName === 'SPAN'` (NO querySelector)
    - `without href, no link is rendered` — `render(<Card title="Static" href={undefined}>body</Card>)`; assert `screen.queryByRole('link')` is `null`
    - `axe-clean for plain card (title + body)` — `const { container } = render(<Card title="t">body</Card>); await expectNoAxeViolations(container);`
    - `axe-clean for href stretched-link card` — `const { container } = render(<Card title="t" href="/x">body</Card>); await expectNoAxeViolations(container);`

    Total: 7–9 `it(` blocks. NO `fireEvent`, NO `querySelector`, NO `configureAxe`, NO `toMatchSnapshot`. Use `it.each` for the `as` prop parametrization to keep the it count tight.

    Atomic commit: `test(components): add Card Tier 1+2 suite (TC-15)`.
  </action>
  <verify>
    <automated>npm run test -w @holmdigital/components -- src/Card/Card.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/components/src/Card/Card.test.tsx` exists
    - `head -n 30 packages/components/src/Card/Card.test.tsx | grep -c 'WCAG SCs covered:'` returns 1
    - `grep -c "describe('Tier 1: Table Stakes'" packages/components/src/Card/Card.test.tsx` returns 1
    - `grep -c "describe('Tier 2:" packages/components/src/Card/Card.test.tsx` returns 1
    - `grep -c "expectNoAxeViolations" packages/components/src/Card/Card.test.tsx` returns ≥ 2
    - `grep -v '^[[:space:]]*\*' packages/components/src/Card/Card.test.tsx | grep -v '^[[:space:]]*//' | grep -cE 'querySelector|configureAxe|toMatchSnapshot|fireEvent'` returns 0
    - `grep -c '^[[:space:]]*it(' packages/components/src/Card/Card.test.tsx` returns ≥ 5 and ≤ 12 (it.each counts as 1 grep hit)
    - `npm run test -w @holmdigital/components -- Card.test --run` exits 0
    - `npm run test:wcag-headers -w @holmdigital/components` exits 0
  </acceptance_criteria>
  <done>Card test suite green; D-02a anti-pattern gates clean; WCAG-header guard passes.</done>
</task>

</tasks>

<verification>
- `npm run test -w @holmdigital/components -- Card.test --run` exits 0
- D-02a anti-pattern grep gates return 0 hits
- WCAG header marker present in first 30 lines
</verification>

<success_criteria>
Card component now has Tier 1 + Tier 2 contract coverage matching the Phase 22 template-setter. 1 of 7 new TC-15 test files complete.
</success_criteria>

<output>
Create `.planning/phases/32-tc-15-remaining-component-test-coverage/32-01-SUMMARY.md` recording the final `it(` count, helper-call count, and any deviations from the planned shape.
</output>
</content>
</invoke>