---
phase: 32-tc-15-remaining-component-test-coverage
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/components/src/Pagination/Pagination.test.tsx
autonomous: true
requirements: [TC-15]
must_haves:
  truths:
    - "Pagination.test.tsx exists colocated with Pagination.tsx and follows the Phase 22 template-setter shape"
    - "First 30 lines contain `WCAG SCs covered:`"
    - "Two top-level describes: Tier 1 + Tier 2"
    - "Tests cover: <nav> with aria-label, ariaLabel prop overrides default 'Pagination', Previous/Next buttons with aria-label, current page has aria-current='page', click on a page button fires onPageChange with that page, first-page disables Previous (button.disabled + aria-disabled), last-page disables Next, totalPages<=1 renders nothing, click+Tab focus parity on page buttons, axe-clean"
    - "Pagination has NO custom keyboard handler (per source) — Tier 2 arrow tests use the RadioGroup precedent: no-throw arrow assertions only (per Plan 22-06 deviations: components without custom keyboard handlers test the keystrokes-do-not-throw pattern, NOT focus movement)"
    - "Click-keyboard parity (D-02a): the Previous-disabled and Next-disabled tests assert click does nothing AND Enter on disabled button does nothing"
    - "Uses expectNoAxeViolations + userEvent; zero querySelector / configureAxe / toMatchSnapshot / fireEvent (D-02a)"
    - "8–14 it() blocks per D-02"
  artifacts:
    - path: "packages/components/src/Pagination/Pagination.test.tsx"
      provides: "Pagination Tier 1+2 contract (TC-15)"
      contains: "Tier 1: Table Stakes"
      min_lines: 130
  key_links:
    - from: "Pagination.test.tsx"
      to: "_test/helpers"
      via: "import"
      pattern: "from '\\.\\./_test/helpers'"
    - from: "Pagination.test.tsx"
      to: "./Pagination"
      via: "import { Pagination }"
      pattern: "from '\\./Pagination'"
---

<objective>
Add the Tier 1 + Tier 2 test suite for Pagination (TC-15). Pagination's Tier-2 contract is the APG navigation pattern: `<nav aria-label>` landmark + aria-current="page" + Prev/Next aria-labels + disabled-at-boundaries. Pagination's source has NO custom arrow-key keyboard handler — it relies on native Tab + Enter/Space activation on `<button>` elements. Therefore the keyboard surface tested is ONLY Tab/Enter/Space (the native button contract), per the Plan 22-06 RadioGroup precedent.

Purpose: Closes TC-15 row for Pagination; brings 7 of 7 new test files online in Wave 2.
Output: One colocated `.test.tsx` (~130–180 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-test-infra-and-first-7-components/22-05-PLAN.md
@.planning/phases/22-test-infra-and-first-7-components/22-06-SUMMARY.md
@packages/components/src/Pagination/Pagination.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/RadioGroup/RadioGroup.test.tsx
@packages/components/src/_test/helpers.ts

<interfaces>
PaginationProps (from Pagination.tsx):
- currentPage: number              (1-based, required)
- totalPages: number               (required)
- onPageChange: (page: number) => void
- ariaLabel?: string               (default 'Pagination')
- className?: string

Pagination is React.FC (NOT forwardRef).

Render shape:
- if (totalPages <= 1) return null;
- <nav aria-label={ariaLabel} className={className}>
-   <ul>
-     <li><button type="button" aria-label="Previous page" disabled={currentPage===1} aria-disabled={currentPage===1} onClick={() => onPageChange(currentPage-1)}>←</button></li>
-     {pages.map(p => <li>{p === '...' ? <span>…</span> : <button type="button" aria-current={currentPage===p ? 'page' : undefined} aria-label={`Page ${p}`} onClick={() => onPageChange(p)}>{p}</button>}</li>)}
-     <li><button type="button" aria-label="Next page" disabled={currentPage===totalPages} aria-disabled={currentPage===totalPages} onClick={() => onPageChange(currentPage+1)}>→</button></li>
-   </ul>
- </nav>

NO ArrowLeft/ArrowRight onKeyDown handler exists in source. Tests MUST NOT assert arrow-key focus-movement (Plan 22-06 RadioGroup precedent: such an assertion would fail in jsdom because native HTML buttons do NOT receive arrow-key roving without a handler). If you want an arrow test, assert that pressing arrow keys does NOT throw — keystrokes-do-not-throw pattern only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Pagination.test.tsx (TC-15)</name>
  <files>packages/components/src/Pagination/Pagination.test.tsx</files>
  <read_first>
    - packages/components/src/Pagination/Pagination.tsx (props + render shape — note: NO custom keyboard handler)
    - packages/components/src/RadioGroup/RadioGroup.test.tsx (no-custom-handler reference: keystrokes-do-not-throw pattern, Plan 22-06)
    - packages/components/src/Button/Button.test.tsx (template-setter)
    - packages/components/src/_test/helpers.ts (expectNoAxeViolations)
  </read_first>
  <behavior>
    - Tier 1: renders a <nav> with aria-label='Pagination' by default; ariaLabel prop overrides the default; renders Previous and Next buttons; renders a page button for currentPage; renders nothing (null) when totalPages <= 1; appends className to the <nav>
    - Tier 2: nav landmark is queryable via `screen.getByRole('navigation', { name: /pagination/i })`; current page button has `aria-current="page"`; non-current page buttons do NOT have aria-current; Previous button is disabled at currentPage===1 (both DOM `disabled` attribute AND `aria-disabled="true"`); Next button is disabled at currentPage===totalPages; clicking a non-current page button fires onPageChange with that page number; clicking Previous on page 1 does NOT fire onPageChange (disabled); clicking Next on the last page does NOT fire onPageChange; Tab moves focus through page buttons in DOM order (native button focus); axe-clean smoke
  </behavior>
  <action>
    Create `packages/components/src/Pagination/Pagination.test.tsx`. Required shape:

    1. Line 1: `// @vitest-environment jsdom`
    2. JSDoc header (lines 2–20) with `WCAG SCs covered:` and at minimum:
       - 1.3.1 Info and Relationships — nav landmark + aria-current expose pagination structure to AT
       - 2.4.4 Link/Button Purpose — Previous/Next + per-page aria-labels
       - 4.1.2 Name, Role, Value — role=navigation, aria-current=page, disabled state
       - Implementation note: Pagination has NO custom arrow-key handler; arrow-roving is intentionally out of scope (Plan 22-06 RadioGroup precedent). Native Tab cycles through buttons.
    3. Imports:
       ```ts
       import { render, screen } from '@testing-library/react';
       import userEvent from '@testing-library/user-event';
       import { describe, it, expect, vi } from 'vitest';
       import { Pagination } from './Pagination';
       import { expectNoAxeViolations } from '../_test/helpers';
       ```
    4. Two top-level describes: `Tier 1: Table Stakes` and `Tier 2: A11y Differentiators (APG Pagination — nav landmark + aria-current)`.

    Tier 1 `it(` blocks (target 4–5):
    - `mounts and renders a navigation landmark` — `render(<Pagination currentPage={1} totalPages={5} onPageChange={() => {}} />)`; assert `screen.getByRole('navigation')` is in the document
    - `applies the default aria-label "Pagination"` — same render; assert `screen.getByRole('navigation', { name: /pagination/i })` is in the document
    - `ariaLabel prop overrides the default` — `render(<Pagination currentPage={1} totalPages={5} onPageChange={() => {}} ariaLabel="Results pages" />)`; assert `screen.getByRole('navigation', { name: /results pages/i })` is in the document
    - `renders nothing when totalPages is 1` — `const { container } = render(<Pagination currentPage={1} totalPages={1} onPageChange={() => {}} />)`; assert `container.firstChild === null`
    - `appends consumer className to the nav additively` — render with `className="extra-pg"`; assert `screen.getByRole('navigation')` `toHaveClass('extra-pg')`

    Tier 2 `it(` blocks (target 6–9):
    - `current page button has aria-current="page"` — `render(<Pagination currentPage={3} totalPages={10} onPageChange={() => {}} />)`; assert `screen.getByRole('button', { name: /page 3/i })` `toHaveAttribute('aria-current', 'page')`
    - `non-current page buttons do not have aria-current` — same render; assert `screen.getByRole('button', { name: /page 2/i })` does NOT have aria-current (`not.toHaveAttribute('aria-current')`); same for page 1
    - `Previous button is disabled at currentPage===1` — `render(<Pagination currentPage={1} totalPages={5} onPageChange={() => {}} />)`; assert `screen.getByRole('button', { name: /previous page/i })` has DOM `disabled` attribute (`toBeDisabled()`) AND `toHaveAttribute('aria-disabled', 'true')`
    - `Next button is disabled at currentPage===totalPages` — `render(<Pagination currentPage={5} totalPages={5} onPageChange={() => {}} />)`; assert Next button `toBeDisabled()` AND `toHaveAttribute('aria-disabled', 'true')`
    - `clicking a page button fires onPageChange with that page` — `const onChange = vi.fn(); render(<Pagination currentPage={1} totalPages={5} onPageChange={onChange} />); const user = userEvent.setup(); await user.click(screen.getByRole('button', { name: /page 2/i }));` assert `onChange` called once with `2`
    - `clicking Previous on page 1 does not fire onPageChange (disabled)` — `const onChange = vi.fn(); render(<Pagination currentPage={1} totalPages={5} onPageChange={onChange} />); await user.click(screen.getByRole('button', { name: /previous page/i }));` assert `onChange` NOT called. PAIRED: also assert `await user.keyboard('{Enter}')` after focusing Previous does NOT fire onChange (D-02a click+keyboard parity)
    - `clicking Next on last page does not fire onPageChange (disabled)` — analogous to Previous-disabled
    - `Tab moves focus to the first interactive control then forwards through buttons in DOM order` — `render(<Pagination currentPage={2} totalPages={3} onPageChange={() => {}} />); const user = userEvent.setup(); await user.tab();` assert `document.activeElement === screen.getByRole('button', { name: /previous page/i })`; further `await user.tab()` lands on a page button (Plan 22-06 precedent: do not assert arrow-key focus movement — Pagination has no arrow handler)
    - `pressing ArrowLeft/ArrowRight on a focused page button does not throw` — focus a page button; `await expect(user.keyboard('{ArrowLeft}{ArrowRight}')).resolves.toBeUndefined();` (RadioGroup no-throw pattern, Plan 22-06)
    - `axe-clean for a typical pagination render (currentPage=3, totalPages=10)` — `const { container } = render(<Pagination currentPage={3} totalPages={10} onPageChange={() => {}} />); await expectNoAxeViolations(container);`
    - `axe-clean for a boundary state (currentPage=1, totalPages=5 — Previous disabled)` — second axe call covering the disabled-Previous shape

    Total: 10–14 `it(` blocks. Every disabled-click test has a paired disabled-keyboard test (D-02a). NO `fireEvent`, NO `querySelector`, NO `configureAxe`, NO `toMatchSnapshot`. The no-throw arrow test cites Plan 22-06 inline.

    Atomic commit: `test(components): add Pagination Tier 1+2 suite (TC-15)`.
  </action>
  <verify>
    <automated>npm run test -w @holmdigital/components -- src/Pagination/Pagination.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/components/src/Pagination/Pagination.test.tsx` exists
    - `head -n 30 packages/components/src/Pagination/Pagination.test.tsx | grep -c 'WCAG SCs covered:'` returns 1
    - `grep -c "describe('Tier 1: Table Stakes'" packages/components/src/Pagination/Pagination.test.tsx` returns 1
    - `grep -c "describe('Tier 2:" packages/components/src/Pagination/Pagination.test.tsx` returns 1
    - `grep -c "expectNoAxeViolations" packages/components/src/Pagination/Pagination.test.tsx` returns ≥ 2
    - `grep -c "aria-current" packages/components/src/Pagination/Pagination.test.tsx` returns ≥ 2 (present + absent)
    - `grep -c "Previous page\|Next page" packages/components/src/Pagination/Pagination.test.tsx` returns ≥ 3
    - `grep -v '^[[:space:]]*\*' packages/components/src/Pagination/Pagination.test.tsx | grep -v '^[[:space:]]*//' | grep -cE 'querySelector|configureAxe|toMatchSnapshot|fireEvent'` returns 0
    - `grep -c '^[[:space:]]*it(' packages/components/src/Pagination/Pagination.test.tsx` returns ≥ 8 and ≤ 15
    - `npm run test -w @holmdigital/components -- Pagination.test --run` exits 0
    - `npm run test:wcag-headers -w @holmdigital/components` exits 0
  </acceptance_criteria>
  <done>Pagination test suite green; nav landmark + aria-current + boundary-disabled pinned; no-throw arrow pattern documented; D-02a clean.</done>
</task>

</tasks>

<verification>
- `npm run test -w @holmdigital/components -- Pagination.test --run` exits 0
- D-02a grep gates clean
- No arrow-focus-movement assertions (RadioGroup precedent honored)
</verification>

<success_criteria>
Pagination has Tier 1 + Tier 2 contract coverage; the nav-landmark + aria-current=page + boundary-disabled contract is pinned. 7 of 7 new TC-15 test files complete — Wave 2 done.
</success_criteria>

<output>
Create `.planning/phases/32-tc-15-remaining-component-test-coverage/32-07-SUMMARY.md` recording final `it(` count and any deviations.
</output>
</content>
</invoke>