---
phase: 32-tc-15-remaining-component-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/Heading/Heading.test.tsx
autonomous: true
requirements: [TC-15]
must_haves:
  truths:
    - "Heading.test.tsx exists colocated with Heading.tsx and follows the Phase 22 template-setter shape"
    - "First 30 lines contain the literal marker `WCAG SCs covered:`"
    - "Two top-level describes: Tier 1: Table Stakes and Tier 2: A11y Differentiators"
    - "Tests cover: each level 1–6 renders the correct h1–h6 tag (parametrized via it.each), forwardRef wires up to the rendered heading element, className passthrough, arbitrary HTML attributes pass through (id, data-*), axe-clean smoke"
    - "Heading.tsx source is NOT modified (TS2590 createElement guard is load-bearing — CLAUDE.md gotcha)"
    - "Uses expectNoAxeViolations; zero querySelector / configureAxe / toMatchSnapshot / fireEvent (D-02a)"
    - "5–10 it() blocks per D-02"
  artifacts:
    - path: "packages/components/src/Heading/Heading.test.tsx"
      provides: "Heading Tier 1+2 contract (TC-15)"
      contains: "Tier 1: Table Stakes"
      min_lines: 90
  key_links:
    - from: "Heading.test.tsx"
      to: "_test/helpers"
      via: "import"
      pattern: "from '\\.\\./_test/helpers'"
    - from: "Heading.test.tsx"
      to: "./Heading"
      via: "import { Heading }"
      pattern: "from '\\./Heading'"
---

<objective>
Add the Tier 1 + Tier 2 test suite for Heading (TC-15). Heading is a forwardRef component whose Tier-2 contract is "the `level` prop maps 1→h1 … 6→h6 semantically". Uses `React.createElement` with a narrow union to dodge TS2590 — DO NOT touch the source.

Purpose: Closes TC-15 row for Heading; brings 3 of 7 new test files online in Wave 1.
Output: One colocated `.test.tsx` (~80–120 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-test-infra-and-first-7-components/22-05-PLAN.md
@packages/components/src/Heading/Heading.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/_test/helpers.ts
@CLAUDE.md

<interfaces>
HeadingProps (from Heading.tsx):
- level: 1 | 2 | 3 | 4 | 5 | 6   (required)
- extends React.HTMLAttributes<HTMLHeadingElement>
- forwardRef<HTMLHeadingElement, HeadingProps>
- Uses React.createElement(`h${level}` as union) to avoid TS2590 — see CLAUDE.md "Known Issues & Solutions" → "TS2590"

Render shape:
- <Component>{children}</Component> where Component is 'h1'..'h6' chosen from `level`
- Display name: 'Heading'

CRITICAL: do NOT modify Heading.tsx. The `React.createElement(Tag, ...)` form is load-bearing — a JSX <Tag /> form would re-trigger TS2590. The test file only consumes the component.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Heading.test.tsx (TC-15)</name>
  <files>packages/components/src/Heading/Heading.test.tsx</files>
  <read_first>
    - packages/components/src/Heading/Heading.tsx (props + createElement form — DO NOT MODIFY)
    - packages/components/src/Button/Button.test.tsx (template-setter, ref-forwarding pattern)
    - packages/components/src/_test/helpers.ts (expectNoAxeViolations)
    - CLAUDE.md (TS2590 gotcha — source is off-limits in this plan)
  </read_first>
  <behavior>
    - Tier 1: renders children; className passes through additively; arbitrary HTML attributes pass through (id, data-*, aria-*); forwardRef wires the ref to the rendered heading element (the ref.current is an HTMLHeadingElement and its tagName matches the level)
    - Tier 2: level={1} renders an h1, level={2} renders h2, … level={6} renders h6 (parametrized via it.each) — the SEMANTIC level matters for screen-reader heading navigation (SC 2.4.6); axe-clean smoke
  </behavior>
  <action>
    Create `packages/components/src/Heading/Heading.test.tsx`. Required shape:

    1. Line 1: `// @vitest-environment jsdom`
    2. JSDoc header (lines 2–18) with `WCAG SCs covered:` and at minimum:
       - 1.3.1 Info and Relationships — `level` prop renders the correct semantic h1–h6 element (this is the whole point of the component)
       - 2.4.6 Headings and Labels — heading-level navigation works because levels are correct
       - 4.1.2 Name, Role, Value — heading role is conferred by the native HTML tag; axe-clean
    3. Imports:
       ```ts
       import { useRef } from 'react';
       import { render, screen } from '@testing-library/react';
       import { describe, it, expect } from 'vitest';
       import { Heading } from './Heading';
       import { expectNoAxeViolations } from '../_test/helpers';
       ```
    4. Two top-level describes: `Tier 1: Table Stakes` and `Tier 2: A11y Differentiators (Semantic Heading Level)`.

    Tier 1 `it(` blocks (target 3–4):
    - `mounts and renders children` — `render(<Heading level={1}>Page</Heading>)`; assert `screen.getByRole('heading', { name: /page/i })` is in the document
    - `forwards ref to the rendered heading element` — use the Button.test.tsx ref-probe pattern: a `Probe` component with `useRef<HTMLHeadingElement>(null)` and a callback-ref captures the node; assert `captured instanceof HTMLHeadingElement` and `captured.tagName === 'H2'` when `level={2}`
    - `appends consumer className additively` — `render(<Heading level={2} className="extra">x</Heading>)`; assert `screen.getByRole('heading').classList.contains('extra')` (or `toHaveClass('extra')`)
    - `passes arbitrary HTML attributes (id, data-*) through to the heading` — `render(<Heading level={3} id="sec" data-foo="bar">x</Heading>)`; assert the heading element has `id="sec"` and `data-foo="bar"` via `toHaveAttribute`

    Tier 2 `it(` blocks (target 3–4):
    - `renders h1 through h6 for level 1–6` — `it.each([1, 2, 3, 4, 5, 6] as const)('level=%i renders h%i', (level) => { render(<Heading level={level}>x</Heading>); const heading = screen.getByRole('heading', { level }); expect(heading.tagName).toBe(\`H\${level}\`); })` — this is the SINGLE most important assertion in the file (the createElement contract)
    - `screen.getByRole('heading', { level }) succeeds for each level` — already covered above; if you split, parametrize separately
    - `axe-clean for h1 + body content` — `const { container } = render(<><Heading level={1}>Page</Heading><p>body</p></>); await expectNoAxeViolations(container);`
    - `axe-clean for h2..h6` — second axe call on a representative level (e.g., level={3})

    Total: 6–8 `it(` blocks. NO `fireEvent`, NO `querySelector`, NO `configureAxe`, NO `toMatchSnapshot`. The level-parametrized it.each counts as one source `it(` but produces 6 test cases.

    Atomic commit: `test(components): add Heading Tier 1+2 suite (TC-15)`.
  </action>
  <verify>
    <automated>npm run test -w @holmdigital/components -- src/Heading/Heading.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/components/src/Heading/Heading.test.tsx` exists
    - `head -n 30 packages/components/src/Heading/Heading.test.tsx | grep -c 'WCAG SCs covered:'` returns 1
    - `grep -c "describe('Tier 1: Table Stakes'" packages/components/src/Heading/Heading.test.tsx` returns 1
    - `grep -c "describe('Tier 2:" packages/components/src/Heading/Heading.test.tsx` returns 1
    - `grep -c "expectNoAxeViolations" packages/components/src/Heading/Heading.test.tsx` returns ≥ 2
    - `grep -c "it.each" packages/components/src/Heading/Heading.test.tsx` returns ≥ 1 (level parametrization)
    - `grep -v '^[[:space:]]*\*' packages/components/src/Heading/Heading.test.tsx | grep -v '^[[:space:]]*//' | grep -cE 'querySelector|configureAxe|toMatchSnapshot|fireEvent'` returns 0
    - `grep -c '^[[:space:]]*it(' packages/components/src/Heading/Heading.test.tsx` returns ≥ 4 and ≤ 10
    - Heading.tsx is byte-equivalent to its current state (no source modification): `git diff packages/components/src/Heading/Heading.tsx` is empty
    - `npm run test -w @holmdigital/components -- Heading.test --run` exits 0
    - `npm run test:wcag-headers -w @holmdigital/components` exits 0
  </acceptance_criteria>
  <done>Heading test suite green; semantic h1–h6 mapping pinned; source untouched.</done>
</task>

</tasks>

<verification>
- `npm run test -w @holmdigital/components -- Heading.test --run` exits 0
- D-02a anti-pattern grep gates clean
- Heading.tsx unchanged (TS2590 guard preserved)
</verification>

<success_criteria>
Heading has Tier 1 + Tier 2 contract coverage; the createElement→level→tag mapping is pinned in a regression test. 3 of 7 new TC-15 test files complete.
</success_criteria>

<output>
Create `.planning/phases/32-tc-15-remaining-component-test-coverage/32-03-SUMMARY.md` recording final `it(` count and any deviations.
</output>
</content>
</invoke>