---
phase: 32-tc-15-remaining-component-test-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/SkipLink/SkipLink.test.tsx
autonomous: true
requirements: [TC-15]
must_haves:
  truths:
    - "SkipLink.test.tsx exists colocated with SkipLink.tsx and follows the Phase 22 template-setter shape"
    - "First 30 lines contain the literal marker `WCAG SCs covered:`"
    - "Two top-level describes: Tier 1: Table Stakes and Tier 2: A11y Differentiators"
    - "Tests cover: renders an <a> with href=#<targetId>, defaults targetId to 'main', forwards ref, className passthrough, default Swedish text 'Hoppa till huvudinnehåll' when no children, custom children override default text, Tab moves focus to the link, axe-clean"
    - "Swedish characters å/ä/ö preserved in test strings (CLAUDE.md user memory)"
    - "Uses expectNoAxeViolations + userEvent for Tab focus; zero querySelector / configureAxe / toMatchSnapshot / fireEvent (D-02a)"
    - "5–10 it() blocks per D-02"
  artifacts:
    - path: "packages/components/src/SkipLink/SkipLink.test.tsx"
      provides: "SkipLink Tier 1+2 contract (TC-15)"
      contains: "Tier 1: Table Stakes"
      min_lines: 90
  key_links:
    - from: "SkipLink.test.tsx"
      to: "_test/helpers"
      via: "import"
      pattern: "from '\\.\\./_test/helpers'"
    - from: "SkipLink.test.tsx"
      to: "./SkipLink"
      via: "import { SkipLink }"
      pattern: "from '\\./SkipLink'"
---

<objective>
Add the Tier 1 + Tier 2 test suite for SkipLink (TC-15). SkipLink's Tier-2 contract is WCAG 2.4.1 Bypass Blocks — Tab reaches the link, the link points at `#<targetId>`, and the default Swedish copy is preserved.

Purpose: Closes TC-15 row for SkipLink; brings 4 of 7 new test files online in Wave 1.
Output: One colocated `.test.tsx` (~90–130 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-test-infra-and-first-7-components/22-05-PLAN.md
@packages/components/src/SkipLink/SkipLink.tsx
@packages/components/src/Button/Button.test.tsx
@packages/components/src/_test/helpers.ts
@CLAUDE.md

<interfaces>
SkipLinkProps (from SkipLink.tsx):
- targetId?: string                      (default 'main')
- extends React.AnchorHTMLAttributes<HTMLAnchorElement>
- forwardRef<HTMLAnchorElement, SkipLinkProps>
- displayName: 'SkipLink'

Render shape:
- <a ref={ref} href={`#${targetId}`} className="<tailwind utilities + consumer className>" style={{ zIndex: 9999, ...style }} {...props}>
-   {children || 'Hoppa till huvudinnehåll'}
- </a>

Default copy is Swedish — preserve å in 'Hoppa till huvudinnehåll'.
NOTE: SkipLink uses Tailwind class names for the off-screen → on-focus transform. jsdom cannot measure layout/transforms; tests must NOT assert on computed styles. Focus assertion = `document.activeElement` only (per Plan 22-05 guidance).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SkipLink.test.tsx (TC-15)</name>
  <files>packages/components/src/SkipLink/SkipLink.test.tsx</files>
  <read_first>
    - packages/components/src/SkipLink/SkipLink.tsx (props + default text — Swedish characters)
    - packages/components/src/Button/Button.test.tsx (template-setter, ref-forwarding + focus pattern)
    - packages/components/src/_test/helpers.ts (expectNoAxeViolations)
    - CLAUDE.md (Swedish chars rule: preserve å, ä, ö verbatim)
  </read_first>
  <behavior>
    - Tier 1: renders an anchor with href starting with `#`; defaults targetId to 'main' (href === '#main'); custom targetId="content" → href === '#content'; forwards ref to the <a>; className passes through additively; arbitrary anchor attributes (id, data-*) pass through
    - Tier 2: renders default Swedish text 'Hoppa till huvudinnehåll' when no children are provided; custom children override the default; Tab from the body moves focus to the link (document.activeElement === link); axe-clean smoke
  </behavior>
  <action>
    Create `packages/components/src/SkipLink/SkipLink.test.tsx`. Required shape:

    1. Line 1: `// @vitest-environment jsdom`
    2. JSDoc header (lines 2–18) with `WCAG SCs covered:` and at minimum:
       - 2.4.1 Bypass Blocks — the entire purpose of this component
       - 2.4.7 Focus Visible — focus assertion (link receives focus on Tab; visual transform deferred to real-browser smoke)
       - 4.1.2 Name, Role, Value — accessible name comes from link text; axe-clean
    3. Imports:
       ```ts
       import { useRef } from 'react';
       import { render, screen } from '@testing-library/react';
       import userEvent from '@testing-library/user-event';
       import { describe, it, expect } from 'vitest';
       import { SkipLink } from './SkipLink';
       import { expectNoAxeViolations } from '../_test/helpers';
       ```
    4. Two top-level describes: `Tier 1: Table Stakes` and `Tier 2: A11y Differentiators (Bypass Blocks)`.

    Tier 1 `it(` blocks (target 4–5):
    - `mounts and renders an anchor` — `render(<SkipLink />)`; assert `screen.getByRole('link')` is in the document
    - `defaults targetId to "main" (href="#main")` — `render(<SkipLink />)`; assert `screen.getByRole('link')` `toHaveAttribute('href', '#main')`
    - `accepts a custom targetId` — `render(<SkipLink targetId="content" />)`; assert link `toHaveAttribute('href', '#content')`
    - `forwards ref to the underlying anchor` — Probe pattern with `useRef<HTMLAnchorElement>(null)`; assert `captured instanceof HTMLAnchorElement` and `captured.tagName === 'A'`
    - `appends consumer className additively` — `render(<SkipLink className="extra-class" />)`; assert `screen.getByRole('link')` `toHaveClass('extra-class')`

    Tier 2 `it(` blocks (target 4–5):
    - `renders default Swedish copy "Hoppa till huvudinnehåll" when no children provided` — `render(<SkipLink />)`; assert `screen.getByRole('link', { name: /hoppa till huvudinnehåll/i })` is in the document — IMPORTANT: this regex MUST contain the literal å characters (do not transliterate to "huvudinnehall"); CLAUDE.md user-memory rule
    - `custom children override the default text` — `render(<SkipLink>Skip to content</SkipLink>)`; assert `screen.getByRole('link', { name: /skip to content/i })` is in the document and `screen.queryByText(/huvudinnehåll/i)` is null
    - `receives focus when Tab is pressed from document.body` — render `<SkipLink />`; `const user = userEvent.setup(); await user.tab();` assert `document.activeElement === screen.getByRole('link')`; this is the canonical 2.4.1 Bypass-Blocks proof under jsdom (visual transform cannot be measured but focus order can)
    - `axe-clean for default skip link` — `const { container } = render(<SkipLink />); await expectNoAxeViolations(container);`
    - `axe-clean with custom targetId and children` — `const { container } = render(<SkipLink targetId="content">Skip to content</SkipLink>); await expectNoAxeViolations(container);`

    Total: 7–10 `it(` blocks. Swedish å MUST appear verbatim in at least one test string. NO `fireEvent`, NO `querySelector`, NO `configureAxe`, NO `toMatchSnapshot`. Visual transform tests (Tailwind `-translate-y-[150%] focus:translate-y-0`) are explicitly NOT in scope under jsdom — comment this in a JSDoc note above the Tier 2 describe.

    Atomic commit: `test(components): add SkipLink Tier 1+2 suite (TC-15)`.
  </action>
  <verify>
    <automated>npm run test -w @holmdigital/components -- src/SkipLink/SkipLink.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/components/src/SkipLink/SkipLink.test.tsx` exists
    - `head -n 30 packages/components/src/SkipLink/SkipLink.test.tsx | grep -c 'WCAG SCs covered:'` returns 1
    - `grep -c "describe('Tier 1: Table Stakes'" packages/components/src/SkipLink/SkipLink.test.tsx` returns 1
    - `grep -c "describe('Tier 2:" packages/components/src/SkipLink/SkipLink.test.tsx` returns 1
    - `grep -c "expectNoAxeViolations" packages/components/src/SkipLink/SkipLink.test.tsx` returns ≥ 2
    - `grep -c "huvudinnehåll" packages/components/src/SkipLink/SkipLink.test.tsx` returns ≥ 1 (Swedish-char preservation)
    - `grep -v '^[[:space:]]*\*' packages/components/src/SkipLink/SkipLink.test.tsx | grep -v '^[[:space:]]*//' | grep -cE 'querySelector|configureAxe|toMatchSnapshot|fireEvent'` returns 0
    - `grep -c '^[[:space:]]*it(' packages/components/src/SkipLink/SkipLink.test.tsx` returns ≥ 6 and ≤ 12
    - `npm run test -w @holmdigital/components -- SkipLink.test --run` exits 0
    - `npm run test:wcag-headers -w @holmdigital/components` exits 0
  </acceptance_criteria>
  <done>SkipLink test suite green; 2.4.1 Bypass-Blocks contract pinned; Swedish copy preserved.</done>
</task>

</tasks>

<verification>
- `npm run test -w @holmdigital/components -- SkipLink.test --run` exits 0
- D-02a grep gates clean
- Swedish å in test file
</verification>

<success_criteria>
SkipLink has Tier 1 + Tier 2 contract coverage; WCAG 2.4.1 Bypass-Blocks proof pinned. 4 of 7 new TC-15 test files complete — Wave 1 done.
</success_criteria>

<output>
Create `.planning/phases/32-tc-15-remaining-component-test-coverage/32-04-SUMMARY.md` recording final `it(` count and any deviations.
</output>
</content>
</invoke>